# 2026-06-08 체류시간 증가 회의

## 1. 회의 목적

현재 LeeTrader에는 메인, 종목 상세화면, AI 자동매매, 수동매매, 랭킹분석, AI 성과 메뉴가 있다.

체류시간을 늘리기 위한 핵심 방향은 사용자를 억지로 오래 붙잡는 것이 아니라, 사용자가 매일 들어와서 더 많이 확인하고 더 깊게 탐색할 이유를 만드는 것이다.

현재 서비스의 자연스러운 사용 흐름은 다음과 같다.

1. 오늘 어떤 종목을 봐야 하는지 확인한다.
2. 왜 이 종목이 추천됐는지 확인한다.
3. 자동매매 또는 수동매매에서 어떻게 대응할지 판단한다.
4. AI 추천과 실제 성과가 믿을 만한지 검증한다.
5. 다음 날 다시 바뀐 내용을 확인한다.

따라서 체류시간 증가 전략은 다음 네 가지 축으로 설계한다.

| 축 | 설명 |
| --- | --- |
| 변화 | 어제와 오늘 사이 바뀐 종목, 점수, 리스크, 순위를 보여준다. |
| 이유 | AI가 왜 추천하거나 제외했는지 판단 근거를 설명한다. |
| 비교 | 종목끼리, 전략끼리, 과거 사례와 현재를 비교하게 한다. |
| 다음 행동 | 관심 추가, 비교 추가, 상세 분석, 모의투자, 주문 검토로 이어지게 한다. |

## 2. 최우선 결론

우선순위는 다음 순서로 진행한다.

| 우선순위 | 과제 | 기대 효과 | 난이도 | 추천 적용 순서 |
| --- | --- | --- | --- | --- |
| 1 | 오늘의 변화 알림 + 메인 대시보드 강화 | 재방문, 첫 화면 체류 증가 | 중 | 가장 먼저 |
| 2 | 종목 상세화면 분석 여정 강화 | 종목별 탐색 시간 증가 | 중상 | 1순위와 병행 가능 |
| 3 | AI 판단 로그 / 자동매매 관제화 | 자동매매 신뢰도와 관찰 시간 증가 | 중상 | 1, 2 이후 |
| 4 | 랭킹분석 탐색형 리그화 + 종목 비교 | 리스트 탐색 시간 증가 | 중 | 2, 3과 연동 |
| 5 | 수동매매 의사결정 보조 | 주문 전 체류 및 전환 증가 | 중상 | 거래 기능 안정 후 |
| 6 | AI 성과 화면 신뢰 콘텐츠화 | 장기 신뢰, 재방문 증가 | 중 | 데이터 누적과 함께 |
| 7 | 개인화 관심종목 대시보드 | 반복 방문 습관화 | 상 | 로그인/저장 구조 확인 후 |

## 3. 우선순위 1: 오늘의 변화 알림 + 메인 대시보드 강화

### 목표

메인 화면을 단순 추천 종목 리스트가 아니라, 사용자가 매일 확인해야 하는 "오늘의 투자 대시보드"로 만든다.

현재 화면은 오늘의 추천 결과를 보여주는 힘은 있지만, 사용자가 어제도 봤다면 오늘 왜 다시 봐야 하는지에 대한 변화 정보가 약하다. 체류시간과 재방문을 늘리려면 "오늘 달라진 것"을 첫 화면에서 바로 보여줘야 한다.

### 구현할 기능

| 기능 | 설명 |
| --- | --- |
| 오늘 새로 진입한 종목 | Top 20 또는 추천 후보에 새로 들어온 종목 표시 |
| 점수 급등 종목 | 전일 대비 final_score 상승폭 상위 종목 |
| 순위 급등 종목 | 전일 대비 ranking_rank 상승폭 상위 종목 |
| 리스크 증가 종목 | risk 관련 점수 또는 경고 수가 증가한 종목 |
| 수급 개선 종목 | 외국인/기관 수급이 개선된 종목 |
| 관심 필요 요약 | "오늘 새로 진입 3개, 리스크 증가 5개, 점수 급등 4개" 형태 요약 |

### 2026-06-08 구현 기준 메모

순위는 상대 지표라 하루 변동성이 크다. 따라서 메인 화면에서는 "랭킹 급등"을 단독으로 노출하지 않고, **오늘의 의미 있는 변화**로 표현한다.

현재 구현 기준은 다음과 같다.

| 변화 유형 | 노출 기준 |
| --- | --- |
| Top20 신규 | 전 비교일에는 Top20 밖, 오늘은 Top20 안 |
| 점수 상승 | 점수 +5 이상 또는 점수 +3 이상이면서 순위 10위 이상 상승 |
| 구간 상승 | 100위 밖/Top100/Top50/Top20 구간이 상승하고 점수 +2 이상 |
| 리스크 완화 | 리스크 페널티가 3 이상 감소하고 현재 100위 이내 |
| 순위 노이즈 | 순위 20위 이상 변동했지만 점수 변화가 2 미만인 경우. 요약에만 표시하고 주요 카드에서는 제외 |

### 2026-06-09 화면 적용 후 결정

실제 메인 화면에 적용해보니, 변화 영역을 큰 카드 섹션으로 펼쳐두면 사용자가 실제 랭킹 테이블을 보기 전에 지칠 수 있다. 메인은 "랭킹을 보러 온 화면"이므로, 변화 정보는 독립 콘텐츠처럼 크게 보여주기보다 랭킹 테이블을 보조하는 방식이 적합하다.

최종 적용 방향은 다음과 같다.

| 항목 | 결정 |
| --- | --- |
| 기본 노출 | `오늘의 의미 있는 변화`는 기본 접힘 상태 |
| 상단 문구 | 숫자 나열 대신 "현재 순위순 · 새 Top20 10개 · 점수 개선 많음 · 순위 노이즈 10개 제외 참고"처럼 해석형 한 줄로 표시 |
| 상세 펼침 | 사용자가 `자세히`를 누를 때만 변화 카드 표시 |
| 상세 카드 수 | 6개 표시 |
| 상세 카드 정렬 | 현재 랭킹 순위순 |
| 상세 카드 형태 | 한 줄에 6개 종목이 보이도록 압축 카드 사용 |
| 카드 내부 메트릭 | `순위/점수/리스크`를 큰 박스 3개가 아니라 얇은 한 줄 메타 정보로 표시 |
| 핵심 노출 위치 | 변화 종목 전체는 메인 랭킹 테이블의 종목명 옆 작은 뱃지로 표시 |

### 2026-06-09 추가 화면 조정

메인 상단의 `기준일/시장 흐름/수급 긍정/점수 우수/리스크 주의/운용 톤/상위20 점수/리스크 밀도/운영 후보` 카드들이 실제 랭킹 테이블 진입을 방해했다. 특히 수급/점수/리스크 3개 카드는 독립 카드로 볼 만큼 시선 효율이 높지 않았다.

다만 너무 작은 1줄 칩 형태로 압축하면 데이터가 눈에 들어오지 않는다. 따라서 상단 운영 요약은 다음처럼 조정한다.

| 기존 | 변경 |
| --- | --- |
| 5개 시장/추천 카드 + 4개 운영 카드 | 핵심 4개 카드 |
| 수급/점수/리스크를 각각 큰 카드로 표시 | 수급 카드는 제거, 점수/리스크는 운영 카드로 유지 |
| 운용 톤/상위20 점수/리스크 밀도/운영 후보를 2번째 줄 카드로 표시 | `시장 흐름`, `상위20 점수`, `리스크 밀도`, `운영 후보` 4개 카드만 표시 |

4개 카드 구성:

| 카드 | 내용 |
| --- | --- |
| 시장 흐름 | 기준일, 장세, 보수/공격 대응 톤 |
| 상위20 점수 | 상위20 평균 점수, 70점 이상 후보 수 |
| 리스크 밀도 | risk penalty 12 이상 종목 수 |
| 운영 검토 | 우선 검토 후보 수 / 주의 관찰 후보 수 |

이 조정의 목적은 **랭킹 테이블이 메인 화면의 주인공으로 빨리 보이게 하되, 중요한 운영 데이터는 카드 형태로 읽히게 하는 것**이다. 상단 정보는 사용자가 랭킹을 읽기 위한 상태 표시로만 남긴다.

운영 검토 용어:

| 용어 | 의미 |
| --- | --- |
| 우선 검토 | 운영 기준상 즉시 검토 우선순위가 높은 후보 |
| 주의 관찰 | 점수나 랭킹상 관찰 가치는 있지만 즉시 매수보다 확인이 필요한 후보 |

주의 관찰 선정은 `manual-trading/summary`의 `caution_candidates`를 사용한다. 우선 검토 후보를 먼저 제외한 뒤, `WATCHLIST`, `PAPER_ONLY`, `BLOCK`, 진입조건 미충족, 확신도 약화, 과열 플래그 등 운영 상태를 기준으로 분류한다.

메인 테이블에서는 주의 관찰 종목의 종목명 옆에 파란 `주의관찰` 뱃지를 붙인다. 이 뱃지는 수익/위험 신호가 아니라 "운영 검토상 바로 진입보다 관찰이 필요한 종목"이라는 의미다.

현재 구현 API:

```http
GET /api/ranking/meaningful-changes?date=YYYY-MM-DD
```

응답 핵심 필드:

| 필드 | 설명 |
| --- | --- |
| `summary` | Top20 신규, 점수 상승, 구간 상승, 리스크 완화, 순위 노이즈 요약 |
| `featured` | 펼침 영역에 표시할 현재 순위순 대표 변화 종목 |
| `items` | 메인 랭킹 테이블 뱃지에 사용할 전체 의미 있는 변화 종목 |

프론트 적용 방식:

| 위치 | 적용 |
| --- | --- |
| 변화 섹션 | 기본 접힘, 요약 한 줄만 표시 |
| 변화 상세 | 펼침 시 6개 압축 카드 |
| 메인 테이블 | 종목명 옆 `Top20 신규`, `점수 +5 이상`, `Top50 → Top20`, `리스크 완화` 등 작은 뱃지 표시 |
| 모바일 카드 | 종목명 옆 동일 뱃지 표시 |

이 결정의 핵심은 "변화 섹션을 키우는 것"이 아니라 **랭킹 테이블 자체를 더 똑똑하게 만드는 것**이다.

### 화면 구성

메인 상단에 현재의 시장 상태 카드 아래 또는 랭킹 테이블 위에 "오늘의 변화" 영역을 추가하되, 기본값은 접힘 상태로 둔다.

권장 UI:

| UI 요소 | 내용 |
| --- | --- |
| 요약 한 줄 | 현재 순위순, 새 Top20 수, 점수 개선 강도, 순위 노이즈 수 |
| 접기/자세히 버튼 | 변화 상세 영역 토글 |
| 압축 카드 | 펼침 시 현재 순위순 6개 종목 표시 |
| 테이블 뱃지 | 의미 있는 변화 종목은 랭킹 테이블 종목명 옆에 직접 표시 |
| 액션 | 변화 카드 또는 테이블 행 클릭 시 종목 상세 이동 |

예시 문구:

```text
오늘 새로 포착된 종목 3개
전일 대비 AI 점수가 크게 오른 종목입니다.
```

```text
리스크 경고 증가 5개
변동성 또는 수급 악화 신호가 커진 종목입니다.
```

### 데이터 구현

현재 문서 기준 주요 데이터는 `data/ranking_final.csv`, `serving/daily_recommendations.json`, `public.daily_ranking` 계열을 활용할 수 있다.

필요한 비교 기준:

| 항목 | 계산 방식 |
| --- | --- |
| 전일 점수 | 직전 거래일의 `final_score` 또는 `live_score` |
| 점수 변화 | 오늘 점수 - 전일 점수 |
| 전일 순위 | 직전 거래일의 `ranking_rank` |
| 순위 변화 | 전일 순위 - 오늘 순위. 양수면 순위 상승 |
| 신규 진입 여부 | 전일 Top N 밖 또는 미존재, 오늘 Top N 안 |
| 리스크 변화 | 오늘 risk flag 개수 - 전일 risk flag 개수 |
| 수급 변화 | 외국인/기관 5일 수급 방향 변화 |

### 백엔드 구현

현재 구현 API:

```http
GET /api/ranking/meaningful-changes?date=YYYY-MM-DD
```

응답 예시:

```json
{
  "asof_date": "2026-06-08",
  "compare_date": "2026-06-05",
  "summary": {
    "new_top20_count": 10,
    "score_jump_count": 53,
    "band_up_count": 32,
    "risk_relief_count": 8,
    "rank_noise_count": 10
  },
  "featured": [
    {
      "code": "214450",
      "name": "파마리서치",
      "rank": 2,
      "previous_rank": 22,
      "rank_delta": 20,
      "score_delta": 15.3,
      "tags": ["Top20 신규", "점수 +5 이상", "Top50 → Top20"]
    }
  ],
  "items": []
}
```

구현 위치 후보:

| 영역 | 작업 |
| --- | --- |
| 랭킹 생성 파이프라인 | 일별 랭킹 저장 시 전일 대비 delta 계산 |
| Node API | `/api/ranking/meaningful-changes` 추가 |
| 프론트 | 변화 요약은 접힘 영역, 핵심 변화는 랭킹 테이블 뱃지로 렌더링 |

### 프론트 구현

메인 화면에 다음 상태를 구현한다.

| 상태 | 처리 |
| --- | --- |
| 데이터 있음 | 접힘 요약 한 줄 표시, 펼침 시 6개 압축 카드 표시 |
| 전일 데이터 없음 | "비교 기준 데이터 없음" 대신 오늘 랭킹 요약만 표시 |
| 장중 갱신 전 | 마지막 갱신 시각 표시 |
| 오류 | 메인 랭킹은 유지하고 변화 영역만 조용히 fallback |

### 지표

| 지표 | 목표 |
| --- | --- |
| 메인 평균 체류시간 | 증가 |
| 랭킹 행 클릭률 | 증가 |
| 종목 상세 진입률 | 증가 |
| 재방문율 | 증가 |

## 4. 우선순위 2: 종목 상세화면 분석 여정 강화

### 목표

종목 상세화면을 단순 정보 나열이 아니라 "AI 분석을 따라가며 판단하는 화면"으로 만든다.

사용자가 종목 상세에서 오래 머무는 이유는 차트 자체가 아니라 다음 질문에 답을 얻기 위해서다.

1. 왜 이 종목이 추천됐는가?
2. 지금 가격이 좋은 위치인가?
3. 어떤 조건이면 매수 또는 관망인가?
4. 틀렸을 때 어디서 위험을 줄여야 하는가?
5. 과거 비슷한 사례에서는 어떻게 움직였는가?

### 구현할 기능

| 섹션 | 설명 |
| --- | --- |
| AI 종합 의견 | 한 문단 요약. 추천 강도, 관망/주의 여부 포함 |
| 추천 근거 분해 | 점수에 기여한 상위 요인 3~5개 |
| 리스크 분해 | 감점 또는 주의 요인 3~5개 |
| 가격 위치 | 현재가가 20일/60일 추세, 변동성 구간에서 어디인지 표시 |
| 수급 추적 | 외국인/기관 5일, 20일 방향성 |
| 시나리오 | 상승, 횡보, 하락 조건별 대응 |
| 과거 유사 사례 | 유사한 점수/수급/기술 패턴 이후 성과 |
| 다음 행동 | 관심 추가, 비교 추가, 수동매매 검토, 모의투자 보기 |

### 화면 구성

권장 순서:

1. 상단: 종목명, 점수, 순위, 전일 대비 변화, 리스크 상태
2. AI 요약: "현재 AI 판단"
3. 왜 추천됐나: 긍정 요인
4. 왜 조심해야 하나: 리스크 요인
5. 지금 가격 위치: 차트 + 주요 기준선
6. 시나리오: 상승 / 횡보 / 하락
7. 과거 유사 사례
8. 행동 버튼

### 데이터 구현

이미 존재하거나 문서상 확인되는 설명 컬럼을 우선 활용한다.

| 데이터 | 용도 |
| --- | --- |
| `score_explain_summary` | AI 요약 문장 |
| `top_driver` | 핵심 추천 근거 |
| `risk_factor` | 핵심 리스크 |
| `action_note` | 대응 메모 |
| `final_score` / `live_score` | 점수 |
| `ranking_rank` | 순위 |
| 기대수익 관련 컬럼 | 상승 여력 설명 |
| 수급 관련 컬럼 | 외국인/기관 흐름 |
| 가격/이동평균 | 기술적 위치 |

추가 계산이 필요한 항목:

| 항목 | 계산 방식 |
| --- | --- |
| 전일 대비 점수 변화 | 오늘 점수 - 전일 점수 |
| 점수 기여도 | 점수 산식의 factor별 contribution |
| 가격 위치 | 현재가와 MA20, MA60, 60일 고점/저점 비교 |
| 리스크 밀도 | risk flag 개수 또는 penalty 합계 |
| 유사 사례 | 과거 날짜 중 점수, 수급, 변동성 조건이 비슷한 케이스 |

### 백엔드 구현

권장 API:

```http
GET /api/stocks/:code/analysis?date=YYYY-MM-DD
```

응답 예시:

```json
{
  "code": "083650",
  "name": "비에이치아이",
  "asof_date": "2026-06-08",
  "rank": 1,
  "score": 75.2,
  "score_delta": 8.4,
  "summary": "60일 기대수익 상위권과 상위권 진입 확률이 점수를 견인했습니다.",
  "positive_factors": [
    {"label": "기대수익", "value": "57.0%", "impact": 14.2},
    {"label": "외국인 수급", "value": "5일 순매수", "impact": 5.1}
  ],
  "risk_factors": [
    {"label": "최근 3개월 수익률", "value": "-40.4%", "severity": "high"}
  ],
  "scenarios": {
    "upside": "거래대금 회복과 MA20 재돌파 시 추가 상승 가능",
    "base": "변동성 축소 전까지 선별 관찰",
    "downside": "최근 저점 이탈 시 리스크 확대"
  }
}
```

### 프론트 구현

| 작업 | 설명 |
| --- | --- |
| 상세 페이지 또는 패널 개편 | 리스트 클릭 후 이동 또는 우측 패널 표시 |
| 요인 카드 | 긍정/부정 요인을 색상과 아이콘으로 구분 |
| 시나리오 탭 | 상승/횡보/하락을 탭으로 표시 |
| 비교 추가 버튼 | 랭킹분석의 비교 기능과 연결 |
| 주문 검토 버튼 | 수동매매 화면으로 종목 context 전달 |

### 지표

| 지표 | 목표 |
| --- | --- |
| 종목 상세 평균 체류시간 | 증가 |
| 상세 진입 후 이탈률 | 감소 |
| 비교 추가 클릭률 | 증가 |
| 수동매매 이동률 | 증가 |

## 5. 우선순위 3: AI 판단 로그 / 자동매매 관제화

### 목표

AI 자동매매 화면을 단순 결과 확인 화면이 아니라 "AI가 지금 무엇을 감시하고 왜 행동하지 않는지 보여주는 관제실"로 만든다.

자동매매에서 사용자가 가장 궁금한 것은 체결 여부보다 다음이다.

1. AI가 지금 어떤 종목을 보고 있는가?
2. 왜 매수하지 않았는가?
3. 어떤 조건이 충족되면 매수하는가?
4. 보유 종목은 왜 계속 보유하는가?
5. 매도 위험은 어떤 기준으로 감시하는가?

### 구현할 기능

| 기능 | 설명 |
| --- | --- |
| 현재 감시 종목 | 자동매매가 후보로 보고 있는 종목 리스트 |
| 조건 충족률 | 진입 조건 5개 중 3개 충족 같은 형태 |
| 매수 보류 사유 | 게이트, 가격, 수급, 리스크 등 미진입 이유 |
| 보유 판단 사유 | 보유 유지, 축소, 매도 검토 사유 |
| 최근 판단 로그 | 시간순으로 AI 판단 이벤트 표시 |
| 자동매매 사이클 타임라인 | 09:10 사전 점검, 09:30 실행, 10:00 재확인 등 |

### 화면 구성

AI 자동매매 메뉴를 다음 구조로 바꾼다.

| 영역 | 내용 |
| --- | --- |
| 상단 상태 | 운영 모드, BUY 허용 여부, 최신 데이터 기준일, 마지막 실행 시각 |
| 감시 리스트 | 현재 매수 후보와 조건 충족률 |
| 보유 리스트 | 보유 종목별 AI 판단과 매도 위험 |
| 판단 로그 | 최근 실행 사이클별 이벤트 |
| 예외/주의 | 데이터 지연, 주문 실패, 게이트 차단 등 |

### 데이터 구현

활용 가능한 산출물 후보:

| 파일/데이터 | 용도 |
| --- | --- |
| `outputs/order_requests_preview.json` | 주문 후보/미리보기 |
| `trade_intents` 계열 산출물 | 매수/매도 의도 |
| `ai_position_state.json` | 보유 종목 상태 |
| `auto_ops_auto_buy_scheduler_status.json` | 자동매매 스케줄 실행 상태 |
| `operational_buy_gate` 계열 | 매수 게이트 상태 |

추가로 저장할 로그:

```json
{
  "timestamp": "2026-06-08T09:30:00+09:00",
  "cycle": "auto_buy",
  "code": "083650",
  "name": "비에이치아이",
  "decision": "WATCH",
  "reason": "BUY gate 차단. 시장 국면 방어 우위.",
  "conditions": [
    {"name": "rank_top20", "passed": true},
    {"name": "score_threshold", "passed": true},
    {"name": "risk_limit", "passed": false},
    {"name": "market_gate", "passed": false}
  ]
}
```

### 백엔드 구현

권장 API:

```http
GET /api/auto-trading/decision-log?date=YYYY-MM-DD
GET /api/auto-trading/watchlist
GET /api/auto-trading/cycle-status
```

구현 원칙:

1. 주문 로직 자체를 먼저 바꾸지 않는다.
2. 기존 판단 결과를 읽어서 설명용 로그를 만든다.
3. 실거래 판단과 UI 설명 사이의 불일치를 막기 위해 같은 산출물에서 파생한다.
4. 데이터가 없을 때는 "판단 로그 없음"이 아니라 "오늘 자동매매 사이클 미실행"처럼 원인을 표시한다.

### 프론트 구현

| 작업 | 설명 |
| --- | --- |
| 조건 체크리스트 | 종목별 진입 조건 통과/실패 표시 |
| 타임라인 | 최근 사이클을 시간순 표시 |
| 보류 사유 뱃지 | 게이트 차단, 리스크 초과, 수급 부족 등 |
| 상세 펼침 | 각 판단의 근거 데이터 노출 |

### 지표

| 지표 | 목표 |
| --- | --- |
| AI 자동매매 화면 체류시간 | 증가 |
| 판단 로그 펼침률 | 증가 |
| 자동매매 화면 재방문율 | 증가 |
| 운영 상태 문의 감소 | 증가가 아니라 감소가 목표 |

## 6. 우선순위 4: 랭킹분석 탐색형 리그화 + 종목 비교

### 목표

랭킹분석을 단순 순위표가 아니라, 사용자가 여러 관점으로 종목을 찾아보는 탐색 도구로 만든다.

전체 1등만 보여주면 사용자는 금방 떠난다. 반대로 "수급 개선", "리스크 감소", "점수 급등", "섹터별 상위"처럼 여러 리그가 있으면 더 오래 탐색한다.

### 구현할 기능

| 기능 | 설명 |
| --- | --- |
| 점수 급등 리그 | 전일 대비 점수 상승 상위 |
| 순위 급등 리그 | 전일 대비 순위 상승 상위 |
| 수급 개선 리그 | 외국인/기관 수급 개선 상위 |
| 리스크 감소 리그 | 리스크 penalty 감소 상위 |
| 섹터별 TOP 5 | 업종별 상위 종목 |
| 반전 후보 | 점수는 중위권이지만 개선 속도가 빠른 종목 |
| 종목 비교 | 2~3개 종목의 점수, 수급, 리스크, 기대수익 비교 |

### 화면 구성

랭킹분석 메뉴에 다음 탭을 둔다.

| 탭 | 내용 |
| --- | --- |
| 종합 랭킹 | 기존 최종 점수 기준 |
| 급상승 | 점수/순위 변화 기준 |
| 수급 | 외국인/기관 흐름 기준 |
| 리스크 | 위험 증가/감소 기준 |
| 섹터 | 업종별 상위 |
| 비교 | 사용자가 선택한 종목 비교 |

### 종목 비교 화면

비교 항목:

| 항목 | 설명 |
| --- | --- |
| 현재 순위 | 전체 랭킹 |
| 최종 점수 | 현재 점수와 전일 대비 변화 |
| 기대수익 | 60일 기대수익 등 |
| 수급 | 외국인/기관 5일, 20일 흐름 |
| 리스크 | 변동성, 낙폭, 유동성, penalty |
| 추천 근거 | 상위 driver |
| 주의 요인 | risk factor |
| AI 판단 | 매수 검토, 관망, 주의 |

### 백엔드 구현

권장 API:

```http
GET /api/ranking/leagues?date=YYYY-MM-DD
GET /api/stocks/compare?codes=083650,214450,071050
```

응답은 기존 랭킹 데이터를 재가공해서 제공한다. 별도 모델을 만들 필요는 없다.

### 프론트 구현

| 작업 | 설명 |
| --- | --- |
| 랭킹 탭 추가 | 리그별 필터 전환 |
| 비교 바 | 선택된 종목을 하단 또는 우측에 고정 표시 |
| 비교 화면 | 2~3개 종목을 컬럼으로 비교 |
| 상세 연결 | 비교 중 특정 항목 클릭 시 종목 상세로 이동 |

### 지표

| 지표 | 목표 |
| --- | --- |
| 랭킹분석 체류시간 | 증가 |
| 필터/탭 전환 횟수 | 증가 |
| 비교 추가율 | 증가 |
| 비교 후 상세 진입률 | 증가 |

## 7. 우선순위 5: 수동매매 의사결정 보조

### 목표

수동매매 화면을 주문 입력 화면이 아니라, 주문 전에 AI가 리스크와 조건을 점검해주는 의사결정 보조 화면으로 만든다.

수동매매에서 사용자는 주문 직전에 가장 오래 고민한다. 이 구간에 정보가 있으면 체류시간과 신뢰가 함께 증가한다.

### 구현할 기능

| 기능 | 설명 |
| --- | --- |
| 매수 전 AI 체크리스트 | 추세, 수급, 리스크, 가격 위치, 시장 게이트 |
| 현재가 적정성 | 추천 구간 대비 현재가 위치 |
| 손익비 계산 | 목표/손절 기준 입력 또는 자동 제안 |
| 손절 기준 제안 | 변동성 또는 최근 저점 기준 |
| 주문 전 경고 | 리스크 높음, 수급 악화, 게이트 차단 등 |
| 주문 메모 | 사용자가 매수 이유를 기록 |

### 화면 구성

수동매매 화면에서 종목 선택 시 우측 또는 상단에 AI 체크 패널을 표시한다.

| 영역 | 내용 |
| --- | --- |
| 종목 요약 | 점수, 순위, 리스크 상태 |
| 체크리스트 | 매수 조건 통과/실패 |
| 가격 계획 | 진입가, 목표가, 손절가 |
| 예상 손익비 | 이익/손실 비율 |
| 주문 버튼 | 경고 확인 후 주문 |

### 데이터 구현

필요 항목:

| 항목 | 설명 |
| --- | --- |
| 현재가 | 실시간 또는 지연 시세 |
| 추천 점수 | 랭킹 점수 |
| 리스크 요인 | 상세화면과 동일 |
| 변동성 | 손절폭 계산 |
| 최근 저점/고점 | 기준가 제안 |
| 시장 게이트 | 매수 허용 여부 |

### 백엔드 구현

권장 API:

```http
GET /api/manual-trading/precheck?code=083650
POST /api/manual-trading/order-plan
```

`precheck` 응답 예시:

```json
{
  "code": "083650",
  "name": "비에이치아이",
  "decision": "CAUTION",
  "checks": [
    {"name": "AI 점수", "status": "PASS", "message": "Top 20 내 상위권"},
    {"name": "시장 게이트", "status": "FAIL", "message": "방어 우위 구간"},
    {"name": "리스크", "status": "WARN", "message": "최근 변동성 확대"}
  ],
  "suggested_plan": {
    "entry_note": "추격 매수보다 눌림 확인 필요",
    "stop_loss_basis": "최근 저점 이탈",
    "risk_reward": 1.8
  }
}
```

### 프론트 구현

| 작업 | 설명 |
| --- | --- |
| 종목 선택 연동 | 메인/상세에서 수동매매로 code 전달 |
| 주문 전 체크 패널 | 주문 버튼 주변에 표시 |
| 경고 확인 흐름 | 위험도가 높을 경우 확인 단계 추가 |
| 주문 계획 저장 | 매수 이유와 손절 기준 저장 |

### 지표

| 지표 | 목표 |
| --- | --- |
| 수동매매 화면 체류시간 | 증가 |
| 주문 전 체크 열람률 | 증가 |
| 주문 계획 저장률 | 증가 |
| 무계획 주문 비율 | 감소 |

## 8. 우선순위 6: AI 성과 화면 신뢰 콘텐츠화

### 목표

AI 성과 화면을 단순 수익률 표시가 아니라, 사용자가 AI를 검증하고 학습할 수 있는 신뢰 콘텐츠로 만든다.

성과 화면에서 중요한 것은 좋은 성과만 보여주는 것이 아니다. 실패 사례와 시장 구간별 약점까지 보여줘야 오히려 신뢰가 올라간다.

### 구현할 기능

| 기능 | 설명 |
| --- | --- |
| 추천 후 성과 | 추천 후 1일, 5일, 20일, 60일 성과 |
| 성공 사례 | 추천 근거와 실제 주가 흐름 |
| 실패 사례 | 실패 원인 분석 |
| 시장 구간별 성과 | 상승장, 하락장, 변동장 구분 |
| 전략별 성과 | 점수 상위, 수급 개선, 리스크 낮음 등 기준별 비교 |
| 만기 코호트 | 추천일 기준 코호트별 누적 성과 |

### 화면 구성

| 영역 | 내용 |
| --- | --- |
| 상단 요약 | 최근 30일/60일 추천 성과 |
| 코호트 차트 | 추천일별 누적 성과 |
| 성공/실패 사례 | 대표 종목 카드 |
| 시장 구간별 분석 | 어떤 장세에서 강하고 약한지 |
| 전략 비교 | 리그별 성과 비교 |

### 데이터 구현

필요 항목:

| 항목 | 설명 |
| --- | --- |
| 추천일 | 종목이 추천된 날짜 |
| 추천 당시 점수/순위 | 당시 기준 값 |
| 이후 가격 | 1일, 5일, 20일, 60일 후 가격 |
| 시장 상태 | 추천 당시 시장 국면 |
| 추천 근거 | 당시 top_driver |
| 리스크 요인 | 당시 risk_factor |

### 백엔드 구현

권장 API:

```http
GET /api/performance/cohorts
GET /api/performance/cases?type=success
GET /api/performance/cases?type=failure
GET /api/performance/by-market-regime
```

주의사항:

1. 아직 60일 만기 데이터가 충분하지 않으면 1일/5일/20일 중심으로 표시한다.
2. 성과가 불리한 구간도 숨기지 않는다.
3. "미래 수익 보장" 표현을 피하고 참고 지표로 표현한다.

### 프론트 구현

| 작업 | 설명 |
| --- | --- |
| 기간 선택 | 1일/5일/20일/60일 |
| 성공/실패 탭 | 케이스 전환 |
| 사례 상세 | 추천 당시 근거와 이후 흐름 |
| 시장 구간 필터 | 방어/중립/공격 등 |

### 지표

| 지표 | 목표 |
| --- | --- |
| AI 성과 화면 체류시간 | 증가 |
| 사례 상세 클릭률 | 증가 |
| 성과 화면 재방문율 | 증가 |
| 자동매매 화면 이동률 | 증가 |

## 9. 우선순위 7: 개인화 관심종목 대시보드

### 목표

사용자가 관심종목을 등록하고 매일 자신의 종목 변화를 확인하게 만든다.

개인화는 체류시간보다 재방문 습관에 더 강하다. 단, 사용자 식별, 저장소, 알림 구조가 필요하므로 우선순위는 뒤로 둔다.

### 구현할 기능

| 기능 | 설명 |
| --- | --- |
| 관심종목 추가 | 메인, 상세, 랭킹에서 추가 |
| 내 관심종목 변화 | 점수, 순위, 리스크 변화 |
| 관심종목 알림 | 신규 리스크, 점수 급등, 매수 조건 충족 |
| 관심종목 비교 | 내 종목끼리 비교 |
| 메모 | 사용자가 관심 이유 기록 |

### 백엔드 구현

권장 API:

```http
GET /api/watchlist
POST /api/watchlist
DELETE /api/watchlist/:code
GET /api/watchlist/changes
```

필요 테이블 예시:

```sql
CREATE TABLE user_watchlist (
  user_id TEXT NOT NULL,
  code TEXT NOT NULL,
  memo TEXT,
  created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (user_id, code)
);
```

로그인 전이라면 임시로 브라우저 localStorage 기반 관심종목부터 시작할 수 있다. 이후 로그인 기능이 안정되면 서버 저장으로 이전한다.

### 프론트 구현

| 작업 | 설명 |
| --- | --- |
| 관심 추가 버튼 | 종목 행, 상세 화면, 비교 화면에 배치 |
| 내 관심 탭 | 메인 또는 별도 메뉴에 표시 |
| 변화 뱃지 | 점수 상승, 리스크 증가 등 |
| 메모 입력 | 간단한 텍스트 저장 |

### 지표

| 지표 | 목표 |
| --- | --- |
| 관심종목 등록률 | 증가 |
| 관심종목 사용자 재방문율 | 증가 |
| 관심종목 화면 체류시간 | 증가 |
| 알림 클릭률 | 증가 |

## 10. MVP 구현 순서

### 1차 MVP: 메인 변화 + 상세 분석

기간 목표: 가장 먼저 적용

작업:

1. 전일 대비 점수/순위 변화 계산
2. 메인 화면에 오늘의 변화 카드 추가
3. 랭킹 테이블에 점수 변화, 신규 진입 뱃지 표시
4. 종목 상세에 AI 요약, 추천 근거, 리스크 요인, 시나리오 섹션 추가

기대 효과:

| 효과 | 설명 |
| --- | --- |
| 첫 화면 체류 증가 | 변화 카드로 탐색 유도 |
| 상세 진입률 증가 | 왜 바뀌었는지 확인하고 싶어짐 |
| 재방문 이유 강화 | 매일 달라지는 정보 제공 |

### 2차 MVP: AI 판단 로그 + 랭킹 리그

작업:

1. 자동매매 판단 로그 생성
2. 자동매매 화면에 감시 종목과 보류 사유 표시
3. 랭킹분석에 급상승, 수급, 리스크, 섹터 탭 추가
4. 종목 비교 기능 추가

기대 효과:

| 효과 | 설명 |
| --- | --- |
| 자동매매 신뢰 증가 | AI가 왜 행동하지 않는지 설명 |
| 탐색 시간 증가 | 여러 랭킹 리그 탐색 |
| 비교 기반 체류 증가 | 종목 간 검토 시간 증가 |

### 3차 MVP: 수동매매 보조 + AI 성과 사례화

작업:

1. 수동매매 precheck API 추가
2. 주문 전 AI 체크리스트 추가
3. 성과 화면에 성공/실패 사례 추가
4. 추천 후 1일/5일/20일 성과 표시

기대 효과:

| 효과 | 설명 |
| --- | --- |
| 주문 전 체류 증가 | 의사결정 보조 정보 확인 |
| 신뢰도 증가 | 성공과 실패를 함께 설명 |
| 장기 재방문 증가 | 성과 검증 목적 방문 |

## 11. 화면별 적용 요약

| 메뉴 | 추가할 핵심 기능 |
| --- | --- |
| 메인 | 기본 접힘 변화 요약, 랭킹 테이블 변화 뱃지, 신규 Top20/점수 개선/구간 상승/리스크 완화 표시 |
| 종목 상세 | AI 분석 여정, 추천 근거, 리스크, 시나리오, 유사 사례 |
| AI 자동매매 | 판단 로그, 감시 종목, 조건 충족률, 보류 사유 |
| 수동매매 | 주문 전 AI 체크리스트, 손익비, 손절 기준, 경고 |
| 랭킹분석 | 급상승/수급/리스크/섹터 리그, 종목 비교 |
| AI 성과 | 코호트 성과, 성공/실패 사례, 시장 구간별 성과 |

## 12. 체류시간 측정 이벤트

기능을 만들기 전에 측정 이벤트를 같이 정의해야 한다.

| 이벤트 | 발생 위치 | 설명 |
| --- | --- | --- |
| `main_change_summary_view` | 메인 | 접힌 변화 요약 노출 |
| `main_change_expand_click` | 메인 | 오늘의 의미 있는 변화 상세 펼침 |
| `main_change_card_click` | 메인 | 변화 상세 카드 클릭 |
| `main_change_badge_view` | 메인 테이블 | 종목명 옆 변화 뱃지 노출 |
| `ranking_row_click` | 메인/랭킹 | 종목 행 클릭 |
| `stock_detail_view` | 종목 상세 | 상세 화면 진입 |
| `stock_factor_expand` | 종목 상세 | 추천 근거 상세 펼침 |
| `stock_compare_add` | 메인/상세/랭킹 | 비교 종목 추가 |
| `auto_decision_log_view` | AI 자동매매 | 판단 로그 조회 |
| `manual_precheck_view` | 수동매매 | 주문 전 체크 조회 |
| `performance_case_view` | AI 성과 | 성공/실패 사례 조회 |
| `watchlist_add` | 공통 | 관심종목 추가 |

최소로 봐야 할 지표:

| 지표 | 설명 |
| --- | --- |
| 평균 세션 시간 | 전체 체류시간 |
| 메뉴별 체류시간 | 어떤 메뉴가 오래 붙잡는지 |
| 상세 진입률 | 메인에서 상세로 이동한 비율 |
| 비교 기능 사용률 | 탐색 깊이 |
| 재방문율 | 다음 거래일 재방문 |
| 주문 전 체크 사용률 | 수동매매 보조 효과 |

## 13. 구현 시 주의사항

1. 실거래 로직은 먼저 바꾸지 않는다.
   - 초기 체류시간 개선은 UI와 설명 데이터 중심으로 진행한다.
   - 자동매매 판단 설명은 실제 주문 산출물에서 파생해야 한다.

2. 추천 표현은 과장하지 않는다.
   - "매수 추천", "수익 보장" 같은 표현을 피한다.
   - "AI 관찰", "후보", "주의 필요", "조건 충족" 중심으로 표현한다.

3. 데이터가 없는 경우를 명확히 처리한다.
   - 전일 데이터 없음
   - 장중 갱신 전
   - 자동매매 사이클 미실행
   - 60일 성과 만기 전

4. 화면은 정보가 많아져도 복잡해 보이지 않게 한다.
   - 메인은 요약 위주
   - 메인 변화 정보는 기본 접힘으로 두고, 핵심 정보는 테이블 뱃지로 흡수
   - 상세는 분석 단계화
   - 랭킹은 탭으로 분리
   - 자동매매는 타임라인 중심

5. 성과 화면은 성공과 실패를 함께 보여준다.
   - 실패 사례를 숨기면 단기적으로 좋아 보일 수 있지만 장기 신뢰가 약해진다.

## 14. 추가 메모: 투자자가 참고하는 핵심 지표와 화면 반영 방향

주식 분석에서 사용자가 오래 머무는 이유는 단순히 종목명이 많아서가 아니라, "왜 이 종목을 봐야 하는지"를 판단할 재료가 충분하기 때문이다. 따라서 LeeTrader의 체류시간 개선은 투자자가 실제로 확인하는 지표를 중심으로 재구성하는 방향이 적합하다.

### 14-1. 투자자가 자주 참고하는 지표 축

| 지표 축 | 사용자가 확인하는 내용 | LeeTrader 반영 포인트 |
| --- | --- | --- |
| 가격 위치 | 현재가, 등락률, 20/60/120일 이동평균, 52주 고점/저점, RSI | 현재 가격이 과열인지, 회복 초입인지, 박스권인지 요약 |
| 거래량/수급 | 거래량, 거래대금, 외국인/기관 순매수, 최근 5일/20일 수급 변화 | `수급개선`, `거래대금 증가`, `관심 증가` 뱃지로 표시 |
| 실적/성장 | 매출, 영업이익, 순이익, 이익률, ROE, EPS, 컨센서스 변화 | 종목 상세에서 사업/실적 맥락을 함께 설명 |
| 밸류에이션 | PER, PBR, PSR, EV/EBITDA, 배당수익률 | 같은 업종 대비 비싼지 싼지 비교형으로 표시 |
| 리스크 | 변동성, 최대 낙폭, 부채비율, 유동성, 단기 급등, 거래량 부족 | `과열주의`, `유동성주의`, `리스크완화`처럼 행동 가능한 신호로 표시 |
| 모멘텀/테마 | 업종 강도, 테마, 정책/뉴스, 상대강도 | 섹터별 상위, 테마 관련 변화, 시장 관심도와 연결 |
| AI/퀀트 판단 | final_score, confidence_score, 예측수익률, 주요 기여 요인, 제외 사유 | 단순 점수보다 `점수가 오른 이유`, `제외된 이유`, `주의할 조건`을 설명 |

### 14-2. 사용자가 궁금해하는 질문 기준

화면은 지표 나열보다 다음 질문에 답하는 방식이 좋다.

1. 이 종목은 지금 왜 상위권인가?
2. 어제와 비교해 무엇이 좋아졌거나 나빠졌는가?
3. 가격, 수급, 실적, 리스크 중 어떤 요인이 핵심인가?
4. 비슷한 종목과 비교하면 더 나은가?
5. 지금 바로 볼 종목인지, 관찰만 할 종목인지, 피해야 할 종목인지?

이 질문에 답하면 사용자는 메인 테이블에서 멈추지 않고 상세 화면, 비교 화면, 자동매매 판단 로그로 자연스럽게 이동할 가능성이 높다.

### 14-3. 제품 적용 우선순위

| 순서 | 적용 방향 | 이유 |
| --- | --- | --- |
| 1 | 종목 상세 화면에 가격/수급/AI근거/리스크/사업·실적 맥락을 단계형으로 배치 | 메인에서 들어온 사용자가 가장 오래 머무를 수 있는 구간 |
| 2 | 메인 랭킹 테이블 뱃지를 투자 지표 기반으로 정리 | 첫 화면에서 "왜 볼 종목인지"를 빠르게 전달 |
| 3 | 랭킹분석을 목적별 리그로 분리 | 사용자가 자신의 관점에 맞는 종목군을 반복 탐색 가능 |
| 4 | AI 판단 로그와 자동매매 보류 사유를 지표 변화와 연결 | AI가 왜 움직이지 않았는지도 신뢰 콘텐츠가 됨 |

### 14-4. 우선 사용할 뱃지 후보

| 뱃지 | 의미 |
| --- | --- |
| `점수급등` | final_score 또는 순위가 의미 있게 상승 |
| `수급개선` | 거래량, 거래대금, 외국인/기관 흐름 개선 |
| `저평가검토` | 밸류에이션이 업종 대비 낮거나 부담이 완화 |
| `리스크완화` | 변동성, 낙폭, 위험 점수가 낮아짐 |
| `과열주의` | 단기 급등, 높은 변동성, 과도한 거래량 |
| `실적확인` | 실적 또는 성장성 확인이 필요한 종목 |
| `섹터상위` | 같은 업종 또는 테마 내 상대 순위가 높음 |

현재 1순위 진행과 연결하면, 메인은 모든 지표를 보여주는 화면이 아니라 "오늘 의미 있게 바뀐 종목을 찾는 입구"로 두고, 상세 화면에서 지표별 해석을 깊게 제공하는 구조가 적합하다.

### 14-5. 2026-06-14 메인 변화 뱃지 적용 기준

전체 테스트 결과, `신규Top20`, `섹터상위`, `수급양호`, `기초체력` 같은 상태형 뱃지는 너무 많은 종목에 붙어 신호성이 약해졌다. 따라서 메인 종목명 옆 뱃지는 상태 설명이 아니라 "오늘 볼 이유가 생긴 변화형 신호"로 제한한다.

1차 적용 기준은 다음과 같다.

| 뱃지 | 적용 기준 | 상세 이동 위치 |
| --- | --- | --- |
| `점수급등` | 점수 변화 +10 이상, 현재 60위 이내 | `#analysis-summary` |
| `관심권진입` | 이전 순위가 실제 숫자로 존재하고 50위 밖, 현재 20위 이내 | `#analysis-summary` |
| `리스크완화` | 리스크 패널티 변화 -4 이하 | `#risk-summary` |

운영 기준:

1. `신규Top20`은 변동성이 커서 1차 뱃지에서 제외한다.
2. 상태형 뱃지는 메인 종목명 옆에 붙이지 않고 상세 화면에서 설명한다.
3. 한 종목에 표시하는 뱃지는 최대 2개로 제한한다.
4. 전체 종목의 10% 내외만 뱃지가 붙는 수준을 목표로 한다.
5. 뱃지 클릭 시 상세페이지의 관련 섹션으로 이동한다.
6. `main_change_badge_view`, `main_change_badge_click`, `stock_detail_view` 이벤트로 효과를 측정한다.

2026-06-12 데이터 기준 안정형 기준을 적용하면 전체 204개 중 21개(10.3%), Top20 중 7개(35.0%)에 뱃지가 붙는다. 이 정도면 메인에서 시선 유도는 가능하지만 전체 화면이 뱃지로 과밀해지지는 않는다.

다음 판단 기준:

| 지표 | 해석 |
| --- | --- |
| 뱃지 클릭률 | 뱃지가 실제 상세 진입을 유도하는지 |
| 뱃지 클릭 후 상세 진입 | 뱃지 링크 흐름이 정상 작동하는지 |
| 뱃지별 클릭 분포 | `점수급등`, `관심권진입`, `리스크완화` 중 어떤 신호가 사용자를 끄는지 |
| 상세 체류시간 | 상세 2차 개편이 필요한지 판단하는 보조 지표 |

운영 확인 API:

- `GET /api/analytics/change-badges?days=7`
- 운영자 인증 후 확인한다.
- `badge_views`, `badge_clicks`, `click_rate_by_badge`, `top_codes`, `detail_focus_entries`로 뱃지 효과를 판단한다.

### 14-6. 2026-06-15 AI 실자동매매 화면 개선 진행

캡처 검토 결과, AI 실자동매매 화면에서 가장 큰 신뢰도 이슈는 주문 초안이 `제출 가능`으로 표시되었지만 실제 실행 결과는 증권사 오류 `APBK0919`로 거절된 점이었다.

오류 원문:

- `장운영일자가 주문일과 상이합니다`

이 문제는 단순 표시 문제가 아니라 자동매매 정합성 문제로 본다. 사용자는 화면의 `제출 가능`을 실제 주문 가능한 상태로 해석하기 때문에, 장운영일/주문일/주문 가능 시간 정합성이 맞지 않으면 사전에 차단되어야 한다.

#### 1순위 완료: 장운영일/주문일 정합성 사전 차단

적용 내용:

| 항목 | 적용 결과 |
| --- | --- |
| 장운영 상태 guard | `MARKET_DATE_GUARD` 추가 |
| 휴장/장전/장후 초안 처리 | `executable_now=false`로 파생 표시 |
| 브로커 오류 승격 | `APBK0919`를 사후 실패가 아니라 사전 차단 근거로 노출 |
| API 정합성 | `/api/order-requests-preview`, `/api/live-auto-trading-diagnostics`, `/api/live-account/summary`가 같은 guard 기준 사용 |
| 화면 표시 | 주문 초안 상태가 `제출 가능`이 아니라 `장외 대기`로 표시 |

검증 결과:

| 확인 항목 | 결과 |
| --- | --- |
| `/api/order-requests-preview` | `allowed=0`, `guard=true`, `blocked_reason=market_date_guard` |
| `/api/live-auto-trading-diagnostics` | `submit_allowed_count=0`, `market_order_guard_active=true` |
| 화면 JS | `장외 대기`, `장운영 guard`, `장운영일자와 주문일자` 문구 반영 |
| 구문 검사 | `node --check node/index.js`, `node --check node/public/live-auto-trading.js` 통과 |

#### 2순위 완료: 상태 용어와 전문 해석 문구 정리

적용 내용:

| 용어 | 화면 설명 방향 |
| --- | --- |
| `WATCH` | 관찰 우선. 전략 후보일 수 있지만 가격, 유동성, 변동성 확인 전까지 자동 확대 제한 |
| `PILOT` | 제한 실운용. 전면 자동매수보다 작은 범위에서 실제 주문 흐름 검증 |
| `SELL_ONLY` | 신규 매수보다 보유 축소, 매도, 교체 검토 우선 |
| `POLICY_BLOCK` | 정책 기준상 자동 주문 제출 대상 제외 |
| `장운영 guard` | 휴장, 장전, 장후 또는 장운영일 불일치 위험 시 제출 가능 초안 사전 차단 |

`APBK0919` 실행 실패 문구는 다음 의미로 해석한다.

> 장운영일자와 주문일자가 맞지 않아 증권사에서 거절했다. 장중 최신 계좌/가격 기준으로 주문 초안을 재생성해야 한다.

#### 3순위 완료: 정합성 카드 클릭 이동

적용 내용:

| 위치 | 동작 |
| --- | --- |
| 분석 요약 `정합성` 카드 | 주문 탭의 `자동매매 정합성` 패널로 이동 |
| 분석 요약 `운영 반영 판단` 카드 | `Quality Guard` 패널로 이동 |
| 분석 요약 `성과 표본` 카드 | `Live KPI` 패널로 이동 |
| 분석 요약 `청산 손익` 카드 | `Closed Trade` 패널로 이동 |
| 클릭 후 표시 | 대상 패널에 focus highlight 적용 |

이 작업의 목적은 분석 탭의 요약 수치와 주문 탭의 상세 근거를 끊기지 않게 연결하는 것이다.

#### 4순위 완료: AI 판단 로그 단계화

주문 탭 상단에 `AI 판단 흐름` 패널을 추가했다.

단계:

1. `Intent`: 전략 판단과 보유/매수/축소 의도
2. `Preview`: 주문 초안과 차단 여부
3. `Execution`: 실제 제출 결과와 실패 여부
4. `Fill`: 체결 및 정합성 연결

각 단계 카드는 클릭 시 해당 표 또는 정합성 패널로 이동한다. 사용자는 자동매매가 어느 단계에서 막혔는지 한눈에 볼 수 있다.

#### 5순위 완료: 분석 탭 전문 해석 강화

분석 탭에 `분석 해석` 패널을 추가했다.

해석 항목:

| 항목 | 설명 |
| --- | --- |
| 운영 반영 판단 | Quality Guard와 검증 상태가 실제 운영 반영 후보인지 설명 |
| 성과 표본 해석 | D+5 관찰 수가 최소 표본 30건 기준을 충족하는지 설명 |
| 신뢰도 주의점 | 정합성 경고와 랭킹 누락이 있으면 제한적 해석 필요 표시 |
| 손익 해석 | 청산 손익과 lot 단위 근거 보조 계산 가능성을 함께 설명 |

#### 현재 판단

1~5순위는 화면과 API 기준으로 1차 완료했다.

다만 자동매매 신뢰도 관점에서 아직 남은 과제는 있다.

| 다음 과제 | 이유 |
| --- | --- |
| 실제 주문 생성 로직 자체의 사전 차단 반영 | 현재는 API/UI 파생 guard다. 주문 산출물을 생성하는 Python 단계에서도 같은 차단이 들어가야 더 안전하다. |
| 장운영일 캘린더 고도화 | 주말 판별뿐 아니라 한국 휴장일, 임시공휴일, 대체공휴일을 정확히 반영해야 한다. |
| 판단 로그 영구 저장 | 현재는 산출물 기반 표시다. 장기 신뢰 분석을 위해 cycle별 로그를 DB에 축적하는 구조가 필요하다. |
| 자동매매 정합성 이벤트 측정 | `decision_flow_step_click`, `auto_trade_consistency_click` 같은 이벤트로 실제 사용 흐름을 확인할 수 있다. |

다음 진행 추천:

1. 주문 산출물 생성 Python 단계에 `MARKET_DATE_GUARD`를 직접 반영한다.
2. 한국 거래소 영업일 캘린더 기반으로 장운영 상태 판별을 정교화한다.
3. 판단 로그를 DB에 누적해 AI 성과 화면과 연결한다.

## 15. 최종 제안

현재까지 적용한 1차 방향은 다음과 같다.

1. 메인 "오늘의 의미 있는 변화"는 기본 접힘으로 유지한다.
2. 변화 상세는 현재 순위순 6개 압축 카드로만 제공한다.
3. 실제 탐색의 중심은 메인 랭킹 테이블이며, 변화 정보는 종목명 옆 작은 뱃지로 흡수한다.

다음 회의에서 이어갈 우선 논의 주제:

| 순서 | 논의 주제 | 이유 |
| --- | --- | --- |
| 1 | 메인 상단 운영 카드 축소 여부 | 랭킹 테이블 진입 전 정보량이 아직 많음 |
| 2 | 테이블 변화 뱃지의 문구/색상 정리 | 뱃지가 많아지면 행이 복잡해질 수 있음 |
| 3 | 종목 상세 진입 후 분석 여정 강화 | 테이블에서 상세로 넘어간 뒤 체류시간을 늘리는 핵심 단계 |
| 4 | 변화 뱃지 클릭 시 상세 내 해당 근거 위치로 이동 | 메인 변화 정보와 상세 분석을 연결 |

이 방향은 현재 서비스 구조를 크게 바꾸지 않으면서도 체류시간 증가에 직접적으로 작용한다.

특히 LeeTrader는 커뮤니티형 서비스보다 "오늘 시장에서 놓치면 안 되는 변화가 있다"는 감각을 만드는 쪽이 더 적합하다. 따라서 체류시간 증가의 핵심은 콘텐츠 양이 아니라 변화, 이유, 비교, 다음 행동을 촘촘하게 연결하는 것이다.
## 16. 2026-06-15 AI 자동매매 운영 안정화 진행

### 16-1. 중복 제거 후 실제 기능개선 항목

중복 항목을 제거한 실제 기능개선 순서는 다음과 같이 정리한다.

1. 주문 산출물 생성 Python 단계에 `MARKET_DATE_GUARD` 직접 반영
2. KRX 영업일 캘린더 고도화 및 테스트 추가
3. 보유종목/종목코드/데이터 정합성 점검
4. AI 판단 로그 DB 누적
5. 2~3주간 PILOT 운영 성과 검증 체계화

### 16-2. 1순위 진행 결과: Python 주문 생성 단계 MARKET_DATE_GUARD 반영

`python/submit_live_orders.py`에 주문 생성/실행 단계의 시장 가드를 직접 반영했다.

적용 내용:

| 항목 | 적용 결과 |
| --- | --- |
| 미리보기 생성 | 장중 `OPEN`이 아니면 주문 초안을 `market_date_guard`로 차단 |
| 실행 단계 | 장외/휴장 상태에서는 KIS 토큰 발급 및 브로커 주문 호출 전에 스킵 |
| 차단 표준화 | 시장 가드가 켜진 경우 `blocked_reason=market_date_guard`, `block_type=MARKET_DATE_GUARD`로 표준화 |
| payload 반영 | `order_requests_preview.json`, `order_requests_execution.json`에 `market_order_guard` 메타정보 추가 |
| markdown 반영 | 주문 미리보기/실행 리포트에 `market_order_guard`, `market_status` 표시 |

검증 결과:

| 테스트 | 결과 |
| --- | --- |
| `python/submit_live_orders.py` 문법 검사 | 통과 |
| `python/trading_diagnostics.py` 문법 검사 | 통과 |
| `tests.test_submit_live_orders_freshness` | 6건 통과 |
| 임시 미리보기 생성 | 현재 `PREOPEN` 기준 주문 2건 모두 `market_date_guard` 차단 |
| 임시 미리보기 제출 가능 수 | `submit_allowed_count=0` |

주의:

- 로컬 시스템 Python에는 `pandas`, `pytest`가 없어 `.venv_research` 가상환경으로 테스트했다.
- 임시 미리보기 생성 중 DB 동기화 best-effort 단계에서 `localhost:15432` 연결 경고가 발생했다. 파일 산출물 생성은 성공했지만, DB 연결 안정성은 별도 점검 대상이다.

### 16-3. 2순위 진행 결과: KRX 영업일 캘린더 보정

`config/trading_calendar_kr.json`과 `python/trading_diagnostics.py`를 연결해 장운영 상태 판단이 주말뿐 아니라 캘린더 휴장일을 반영하도록 수정했다.

적용 내용:

| 항목 | 적용 결과 |
| --- | --- |
| 캘린더 로딩 | `config/trading_calendar_kr.json`의 `closed_dates`, `open_dates` 반영 |
| 휴장일 판단 | 대체공휴일/근로자의 날/선거일/연말휴장 반영 |
| 상태값 | `HOLIDAY`, `PREOPEN`, `OPEN`, `AFTER_HOURS` 유지 |
| 한글 상태 | `휴장`, `장전`, `장중`, `장후`로 보정 |

보정한 2026년 주요 휴장일:

- 2026-03-02 삼일절 대체휴일
- 2026-05-01 근로자의 날
- 2026-05-25 부처님오신날 대체휴일
- 2026-06-03 전국동시지방선거
- 2026-08-17 광복절 대체휴일
- 2026-10-05 개천절 대체휴일
- 2026-12-31 연말 휴장

테스트에서 `2026-03-02 10:00 KST`는 `HOLIDAY`, `2026-06-15 10:00 KST`는 `OPEN`으로 확인했다.

### 16-4. 현재 판단

1~2순위는 실제 기능 개선까지 완료했다. 이제 자동매매 안전장치의 1차 방어선은 API/UI 파생 guard가 아니라 Python 주문 산출물 생성 단계에도 들어갔다.

다음 진행은 3순위가 맞다.

### 16-5. 다음 진행: 3순위 데이터 정합성 점검

다음 작업에서는 아래 항목을 확인한다.

| 점검 항목 | 목적 |
| --- | --- |
| 보유종목 9개 vs 45개 차이 | 실계좌/AI계좌/ledger/과거 스냅샷 기준이 섞였는지 확인 |
| 종목코드 6자리 정규화 | `083650`이 `83650`처럼 깨져 조인 실패하는 문제 방지 |
| ranking context 누락 | 성과/정합성 리포트에서 일부 체결이 랭킹 문맥을 못 찾는 원인 확인 |
| DB 동기화 경고 | `localhost:15432` 연결 경고 재현 및 원인 확인 |

3순위가 끝나야 4순위인 AI 판단 로그 DB 누적의 테이블 기준과 저장 키를 정확히 설계할 수 있다.
## 17. 2026-06-15 AI 자동매매 데이터 정합성 점검 진행

### 17-1. 3순위 진행 결과: 보유종목/종목코드/스냅샷 정합성 개선

3순위 데이터 정합성 점검을 진행했다.

확인된 문제:

| 문제 | 원인 | 조치 |
| --- | --- | --- |
| 보유종목 9개 vs 45개 차이 | `research.live_position_snapshot`에서 같은 날짜의 여러 `snapshot_at` 행을 모두 합산 | 최신 `snapshot_at` 1회분만 집계하도록 정합성 리포트 쿼리 수정 |
| 종목코드 앞자리 0 누락 가능성 | CSV를 pandas가 숫자로 추론하면 `083650`이 `83650`처럼 변환될 수 있음 | CSV payload 동기화, live ledger snapshot, Node API 응답에서 6자리 정규화 적용 |
| 웹 API 코드 표시 불일치 | DB payload가 숫자형으로 저장된 경우 Node 응답에서 그대로 문자열화 | `/api/live-account/holdings` 응답 시 1~6자리 숫자 코드를 `padStart(6, "0")` 처리 |

적용 파일:

- `python/sync_auxiliary_payloads.py`
- `python/sync_live_trade_ledger.py`
- `python/build_live_trade_consistency_report.py`
- `node/index.js`
- `tests/test_submit_live_orders_freshness.py`

검증 결과:

| 테스트 | 결과 |
| --- | --- |
| 관련 Python 단위 테스트 | 7건 통과 |
| Python 문법 검사 | 통과 |
| Node 문법 검사 | 통과 |
| diff whitespace 검사 | CRLF 경고만 있고 오류 없음 |
| `GET /api/live-account/holdings` | `count=9`, 코드 `083650,316140,010950,082920,058470` 확인 |
| `GET /api/order-requests-preview` | `items=2`, `allowed=0`, `blocked_reason=market_date_guard` 확인 |

### 17-2. 현재 완료 범위

현재 1~3순위는 기능 개선까지 완료했다.

1. Python 주문 생성/실행 단계 `MARKET_DATE_GUARD` 반영
2. KRX 영업일 캘린더 반영 및 테스트
3. 보유종목/종목코드/스냅샷 정합성 보강

### 17-3. 다음 진행

다음은 4순위 `AI 판단 로그 DB 누적`이다.

다만 바로 테이블을 만들기 전에 아래 저장 단위를 확정해야 한다.

| 저장 단위 | 설명 |
| --- | --- |
| run/cycle | 자동매매 1회 실행 단위 |
| intent | AI가 매수/매도/보유/축소를 판단한 원본 |
| preview | 주문 초안과 차단 사유 |
| execution | 실제 제출/스킵/실패 결과 |
| guard | 시장 가드, 정책 가드, 신호 신선도 가드 |
| outcome | 체결 후 D+1/D+3/D+5 성과 연결 |

추천 방향은 기존 산출물 파일을 유지하되, DB에는 append-only 방식으로 누적하는 것이다. 그래야 기존 파일 기반 운영을 깨지 않으면서 장기 성과 분석 화면으로 확장할 수 있다.
## 18. 2026-06-15 메인 변화 뱃지/상세 분석 여정 복구 확인

### 18-1. 확인 배경

메인 변화 뱃지와 종목 상세화면 분석 여정 강화가 화면에서 적용되지 않는 것처럼 보여 작업 이력을 재추적했다.

추적 결과:

| 항목 | 확인 결과 |
| --- | --- |
| 작업 커밋 | `b218d81 Improve main ranking engagement UX`에 메인 변화 패널/초기 뱃지 작업이 존재 |
| 문서 기준 | 14-5에 최종 뱃지 기준 `점수급등`, `관심권진입`, `리스크완화`가 정리되어 있음 |
| 현재 문제 | 일부 코드는 남아 있었지만 최종 14-5 기준과 클릭 후 상세 focus 이동이 충분히 반영되지 않은 상태 |

### 18-2. 복구 및 재적용 내용

메인 변화 뱃지를 14-5 최종 기준으로 다시 맞췄다.

| 뱃지 | 적용 기준 | 상세 이동 위치 |
| --- | --- | --- |
| `점수급등` | 점수 변화 +10 이상, 현재 60위 이내 | `#analysis-summary` |
| `관심권진입` | 이전 순위가 실제 숫자로 존재하고 50위 밖, 현재 20위 이내 | `#analysis-summary` |
| `리스크완화` | 리스크 패널티 변화 -4 이하 | `#risk-summary` |

적용 파일:

- `node/index.js`
- `node/public/index.html`
- `node/public/detail.html`

추가 반영:

1. 메인 뱃지는 최대 2개만 표시한다.
2. `Top20 신규` 같은 상태형 뱃지는 메인 종목명 옆 뱃지에서 제외한다.
3. 뱃지 클릭 시 상세 화면의 관련 섹션으로 이동한다.
4. `main_change_badge_view`, `main_change_badge_click`, `stock_detail_view` 이벤트 전송을 추가했다.
5. 상세 화면에 `analysis-summary`, `risk-summary` 앵커와 포커스 하이라이트를 추가했다.

### 18-3. 검증 결과

포트 `3400` 기준으로 재기동 후 확인했다.

| 확인 항목 | 결과 |
| --- | --- |
| `/api/ranking/meaningful-changes` | `meaningful_count=21`, `badge_stock_ratio=0.1029` |
| 점수급등 | 20개 |
| 관심권진입 | 2개 |
| 리스크완화 | 1개 |
| 첫 번째 뱃지 예시 | `우리기술(032820)` → `점수급등`, focus `analysis-summary` |
| 메인 HTML | `점수급등`, `main_change_badge_click`, `source=main_change_badge` 반영 확인 |
| 상세 HTML | `id="analysis-summary"`, `id="risk-summary"`, `focusDetailSection`, `stock_detail_view` 반영 확인 |

현재 기준은 14-5 문서의 목표치인 전체 204개 중 21개, 약 10.3%와 일치한다.

### 18-4. 원인 판단

소스 전체가 사라진 것은 아니었다. 다만 다음 두 부분이 불완전했다.

1. 메인 변화 뱃지 API/렌더링이 최종 14-5 기준이 아니라 초기 기준 일부에 머물러 있었다.
2. 상세 화면 이동용 focus 앵커와 클릭 이벤트 추적이 빠져 있어 사용자가 보기에는 적용 전처럼 보일 수 있었다.

따라서 이번 조치로 14-5 기준을 실제 API, 메인 UI, 상세 이동 경로에 다시 맞췄다.
## 19. 2026-06-15 종목 상세 AI 요약/시나리오/비교 후보 복구

### 19-1. 확인된 문제

종목 상세 화면에서 이전에 확인했던 `AI 요약`, `추천 근거`, `리스크 요인`, `시나리오 판단`, `비교 후보` 기능이 보이지 않는 문제가 있었다.

추적 결과 현재 `node/public/detail.html`에는 일부 영역의 HTML 껍데기만 남아 있었고, 실제 데이터를 받아 시나리오 카드와 비교 후보를 렌더링하는 JavaScript 함수가 빠져 있었다.

복구 기준은 `tmp/adsense_recovery_backup_20260614/detail.corrupted.html`에 남아 있던 이전 상세 화면 구현 흔적을 참고했다.

### 19-2. 복구 내용

적용 파일:

- `node/public/detail.html`

복구한 상세 화면 섹션:

| 섹션 | 복구 내용 |
| --- | --- |
| AI 요약 | 현재 판단 문장, 추천 근거, 리스크 요인을 3개 카드로 표시 |
| 추천 근거 | 품질, 수급, 확률, 기술 등 긍정 요인을 요약 |
| 리스크 요인 | 감점 요인, 하락 부담, 신뢰도/가격 리스크를 요약 |
| 시나리오 판단 | 상승 재평가 조건, 기준 시나리오, 주의/제외 조건을 조건부 카드로 표시 |
| 비교 후보 | 상세 화면에서 후보 추가, 목록 보기, 후보 제거, 후보 상세 이동 가능 |

시나리오 판단 기준:

- 기대수익
- 예상 MDD
- 리스크 패널티
- 수급 점수
- MA20/MA60 가격 위치
- 신뢰도
- 현재 순위
- 고점 대비 하락률

비교 후보 저장 방식:

- 브라우저 `localStorage`의 `leeTrader.detail.compareCandidates` 키에 저장한다.
- 같은 브라우저 안에서만 유지된다.
- 버튼을 누르면 같은 종목은 중복 저장하지 않고 `비교 후보에 있음` 상태로 표시한다.
- `비교 후보 보기`에서 후보 목록 확인과 제거가 가능하다.

### 19-3. 검증 결과

검증 기준:

| 항목 | 결과 |
| --- | --- |
| inline script 문법 검사 | 통과 |
| `node/index.js` 문법 검사 | 통과 |
| `localhost:3400/detail.html?code=083650&source=main_ranking` 응답 | `AI 요약`, `추천 근거`, `리스크 요인`, `시나리오 판단`, `비교 후보 추가`, `renderScenarioPanel`, `renderAiJourneyPanel` 반영 확인 |
| `localhost:3400/api/stocks/083650?limit=90` 응답 | 200 OK, 90개 가격 데이터 응답 확인 |
| 3400 서버 반영 | `lee_trader_api` 컨테이너 재기동 완료 |

### 19-4. 다음 확인 사항

브라우저에서 `Ctrl+F5`로 강력 새로고침 후 상세 화면 하단을 확인한다.

우선 확인할 흐름:

1. 상세 화면에서 `AI 요약`, `추천 근거`, `리스크 요인` 3개 카드가 보이는지 확인한다.
2. `시나리오 판단`에 상승/기준/주의 카드가 보이는지 확인한다.
3. `비교 후보 추가`를 누른 뒤 `비교 후보 보기`에서 후보가 표시되는지 확인한다.
4. 후보 목록의 `제거` 버튼으로 해제가 되는지 확인한다.
5. 메인 변화 뱃지에서 상세로 들어왔을 때 `analysis-summary` 또는 `risk-summary` 위치 이동이 유지되는지 확인한다.

다음 개발 우선순위는 상세 화면 기능 복구 확인 후, `AI 판단 로그 DB 누적`과 자동매매 운영 로그 연결로 넘어간다.

### 19-5. 2026-06-15 시나리오 카드 배치 재정렬

복구 직후 `시나리오 판단` 섹션이 좌측 요약 컬럼에 들어가면서 3개 카드 문장이 세로로 좁게 쪼개지는 문제가 다시 확인되었다.

수정 내용:

1. `시나리오 판단` 섹션을 좌측 요약 컬럼에서 제거했다.
2. 오른쪽 넓은 분석 컬럼의 `세부 해석 대시보드` 아래로 이동했다.
3. 기본 `.scenario-grid`는 1열로 안전하게 두었다.
4. 오른쪽 넓은 영역의 `.main-scenario-panel .scenario-grid`에서만 3열을 사용한다.
5. 1180px 이하에서는 `.main-scenario-panel`도 1열로 전환한다.
6. 좌측 컬럼 폭은 `minmax(420px, 480px)`로 안정화했다.

검증 결과:

| 항목 | 결과 |
| --- | --- |
| 상세 HTML inline script 파싱 | 통과 |
| `localhost:3400/detail.html?code=083650&source=main_ranking` | 200 OK |
| `main-scenario-panel` 반영 | 확인 |
| `scenarioPanel` 개수 | 1개 |
| DOM 본문 순서 | `세부 해석 대시보드` 다음, `기술 상태와 가격 흐름` 이전 |
| 1180px 이하 1열 전환 규칙 | 반영 확인 |

이제 좌측은 오늘 판단, 핵심 근거, AI 요약, 리스크 요약처럼 짧게 읽는 영역이고, 시나리오 판단은 오른쪽 넓은 분석 흐름에서 확인한다.

## 20. 2026-06-15 최종 적용 사항 및 다음 진행 우선순위

### 20-1. 이번 최종 반영 범위

이번 정리 기준으로 실제 화면과 운영 로직에 반영된 핵심 개선은 다음과 같다.

| 구분 | 적용 내용 | 상태 |
| --- | --- | --- |
| 메인 화면 | 변화형 뱃지 `점수급등`, `관심권진입`, `리스크완화` 기준 재적용 | 완료 |
| 메인 화면 | 뱃지 클릭 시 상세 화면의 관련 분석 섹션으로 이동 | 완료 |
| 종목 상세 | `AI 요약`, `추천 근거`, `리스크 요인` 카드 복구 | 완료 |
| 종목 상세 | `시나리오 판단` 카드 복구 및 오른쪽 넓은 컬럼 배치 | 완료 |
| 종목 상세 | `비교 후보 추가/보기/제거` localStorage 기능 복구 | 완료 |
| 종목 상세 | 상세 진입, 뱃지 클릭, 후보 추가 등 상호작용 이벤트 전송 | 완료 |
| 자동매매 | 주문 생성/제출 단계의 시장일자/장시간 가드 보강 | 완료 |
| 자동매매 | KRX 영업일 캘린더 기반 테스트 추가 | 완료 |
| 자동매매 | 보유종목/종목코드/스냅샷 정합성 보강 | 완료 |

### 20-2. 검증 기준

최종 확인한 검증 항목은 다음과 같다.

| 검증 | 결과 |
| --- | --- |
| 상세 HTML inline script 파싱 | 통과 |
| `node/index.js` 문법 검사 | 통과 |
| `localhost:3400/detail.html?code=083650&source=main_ranking` | 200 OK |
| 상세 `scenarioPanel` | 1개만 존재 |
| 상세 시나리오 배치 | 오른쪽 분석 컬럼에 `main-scenario-panel`로 반영 |
| 상세 API `/api/stocks/083650?limit=90` | 200 OK, 90개 가격 데이터 확인 |
| 3400 서버 | `lee_trader_api` 재기동 반영 |

### 20-3. 다음 진행 우선순위

다음 단계는 기능을 더 늘리기보다, 실제 운영 신뢰도를 높이는 방향이 우선이다.

| 우선순위 | 진행 항목 | 이유 |
| --- | --- | --- |
| 1 | `AI 판단 로그 DB 누적` 설계 및 적용 | 현재 산출물 기반 판단을 cycle별로 쌓아야 자동매매 성과와 원인 분석이 가능하다. |
| 2 | 자동매매 `intent-preview-execution-outcome` 연결 | 매수/매도/보유 판단이 실제 주문과 성과로 어떻게 이어졌는지 추적해야 한다. |
| 3 | 상세 화면 시나리오 클릭/체류 이벤트 분석 | 메인 뱃지와 상세 시나리오가 실제 탐색을 늘리는지 확인한다. |
| 4 | 비교 후보군을 localStorage에서 DB 저장으로 확장 검토 | 브라우저 단위 후보 저장은 편하지만 장기 운영 분석에는 한계가 있다. |
| 5 | 상세 화면 데이터 신뢰도 표기 고도화 | 전문 사이트처럼 보이려면 각 지표의 기준일, 결측, 보정 여부를 더 명확히 보여야 한다. |

### 20-4. 운영 판단

현재는 화면 체류시간을 늘리기 위한 1차 구조는 복구되었다.
다음부터는 UI 추가보다 `AI가 왜 판단했는지`, `그 판단이 주문으로 이어졌는지`, `실제 성과가 어땠는지`를 DB에 누적하는 쪽이 더 중요하다.

따라서 다음 개발 시작점은 `AI 판단 로그 DB 누적`으로 둔다.
