# 2026-05-29 시스템 변경 종합 이력

> 이 문서는 오늘 발생한 모든 코드/문서/운영 변경을 한 곳에 모은 종합 이력입니다.
> 각 기존 문서의 핵심 부분에 직접 반영된 변경 외에도 영향 범위 추적용 통합 기록입니다.

---

## 1. 모델 학습 변경 — Confidence Calibration OOF 적용

### 변경 파일
- [python/model_train.py](../python/model_train.py)
  - line 13: `IsotonicRegression` import 추가
  - line 14-19: 사용하지 않게 된 `CalibratedClassifierCV`/`FrozenEstimator` import 제거
  - line 612-690: CV 루프에서 OOF 예측 수집 + IsotonicRegression 직접 학습

### 변경 이유
기존 calibration이 누설 발생:
- `cls.fit(X, y)`로 전체 데이터 학습
- `last_va_mask`(이미 X에 포함됨)로 calibration
- → calibrator가 학습 데이터로 fit되어 confidence가 실제보다 낙관적

### 새 방식 (Out-of-Fold)
1. CV 각 fold에서 cls_i가 validation set 예측
2. 모든 fold의 OOF 예측을 oof_proba에 수집
3. 최종 cls는 전체 데이터로 학습 (데이터 손실 없음)
4. `IsotonicRegression`을 OOF 예측값(누설 없음)으로 학습

### 적용 결과
- 모델 재학습 완료 (`OOF n_cal=135573, 3 folds`)
- 새 예측 분포: 평균 0.027 → 0.154 (base rate ~20%에 근접 = 정직)
- 표준편차 좁아짐 (0.069 → 0.026)
- 종목 순위 상관: Pearson 0.63, Spearman 0.77 (순위 보존)

### 백업
- `data/model.pkl.backup_20260529_calibration`
- `data/model_feature_importance.json.backup_20260529_calibration`

---

## 2. A-3 Features Merge 버그 수정

### 변경 파일
- [python/feature_builder.py](../python/feature_builder.py) — `merge_financial_momentum()` 함수 (line 620-628)

### 변경 이유
`revenue_growth_yoy`/`op_income_growth_yoy` 피처가 features 테이블에서 0행이었음.

원인:
- `merge_quality()`가 quality.csv에서 두 컬럼을 추가
- 이어서 `merge_financial_momentum()`이 financial_momentum_quarterly에서 같은 이름 컬럼을 또 가져옴
- `pd.merge_asof`가 동일 컬럼명 충돌로 `_x`/`_y` 접미사 생성
- `save_features()`의 `FEATURES_DB_COLUMNS` 정렬 단계에서 canonical 이름 없으니 `pd.NA`로 채움

### 수정 내용
`merge_asof` 직후 `_x`/`_y` 접미사 발견 시 `combine_first`로 통합 (FIN 우선, quality fallback).

### 적용 결과
| 컬럼 | Before | After |
|---|---:|---:|
| `revenue_growth_yoy` | 0 (0.0%) | **95,739 (51.6%)** |
| `op_income_growth_yoy` | 0 (0.0%) | **84,305 (45.5%)** |
| `roe_yoy` | 117,120 (63.2%) | 영향 없음 |
| `fin_momentum_score` | 141,732 (76.5%) | 영향 없음 |

최근 일자(2026-05-28) 커버리지: revenue 90.7%, op_income 73.5%

---

## 3. market_guard 컨테이너 버그 수정

### 변경 파일
- [python/run_daily_scheduler.py:200-248](../python/run_daily_scheduler.py) — `_resolve_run_steps()` 함수
- [python/run_daily_scheduler.py:286-296](../python/run_daily_scheduler.py) — `_resolve_post_sync_steps()` 함수

### 변경 이유
`lee_trader_market_guard` 컨테이너가 `SCHEDULER_COMMAND_SET=market_guard`로 설정되어 있으나 `_resolve_run_steps()`에 `market_guard` 분기가 없어 기본값(`run_manual_close_batch`)로 fallback.

결과: 약 2개월간(2026-03 ~ 2026-05) KOSPI 급락 자동 차단(kill switch)이 작동하지 않음. 대신 09:10과 14:00에 불필요한 close batch가 추가 실행되어 KIS API rate limit 위험.

### 수정 내용
```python
if command_set == "market_guard":
    return [("run_market_guard", [sys.executable, str(ROOT / "python" / "market_guard" / "run_market_guard.py")])]
```

post_sync_steps에도 `market_guard` 분기 추가 (web sync 불필요).

### 적용 결과
- 컨테이너 재시작 완료
- 첫 실행에서 KOSPI 데이터 정상 수집 (2026-05-27 KOSPI=8228.70, 1d=+2.25%, 5d=+13.16%)
- 감지 레벨 NONE (강세장이라 차단 불필요)
- `outputs/market_guard_status.json` 사상 처음 생성

### 환경변수 상태 (.env)
```
MARKET_GUARD_ENABLED=1                    # 활성
MARKET_GUARD_DRY_RUN=1                    # 감지만, 실차단 안 함
MARKET_GUARD_KOSPI_1D_THRESHOLD=-0.03    # 당일 -3% 시 CRITICAL
MARKET_GUARD_KOSPI_5D_THRESHOLD=-0.07    # 5일 -7% 시 CRITICAL
```

---

## 4. ai_filtered 파이프라인 복구

### 변경 파일
- [python/run_operational_refresh.py:37-39](../python/run_operational_refresh.py) — 스크립트 상수 2개 추가
- [python/run_operational_refresh.py:271-272](../python/run_operational_refresh.py) — steps 리스트에 2개 등록

### 변경 이유
`ai_filtered_top_candidates.csv`가 24일째 stale 상태.

원인:
- `build_ai_entry_quality_score.py`와 `build_ai_filtered_top_candidates.py`가 `run_operational_refresh.py`의 steps 리스트에 등록되어 있지 않음
- 따라서 `trade_intents.py`는 매일 실행되지만 24일 묵은 stale 파일을 stale 검사로 자동 무시
- 운영 의사결정에 entry_quality 정보 미반영

### 수정 내용
```python
("operational_buy_gate", [PYTHON, str(BUY_GATE_SCRIPT)], True),
("ai_entry_quality_score", [PYTHON, str(AI_ENTRY_QUALITY_SCRIPT)], False),       # 추가
("ai_filtered_top_candidates", [PYTHON, str(AI_FILTERED_TOP_SCRIPT)], False),    # 추가
("trade_intents", trade_intents_command, True),
```

두 빌더 모두 `required=False`로 등록 (실패 시 trade_intents가 stale 검사로 fallback).

### 적용 결과
- 24일 만에 두 파일 정상 생성
- trade_intents가 새 ai_filtered 데이터 활용 시작
- `top10_missing_entry_quality_count: 8` → `0` (8개 누락이 해소)

---

## 5. ai_adjusted_score 공식 v2 적용

### 변경 파일
- [python/build_ai_filtered_top_candidates.py](../python/build_ai_filtered_top_candidates.py) — `build_adjusted_score()` 함수

### 변경 이유
v1 공식의 3가지 의문점:
1. **entry_quality 과대 가산**: `× 0.20`이 +16점 수준 → final_score 의미를 흐림
2. **ret_10d/ret_5d 단위 미스매치**: fraction(0.05) × 0.30 = 0.015점 → 사실상 효과 없음 (단위 의도 vs 코드 불일치 가능성)
3. **risk_penalty 이중 차감**: final_score에 이미 비례 차감되었는데 또 ×0.50 차감

### 새 공식 v2
```
adjusted = final_score
         + entry_quality_bonus    # (entry_quality_score - 70) × 0.10, ±3점 내외
         - overheat_penalty       # 구간형 단기 과열 페널티 (최대 -7점)
         - risk_extra_penalty     # 구간형 위험 추가 차감 (최대 -5점)
adjusted = clip(adjusted, 0, 100)
```

#### 구간형 페널티 (이전 비례 차감 대체)
```
overheat_penalty:
    ret_5d  >= 12% : +3
    ret_5d  >=  8% : +1.5
    ret_10d >= 20% : +4
    ret_10d >= 12% : +2
risk_extra_penalty:
    risk_penalty >= 18 : +5
    risk_penalty >= 15 : +2
```

#### 임계값 정합성
- 구간 임계값(8%/12%/20%, 15/18)이 `build_ai_entry_quality_score.py`의 WATCH/BLOCK 임계값과 부합 → Stage 간 일관성

### 적용 결과 (2026-05-29 측정)
| 항목 | v1 | v2 |
|---|---|---|
| Top 5 중복 | - | 3/5 (60%) |
| Top 1 (083650) | 1위 유지 | 1위 유지 |
| 단기 급등 종목 (018880, ret_5d 17%) | Top 5 (4위) | Top 10 외(9위) ← 페널티 발동 |
| 점수 변동 폭 | ±20점 | ±7점 내 (안정) |

### 백업
- `data/ai_filtered_top_candidates.backup_20260529_pre_formula_v2.csv`
- `outputs/trade_intents.backup_20260529_pre_formula_v2.json`

---

## 6. score_kpi_monitor.json stale orphan 정리

### 변경 내용
- 삭제: `outputs/score_kpi_monitor.json` (2026-03-24, 2개월 stale orphan)
- 유지: `data/score_kpi_monitor.json` (2026-05-28 정상 갱신 중)

### 변경 이유
검토 문서가 "JSON이 갱신 안 됨"이라고 진단했으나, 실제로는:
- 코드가 의도적으로 JSON을 `data/`에, MD를 `outputs/`에 저장
- 운영자 페이지(`node/index.js:2062`)는 `data/` 우선 시도 → 정확한 데이터 표시 중
- `outputs/score_kpi_monitor.json`은 코드 변경 전 잔재로 사용되지 않음

→ 운영에는 영향 없었음. 단순 정리.

---

## 7. 운영자 페이지 스케줄러 목록 정리

### 변경 파일
- [node/index.js:3519-3534](../node/index.js) — `readAllSchedulerStatuses()` 정리
- [node/index.js:7944](../node/index.js) — payload 빈 체크 갱신
- [node/public/ops-readiness.js:178-184](../node/public/ops-readiness.js) — `runtimePairs` 정리
- [node/public/ops-readiness.js:791-794](../node/public/ops-readiness.js) — `schedulerActionLinks` 정리
- [node/public/ops-readiness.js:843-855](../node/public/ops-readiness.js) — "오늘 실행 상태" 카드 chips/kv
- [node/public/ops-readiness.js:891-902](../node/public/ops-readiness.js) — 스케줄러 테이블 행
- [node/public/ops-readiness.html:513](../node/public/ops-readiness.html) — 캐시 버스터 갱신

### 변경 내용
- 제거: RULE 3개(`rule_before_open`, `rule_after_open`, `rule_after_close`), US 3개(`us_macro`, `us_macro_shadow`, `us_pipeline`)
- 추가: `market_guard` 스케줄러

### 운영자 페이지 최종 스케줄러 목록 (5개)
```
close (18:10) · intraday/recovery (12:00) · auto_buy (09:30) · live_sync (10:00/14:00/18:00) · market_guard (09:10/14:00)
```

---

## 8. 종료 서비스(RULE/US) Docker/파일 정리

### Docker 컨테이너 제거 (5개)
```
lee_trader_scheduler_us_macro            (가동 중 → 정지·제거)
lee_trader_scheduler_us_macro_shadow     (가동 중 → 정지·제거)
lee_trader_scheduler_rule_before_open    (8일 전 Exited → 제거)
lee_trader_scheduler_rule_after_open     (8일 전 Exited → 제거)
lee_trader_scheduler_rule_after_close    (8일 전 Exited → 제거)
```

### Stale 상태 파일 삭제 (6개)
```
outputs/rule_before_open_scheduler_status.json
outputs/rule_after_open_scheduler_status.json
outputs/rule_after_close_scheduler_status.json
outputs/us_macro_scheduler_status.json
outputs/us_macro_shadow_scheduler_status.json
outputs/us_pipeline_scheduler_status.json
```

### docker-compose.yml 서비스 정의 제거 (6개)
```
scheduler-rule-after-close
scheduler-rule-before-open
scheduler-rule-after-open
scheduler-us-macro
scheduler-us-macro-shadow
scheduler-us-pipeline
```

docker-compose.yml: 380줄 → 221줄 (-42%)

### 현재 활성 서비스 (8개)
```yaml
postgres                       # DB
python-pipeline                # 일회성 파이프라인
scheduler                      # close 배치 (18:10)
scheduler-recovery             # intraday (12:00)
scheduler-auto-buy             # AI 자동매수 (09:30)
scheduler-live-account-sync    # 계좌 동기화
scheduler-market-guard         # 시장 급락 감시 (09:10/14:00)
node-api                       # 운영자 페이지 (port 3400)
```

---

## 9. 코드 docstring 보강

### 변경 파일 (3개)
- [python/scoring/final_score.py](../python/scoring/final_score.py) — Stage 1 v9_flow 공식 모듈 docstring (1,572자)
- [python/build_ai_entry_quality_score.py](../python/build_ai_entry_quality_score.py) — Stage 2 진입 안전성 평가 모듈 docstring (1,369자)
- [python/build_ai_filtered_top_candidates.py](../python/build_ai_filtered_top_candidates.py) — Stage 3 가산점 적용 모듈 docstring (v2 공식 포함, 1,434자+)

### 변경 이유
3개 모듈 모두 module docstring 부재 → 미래 유지보수 시 설계 의도 불명확.

### 변경 내용
- 각 Stage의 역할 명시
- 입력/출력 파일 명시
- 점수 공식 코드와 일치 기록
- 운영 파이프라인 위치 표시
- 설계 의도·이력·미문서화 가중치 출처 솔직 기록

---

## 10. 문서 갱신

### CLAUDE.md 재작성
- 380줄 → 176줄 (-54%)
- 종료된 RULE/US 잔재 제거
- KR AI 단독 운영 체제 반영

### 신규 작성
- `doc/시스템로드맵.md` — 평생 운용 5단계 로드맵 + 핵심 원칙 7가지 + 정기 점검 체크리스트
- `doc/20260529_시스템변경이력.md` — 이 문서

### 갱신 (이번 작업)
- `doc/20260529_검토사항.md` — 우선순위별 완료 상태 표시
- `doc/score_column_definitions.md` — v9_flow 가중치, ai_adjusted_score v2
- `doc/feature_dictionary.md` — revenue_growth_yoy/op_income_growth_yoy 복구
- `doc/KR_운영핵심컬럼사전.md` — v9_flow 가중치
- `doc/modules/Lee_trader_ai/OPERATIONS.md` — 파이프라인 변경, market_guard
- `doc/운영명령어.md` — RULE/US 제거, market_guard 추가

---

## 종합 효과

### 안전성 회복
- **market_guard 정상화**: 2개월간 무력화됐던 KOSPI 급락 자동 차단 복구
- **calibration 누설 수정**: confidence 신뢰도 향상

### 데이터 정합성
- **A-3 merge 버그**: 누락됐던 재무 YoY 피처 51%/45% 복구
- **ai_filtered 파이프라인 복구**: 24일 stale 해소

### 운영 단순화
- 종료 서비스 잔재 제거 (컨테이너 5개, 파일 6개, docker-compose 6개)
- 운영자 페이지 스케줄러 목록 정확화
- CLAUDE.md 60% 축약

### 의사결정 품질
- `ai_adjusted_score` v2: 의문점 3가지 해소, Stage 간 임계값 정합성 확보
- Top 1 (083650) 보존, 단기 급등 종목 페널티 정상 작동

---

## 영향받은 운영 데이터 갱신

| 산출물 | 갱신 시각 | 내용 |
|---|---|---|
| `data/model.pkl` | 02:09 | OOF calibration 모델 |
| `data/predictions.csv` | 02:09 | 204종목 새 예측 |
| `data/ranking_final.csv` | 02:12 | top 20 중 10개 교체 (Top 1 보존) |
| DB `daily_ranking` 2026-05-28 | 02:12 | OOF ranking으로 갱신 |
| `data/features.csv` | 02:27 | A-3 버그 수정 반영 |
| DB `features` | 02:27 | revenue/op_income YoY 51%/45% 복구 |
| `data/ai_entry_quality_score.csv` | 02:24 | 24일 만에 재생성 |
| `data/ai_filtered_top_candidates.csv` | 02:45 | v2 공식 적용 |
| `outputs/trade_intents.json` | 02:50 | 새 입력 기반 |
| `outputs/order_requests_preview.json` | 03:04 | 자동매매 초안 |
| `outputs/live_order_preview.json` | 03:13 | 운영자 페이지 표시용 |
| WEB DB (Supabase) | 03:12 | 핵심 테이블 sync 완료 |

---

## 다음 운영 사이클 (자동 적용)

다음 09:30 KST `auto_buy` 스케줄러부터:
- 새 OOF calibration 모델 자동 사용
- 새 ai_adjusted_score v2 공식 자동 사용
- ai_filtered 파이프라인 자동 실행

다음 18:10 KST `close batch`부터:
- features 재빌드 시 A-3 수정 적용 (이미 적용됨)
- 새 모델로 predictions 재계산
- 새 가중치로 ranking 생성

다음 09:10/14:00 KST부터:
- `market_guard` 정상 가동 (KOSPI 급락 감시 시작)

---

## 잔여 작업 (미진행)

`doc/20260529_검토사항.md` 참조:
- 3순위: KR 자동매매 회귀 테스트 추가 — 2026-07~08 권장
- 5순위 일부: walkforward/confidence ALERT — 시간 해결 (matured_dates 누적)
- 6순위: 리포트 인코딩 정리 — 우선순위 낮음

장기 방향:
`doc/시스템로드맵.md` 참조 — 평생 운용 5단계 발전 로드맵

---

*2026-05-29 작성 — 오늘 발생한 모든 시스템 변경 종합 기록*

---
---

# 2026-06-05 변경 이력 (추가)

> 2026-06-05 발생 변경. 종가 배치 후 블로그 초안이 생성되지 않는 문제 복구 + AI 성과 페이지 표시 개선.
> AI 랭킹(live)은 Gate 전환 직전이라 의도적으로 동결 — 변경 없음.

---

## 11. 블로그 초안 자동 생성 복구 — jinja2 의존성 추가

### 변경 파일
- [python/requirements.txt](../python/requirements.txt) — `jinja2` 추가 (마지막 줄)

### 증상
- 종가 배치 후 블로그 초안이 생성되지 않음
- 로컬(localhost:3400)은 어제(2026-06-04) 글로 폴백 표시
- 웹(leetrader.kr)은 "오늘 생성된 블로그 초안 없음" — 웹 DB에 `blog_drafts_*` 행이 아예 없음

### 원인
- `close` 배치의 후처리 단계 [generate_blog_posts.py](../python/generate_blog_posts.py)가 모듈 로드 시점(line 26)에 `from jinja2 import ...` 실행
- python-runtime 이미지는 [python/Dockerfile](../python/Dockerfile)에서 `requirements.txt`만 설치하는데 거기에 `jinja2`가 없어 `ModuleNotFoundError`로 즉시 크래시
- 이 단계는 [run_daily_scheduler.py:47](../python/run_daily_scheduler.py) `OPTIONAL_POST_SYNC_STEPS`라서 실패해도 배치는 "성공"으로 종료 → **조용히 누락**(점검 문서의 "jinja2 경고"가 이것)
- 크래시로 파일 생성도, [save_drafts_to_db()](../python/generate_blog_posts.py)(로컬 `DATABASE_URL` + 웹 `WEB_DATABASE_URL` 둘 다 저장)도 실행되지 않음

### 수정 내용
```
# python/requirements.txt
beautifulsoup4
urllib3
jinja2        # 추가
```
- `lee_trader-python-runtime:latest` 이미지 재빌드(`docker compose build scheduler`)
- scheduler 5종(`scheduler`, `scheduler-recovery`, `scheduler-auto-buy`, `scheduler-live-account-sync`, `scheduler-market-guard`) 새 이미지로 재기동 (장 마감 후 21시대 → live 자동매매 영향 없음, `.env` 플래그 그대로 유지)

### 검증
- 컨테이너 내부 `python -c "import jinja2"` → 3.1.6 확인
- 컨테이너 내부 `generate_blog_posts.py --type all --force` → 6파일 생성, 실패 0건
- 로그: `DB 저장 완료(local)` + `DB 저장 완료(web)` 모두 출력

### 적용 결과
| 항목 | Before | After |
|---|---|---|
| 컨테이너 jinja2 | 없음 (import 크래시) | 3.1.6 |
| 2026-06-05 초안 파일 | 미생성 | typeA/B/C × naver/tistory 6개 |
| 로컬 DB `blog_drafts_2026-06-05` | 없음 | 저장 |
| 웹 DB `blog_drafts_2026-06-05` | 없음 | 저장 (오늘분 백필) |
| 내일 이후 | silent 누락 | 18:10 종가 배치에서 자동 생성·웹 반영 |

---

## 12. AI 성과 페이지 개선 — 코호트 누적 뷰 (표시 로직, 모델 무관)

### 변경 파일
- [node/index.js](../node/index.js) — `/api/meaningfulness-cohorts` 엔드포인트 신규 (`/api/meaningfulness-outcomes` 직후)
- [node/public/meaningfulness.html](../node/public/meaningfulness.html) — 코호트 패널 + 기본 표시일 변경 + 문구 개선

### 변경 이유
페이지가 전부 `0% / +0.00% / 판단 보류`로 보이던 것은 고장이 아니라, **초기화 시 항상 최신일(관찰 1거래일)을 기본 표시**했기 때문. 성과는 60거래일 관찰이 쌓여야 의미가 생기는데 최신일은 정의상 무조건 0.

### 수정 내용
- **A. 기본 표시일 = 가장 성숙한 코호트**: 신규 엔드포인트가 `daily_ranking` 분석일 중 관찰일수 최대인 날짜(`most_matured_date`)를 반환 → 프런트가 이를 기본 선택, date picker max는 최신일로 유지
- **B. "코호트 누적 성과" 패널 신설**: 분석일별 Top-N을 그날부터 추적한 결과(관찰일·종목수·히트율·평균수익률) + 성숙 코호트(관찰 20거래일↑) 누적 요약 카드
- **C. 문구 명확화**: "판단 보류" → "축적 중 D+N", 안내문에 "60거래일 만기까지 N일 남음" 표기

### 구현 메모
- 신규 엔드포인트는 기존 검증 함수 `getRanking()`(점수순 정렬) + `buildMeaningfulnessOutcomes()`(순방향 결과)를 재사용 → 신규 SQL 최소화, 리스크 낮음
- 코호트는 최대 10개로 균등 샘플, 10분 메모리 캐시
- live 점수 공식·모델·주문 경로에는 영향 없음 (순수 표시 계층)

### 적용 결과 (2026-06-05 측정)
- `most_matured_date` = 2026-03-17 (55거래일 관찰)
- 누적 요약: 성숙 6코호트 평균 **+3.77%**, 히트율 **39.8%**
- 코호트별 패턴: 3월 코호트 플러스(+20.6%/+6.2%/+14.6%), 4월말~5월 코호트 마이너스 → 실제 트랙레코드가 화면에 그대로 노출
- node-api 재시작 후 페이지 HTTP 200, 신규 패널·JS·API 정상 동작 확인

---

## 13. AI 랭킹 — live 동결 결정 (코드 변경 없음)

### 결정
개선회의 결과, **현재 live 랭킹 공식·피처는 변경하지 않는다.**

### 사유
- Gate 전환(PILOT → BUY_ALLOWED, 2026-06-10 전후) 직전. 랭킹 공식을 바꾸면 누적 중인 벤치마크·60일 만기 데이터가 새 공식 기준으로 무효화되어 전환이 오히려 지연됨
- CLAUDE.md §7 원칙: "현재 KR AI는 Gate 전환 직전이라 신규 피처 추가 시 인과 해석 불가능"
- §5 원칙: ranking_builder 수정은 점수·순위·UI·주문 동시 영향 → shadow 비교 선행

### 향후 경로
- 2026-08 이후 운영 데이터(실제 알파 누수 패턴) 기반 재검토
- 개선 아이디어는 live와 분리된 격리 연구 트랙(중형주 확장 연구 등)에서만 실험

---

## 잔여 작업 (미진행)

- **Docker 빌드 컨텍스트 비대화**: 재빌드 시 빌드 컨텍스트가 2GB+ 전송됨(`.dockerignore` 미비로 `data/`·`.venv_research/`·`outputs/` 등 포함). 단, [python/Dockerfile](../python/Dockerfile)이 `COPY . .`라 `.dockerignore` 추가 시 이미지 내용물에도 영향 → 신중히 정리 필요. 우선순위 낮음.

---

*2026-06-05 추가 — 블로그 초안 자동 생성 복구 및 AI 성과 표시 개선 (live 랭킹 동결)*
