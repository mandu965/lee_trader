# 2026-04-29 진행 메모

## 현재 목표

- RULE 자동매매 모듈의 전체 실행 흐름을 만든다.
- 단, 실주문 API 호출은 하지 않는다.
- after-close -> before-open -> paper execution -> reconciliation 까지 모두 수행한다.

## 오늘까지 완료

- [x] RULE signal / backtest / portfolio / order preview 생성
- [x] paper state 누적
- [x] hold / exit / reduce / cooldown 검증
- [x] RULE 운영 화면 1차 추가
- [x] `run_rule_after_close.ps1`
- [x] `run_rule_before_open.ps1`
- [x] before-open paper execution simulator 추가
- [x] execution results / reconciliation report 생성
- [x] 거래일 캘린더 파일 추가
- [x] before-open 거래일 / 세션 / 이전 reconciliation 상태 검증 추가
- [x] execution 결과 history jsonl 누적
- [x] paper state snapshot history jsonl 누적
- [x] aborted run 사유를 RULE 화면 상태 문구에 반영

## 현재 산출물

- `data/rule_signals.csv`
- `outputs/rule_portfolio_plan.json`
- `outputs/rule_order_preview.json`
- `outputs/rule_account_paper_state.json`
- `outputs/rule_execution_results.json`
- `outputs/rule_execution_reconciliation_report.md`
- `outputs/rule_strategy_backtest_report.json`
- `outputs/rule_strategy_daily_report.md`

## 현재 판단

- 지금 상태는 실주문만 제외한 paper 운영 흐름으로 거의 연결되었다.
- 아직 남은 것은 일자별 history/snapshot 누적, before-open 실시간 시가 조회 연동 전 단계 정리, UI execution 영역 보강이다.

## 다음 작업

- [ ] paper 1~2주 관찰용 snapshot/ledger 구조 추가
- [ ] 이후에도 `run_mode=paper`, `RULE_LIVE_ENABLED=0`, `RULE_ORDER_SUBMIT_ENABLED=0` 유지
