# 작업계획서 — KR AI 주력 + US AI paper 검증 운영 로드맵

**작성일**: 2026-05-20 / **최종 방향 확정**: 2026-05-22  
**운영 방향**: KR Rule 중단 → KR AI 주력 + US AI paper 전용  
**수익 목표**: 월 2% — KR AI 안정화 후 단계적 달성 (US 실전 전환 전까지 KR 단독 기준)

> **운영 계층 방향 문서**: [20260522_시스템운영방얀.md](20260522_시스템운영방얀.md)  
> US는 당분간 paper 전용 유지. 실전 전환은 KR 안정화 + 별도 승격 조건 충족 후 재검토.

---

## 0. 운영 방향 요약

```
중단
└── KR Rule 자동매매: RULE_LIVE_ENABLED=0 (즉시)
    └── 기존 포지션: 보유자 직접 청산 처리

유지·강화
├── KR AI 자동매매: PILOT → BUY_ALLOWED 전환 준비 (5월 28일 예상)
└── US AI 자동매매: 당분간 paper 전용 유지 + 정책/백테스트/라이프사이클 검증
```

---

## 0-1. 2026-05-22 운영 결론 (최종 확정)

> 이 섹션은 `20260522_시스템운영방얀.md`, `20260522_자동매매 운영 방안.md` 두 문서의 핵심 결론을 통합한 것이다.  
> 이후 두 문서는 배경 참고용으로만 유지한다.

---

### 시스템 운영 계층 (Tier)

| Tier | 시스템 | 역할 | 자금 |
|---|---|---|---|
| 1 | KR AI 자동매매 | 주력 실전 전략 | 실자금 대부분 |
| 2 | US AI 자동매매 | paper 검증 단계 | 실자금 배정 없음 |
| 인프라 | Market Guard | 전 시스템 공통 방어막 | — |

**핵심 원칙**: 지금은 다각화보다 집중. KR AI 하나를 제대로 만드는 것이 우선이다.

---

## 0-2. 착수용 정리 (2026-05-22 기준)

이 문서는 방향 문서이면서 동시에 **서버 착수 체크리스트**로 사용한다.

| 상태 | 의미 |
|---|---|
| 완료 | 코드/설정/검증까지 끝나서 추가 액션이 필요 없음 |
| 서버 반영 대기 | 소스 수정은 끝났고 서버 `.env` 반영 또는 배포만 남음 |
| 서버 검증 필요 | 실제 데이터와 운영 로그로 확인해야 다음 단계로 갈 수 있음 |
| 장기 보류 | 현재 우선순위에서 제외, 조건 충족 후 재검토 |

**현재 판단**:
- 이 문서는 실데이터 기반 검증 과제가 중심이지만, **실데이터가 있는 작업만 남은 상태는 아니다.**
- `close batch 정상화`, `BUY_ALLOWED 전환`, `KR-E 재학습/OOS`, `ranking.html` 이력 복구 확인은 **서버 검증 필요** 상태다.
- `KR-G`, `KR-I`, `Market Guard` 일부는 **소스 구현 완료 + 서버 반영 대기** 상태다.

### 지금 바로 서버에서 할 일

1. `run_manual_close_batch.py --local` 전체 재실행 후 완주 여부 확인
2. 웹 DB `daily_ranking` 날짜 이력과 `ranking.html` 초기화 증상 재확인
3. KR 관련 `.env` 반영 확인
4. `model_train.py` 재학습 실행 후 OOS / paper 비교
5. `BUY_ALLOWED` 전환 여부 판단

### `BUY_ALLOWED` 전환 기준

1. `close batch`와 웹 동기화가 연속 실행에서 안정적일 것
2. KR 재학습 결과가 기존 대비 OOS 수익률 열화 없이 MDD 악화가 통제될 것
3. `trailing_stop_exit`, `max_holding_days_*`, `replace deferred` 로그가 의도대로 바뀔 것
4. KR cash / exposure 상태가 제한 해제 후에도 관리 가능할 것

### 서버 실행 체크리스트

#### 1. close batch 전체 재실행

실행:
```bash
python run_manual_close_batch.py --local
```

확인:
- 배치가 `sync_web_display_data.py` 단계까지 중단 없이 끝나는지
- 스케줄러 상태가 `error`로 남지 않는지
- 실패 시 어느 단계에서 끊기는지 로그를 바로 기록

완료 기준:
- `run_manual_close_batch.py --local` 전체 완주
- 후속 수동 재실행 없이 종료 코드 정상

#### 2. 웹 동기화 / 랭킹 이력 확인

확인:
- `sync_web_display_data.py` 로그에서 아래 중 어떤 경로를 탔는지
  - `Loaded KR ranking archives ...`
  - `Loaded KR ranking history from research.ranking_history ...`
  - `KR ranking archive/history unavailable; fallback to source DB daily_ranking sync`
- 웹 DB `daily_ranking`의 최근 날짜 이력이 5영업일 이상 유지되는지
- `ranking.html`에서 `NEW` 과다, `급부상`, `급하락` 공란 증상이 사라졌는지

완료 기준:
- 웹 DB `daily_ranking` 최근 5영업일 이상 존재
- `ranking.html` 초기화 증상 미재현

#### 3. KR 서버 `.env` 반영 확인

필수 값:
```env
AI_MAX_HOLDING_DAYS=30
AI_TRAILING_STOP_MIN_PROFIT=0.08
AI_TRAILING_STOP_PCT=0.10
GLOBAL_MAX_DAILY_BUY_RATIO=0.0
GLOBAL_MAX_WEEKLY_BUY_RATIO=0.0
GLOBAL_BLOCK_BUY_ON_DAILY_LOSS_UNAVAILABLE=1
```

확인:
- 반영 후 관련 컨테이너/스케줄러 재기동 여부
- 실행 로그에서 값이 기대대로 로드되는지

완료 기준:
- 설정값 반영 확인
- 재기동 후 게이트/리스크 로그 정상

#### 4. KR 재학습 실행 및 OOS 검증

실행:
```bash
python model_train.py
```

비교 기준:
- 기존 대비 OOS 수익률 열화 여부
- 기존 대비 MDD 악화 여부
- 필요 시 paper 결과와 방향성 일치 여부

완료 기준:
- 재학습 정상 종료
- OOS 기준에서 live 반영 가능한 수준으로 판단

#### 5. `BUY_ALLOWED` 전환 판단

전환 전 체크:
- close batch 안정
- 웹 이력 복구 정상
- KR 재학습/OOS 통과
- `trailing_stop_exit`, `max_holding_days_*`, `replace deferred` 로그 정상
- KR cash / exposure 감당 가능

완료 기준:
- 위 조건 충족 시에만 `BUY_ALLOWED` 전환
- 하나라도 미충족이면 전환 보류 후 원인 항목 재작업

---

### US AI 실전 전환 조건 (4가지 모두 충족 시에만 재검토)

| 조건 | 기준 |
|---|---|
| KIS smoke test 통과 | broker_order_id 정상 반환 |
| paper 30거래일 이상 | buy/sell lifecycle 정합성 확인 |
| 백테스트 체결 가정 검토 | 실운영 주문 경로와 동형성 확인 |
| KR AI 월 1% 이상 안정 | 3개월 이상 실거래 데이터 |

---

### KR 자동매매 운영 리듬 결론

**매수 집행**
- 진입 시간: `09:30` 유지 (변경 시 서버 데이터 확인 후 결정)
- 갭 필터: 이미 실환경 작동 확인 — 별도 개발 불필요

```
ENTRY_GAP_BLOCK_UP_PCT=0.03      갭 +3% 이상 → 매수 차단 (소프트)
ENTRY_GAP_HARD_BLOCK_UP_PCT=0.05 갭 +5% 이상 → 매수 차단 (하드)
ENTRY_GAP_BLOCK_DOWN_PCT=-0.04   갭 -4% 이하 → 매수 차단
```

**장중 자동화 — 도입 안 함**

| 검토 항목 | 결정 | 이유 |
|---|---|---|
| 텔레그램 급락 알림 | 도입 안 함 | 운영자가 직접 계좌 확인 |
| intraday refresh 스케줄 | 보류 | 알림 없으면 의미 제한적 |
| 오후 자동 매수 추가 | 도입 안 함 | 현 단계 불필요 |
| 장중 자동 손절 | 도입 안 함 | 30일 예측 모델과 충돌 |

**수동 매도 원칙**: 이력 정합성 유지를 위해 마감 배치(close batch)를 통해 처리. HTS/MTS 직접 매도 시 AI가 다음날 동일 종목 재매수할 수 있음.

---

### 지금 수익률에 가장 직접적인 과제 (서버 접속 후 순서)

```
1순위: BUY_ALLOWED 전환 — PILOT 수량 제한 해제, 가장 큰 수익 레버
2순위: 모델 재학습 — pred_return_30d + Sample Weight + Calibration 반영
3순위: 보유기간 정합 확인 — AI_MAX_HOLDING_DAYS=30, trailing 파라미터 서버 반영
```

---

## 1. 월 2% 달성 경로 분석

### 1-1. 목표 현실성

| 기준 | 수익률 |
|---|---|
| S&P500 장기 평균 | 연 ~10% → 월 0.8% |
| QQQ 장기 평균 | 연 ~16% → 월 1.2~1.4% |
| **월 2% 목표** | **연 26.8%** — 상위 퀀트펀드 수준, 어렵지만 불가능하지 않음 |

**월 2% 달성 조건 (2026-05-22 방향 수정)**:  
현 단계에서는 KR AI 단독으로 1.0~1.5%/월 달성이 우선 목표다.  
US AI는 paper 전용이므로 기여 없음.  
US 실전 전환 이후에야 합산 2% 목표 재산출이 가능하다.

---

### 1-2. 현재 성과와 목표 간 갭

| 시스템 | 현재 성과 | 현재 목표 기여 |
|---|---|---|
| KR AI (실거래, PILOT) | top5 누적 +1.1% → 월 약 **0.55%** | **1.0~1.5%/월** (BUY_ALLOWED 이후) |
| US AI (paper 전용) | 운영 극초기, WDC 1종목 | **0%** (paper 단계, 기여 목표 보류) |
| **현실적 단기 목표** | 현재 약 0.55%/월 | **KR 단독 1.0~1.5%/월** |

**갭의 원인**: KR AI는 PILOT 제한(최대 4종목). US는 paper 전용 방향으로 확정됐으므로 합산 기여 없음.  
**현 시점 집중 과제**: KR BUY_ALLOWED 전환 + 보유·청산 정책 안정화.

---

## 2. KR AI 자동매매 — 개선 과제

> **운영 방안 검토 결과 (2026-05-22)**: [20260522_자동매매 운영 방안.md](20260522_자동매매%20운영%20방안.md)  
> 장중 자동화(알림·intraday refresh 스케줄) 논의 완료 → 현 단계에서 도입 안 함.  
> **갭 필터는 이미 실환경 작동 확인** (`ENTRY_GAP_BLOCK_UP_PCT=0.03` / `hard=0.05` / `down=-0.04`).  
> 지금 수익률에 가장 직접적인 과제는 아래 순서다: BUY_ALLOWED 전환 → 모델 재학습 → 보유기간 정합.



### [KR-A] 모델 예측 기간과 보유 기간 정합 (핵심)

**파일**: `python/model_train.py`, `python/ai_position_risk.py`

**현재 문제**:

| 모델 예측 기간 | 실제 보유 기간 |
|---|---|
| `pred_return_60d` / `pred_return_90d` 중심 | `AI_MAX_HOLDING_DAYS=15일` |

60~90일 신호로 뽑은 종목을 15일 만에 끊으면 **모델이 맞아도 수익 실현이 안 됨.**  
이 불일치가 현재 0.55%/월에 가장 큰 영향을 주는 구조적 문제.

**작업 내용**:
1. `label_builder.py`에 이미 있는 `pred_return_30d` 레이블 → `model_train.py`에 학습 타겟 추가
2. `AI_MAX_HOLDING_DAYS` 를 30일로 조정 검토 (서버 확인 후)
3. 30일 타겟이 안정화되면 60/90d 혼합 가중치 재검토

**운영 반영 조건**:
- `pred_return_30d` 추가 모델이 기존 기준 대비 OOS 수익률·MDD·turnover 기준에서 열화되지 않을 것
- `AI_MAX_HOLDING_DAYS=30` 변경은 백테스트 또는 paper 재현 결과 확인 후 반영할 것
- 모델 타겟 변경과 보유기간 변경을 동시에 반영할 경우, 변경 전후 비교표를 남길 것

**우선순위**: 1순위 (서버 접속 후 즉시)

---

### [KR-B] 불필요 모델 타겟 정리 (학습 비용 절감)

**파일**: `python/model_train.py`

**현재 확인된 사항** ([20260518 학습 및 예측 전략 논의](explain/20260518%20학습%20및%20예측%20전략%20논의.md)):

| 타겟 | 상태 | 조치 |
|---|---|---|
| `prob_top20_90d` | 점수에 전혀 반영 안 됨 (데드코드) | **즉시 제거** |
| `pred_mdd_90d` | `pred_mdd_60d`와 상관 0.84 (거의 중복) | MDD 정확도 측정 후 제거 |
| `pred_return_30d` | 레이블 존재, 모델 미학습 | **즉시 추가** (30d 정합성 목적) |

**우선순위**: 1순위 (서버 접속 후 즉시)

---

### [KR-C] MDD 예측 정확도 측정 인프라 구축

**파일**: 신규 `python/archive_predictions.py`, `python/evaluate_prediction_accuracy.py`

**현재 문제**: `pred_mdd_60d`가 `risk_penalty`에 사용 중이지만 예측이 실제로 정확한지 모르는 상태.

**작업 내용**:
1. 매일 배치 후 `predictions.csv → data/predictions_archive/{YYYYMMDD}.csv` 저장
2. 60일 경과 후 예측 vs 실현값 RMSE / 방향 일치율 집계

**우선순위**: 2순위

---

### [KR-D] 청산 로직 개선

**파일**: `python/ai_position_risk.py`

**발견된 문제 2가지**:

#### D-1. `ai_position_state.json` 복구 취약점

서버 재시작 / 파일 손실 시 `entry_date=오늘`, `peak_price=현재가`로 초기화 → trailing stop·max_holding 무력화.

```python
# 현재: 신규 포지션 등록 시 silent 처리
# 개선: first_seen_reason 필드 추가 + WARN 로그 강화
positions[code] = {
    ...
    "first_seen_reason": "NEW_POSITION" or "STATE_RECOVERED",
}
```

#### D-2. `max_holding_days` 하드캡 추가

수익 2% 이상이면 15일 초과해도 무기한 보유. `AI_MAX_HOLDING_DAYS_HARD_CAP` 환경변수 추가 (기본 45일).

```
현재: 15일 초과 + 수익 2% 미만 → exit
추가: 45일 초과 → 수익 무관 reduce (하드캡)
```

**우선순위**: 2순위 (소스 환경에서 구현 가능)

---

### [KR-E] prob_top20 Calibration + Sample Weight

**파일**: `python/model_train.py`, `python/final_score.py`

LightGBM 분류기의 raw 확률은 실제 확률과 다를 수 있음. `confidence_score` 신뢰도 직결.

- Calibration: Isotonic Regression 후보정 추가
- Sample Weight: 최근 데이터에 지수적 감쇠 가중치 부여

**우선순위**: 3순위 (수급 피처 30일 안정화 후)

---

### [KR-G] KR AI 매도 정책 개선 — 보유 기간 정합성 (2026-05-21 설계 확정)

**배경**: trailing stop 파라미터와 max_holding soft exit 기준이 기존 15일 운영 기준에 맞춰져 있어, 30일 보유 목표에서 winner를 조기에 청산하는 구조적 문제가 있다. 현재 문제의 핵심은 "강한 종목을 너무 빨리 내보내는 것"이며, stop_loss 조정보다 trailing/holding 완화가 우선이다.

**구현 목록**:

| # | 항목 | 변경 내용 | 파일 | 방법 | 우선 |
|---|---|---|---|---|---|
| 1 | trailing 발동 임계 완화 | `AI_TRAILING_STOP_MIN_PROFIT`: 3% → 8% | `.env` | env var 수정 | 즉시 |
| 2 | trailing 허용 폭 확대 | `AI_TRAILING_STOP_PCT`: 5% → 10% | `.env` | env var 수정 | 즉시 |
| 3 | max_holding soft exit 기준 완화 | `pnl_pct < 0.02` → `pnl_pct < 0.0` | `python/ai_position_risk.py` | 코드 수정 | 소스 배포 후 |
| 4 | 진입 후 교체 유예 | `min_hold_days_before_replace` 파라미터 추가 (기본 10영업일) | `python/apply_execution_policy.py` | 코드 수정 | 소스 배포 후 |
| 보류 | stop_loss | 7% 유지, 1~4번 결과 비교 후 2차 결정 | — | 미적용 | — |
| 보류 | 구간별 trailing B안 | 1~4번 안정화 후 검토 | — | 미적용 | — |

---

#### KR-G-1/2: `.env` 수정 (서버 접속 즉시 적용)

```env
AI_TRAILING_STOP_MIN_PROFIT=0.08
AI_TRAILING_STOP_PCT=0.10
```

**현재 → 변경 효과**:
- 기존: +3% 이상 수익에서 고점 대비 -5% 하락 시 trailing stop 발동
- 변경: +8% 이상 수익에서 고점 대비 -10% 하락 시 발동
- 효과: 정상적인 한국 주식 변동성(±5% 수준) 내에서 winner를 유지할 수 있도록 여유 확보

**적용 방법**: `.env` 수정 후 ai 배치 컨테이너/스케줄러 재기동

---

#### KR-G-3: `ai_position_risk.py` 코드 수정

**파일**: `python/ai_position_risk.py`, 256~265행

```python
# 변경 전
elif pnl_pct is None or pnl_pct < 0.02:
    # 수익률 2% 미만인 경우만 청산 (수익권은 하드캡까지 유지)
    action = "exit" if (pnl_pct is None or pnl_pct <= 0) else "reduce"
    reason = (
        f"max_holding_days_{action} days={holding_days} > {max_holding_days}"
    )
    logger.warning(
        "AI_POSITION_RISK_BLOCK | code=%s reason=%s", code, reason
    )
    return action, reason
```

```python
# 변경 후
elif pnl_pct is None or pnl_pct < 0.0:
    # 0% 미만인 경우만 청산 (0% ~ hard_cap 구간은 HOLD)
    action = "exit"
    reason = f"max_holding_days_exit days={holding_days} > {max_holding_days}"
    logger.warning(
        "AI_POSITION_RISK_BLOCK | code=%s reason=%s", code, reason
    )
    return action, reason
```

**변경 전 동작**: max_holding 도달 후 pnl 0~2% 구간 → `reduce`(절반 축소)  
**변경 후 동작**: max_holding 도달 후 pnl 0% 이상 → hard_cap(45일)까지 HOLD, 0% 미만만 exit

추가로 소스 기본값 정렬 (line 182):
```python
# 변경 전
max_holding_days = _cfg_int("AI_MAX_HOLDING_DAYS", 15)

# 변경 후 (서버 .env에 이미 30 반영됨, 소스 기본값도 정합 유지)
max_holding_days = _cfg_int("AI_MAX_HOLDING_DAYS", 30)
```

---

#### KR-G-4: `apply_execution_policy.py` 코드 수정

**파일**: `python/apply_execution_policy.py`

**목적**: 진입 직후 10영업일(기본값) 이내에는 점수·랭크 기반 교체를 유예한다. 새로 진입한 종목이 초기 변동성으로 잠시 랭크 밖에 나갔다가 돌아오는 경우 불필요한 교체를 방지.

**① CLI arg 추가** (`parse_args` 함수, 기존 `--max-replacements-per-cycle` 인자 근처):

```python
parser.add_argument(
    "--min-hold-days-before-replace",
    type=int,
    default=int(get_production_config_value(
        ["execution_policy", "min_hold_days_before_replace"], 10
    )),
    help="Minimum business days after entry before score-based replacement is allowed (default 10).",
)
```

**② per-code 루프 내 경과 영업일 계산** (`_ai_position_state = load_ai_position_state()` 이후, 각 종목 처리 시작 부분):

```python
# 진입 후 경과 영업일 계산 (교체 유예 판단용)
_pos_entry_date_str = (_ai_position_state.get(code) or {}).get("entry_date")
_holding_bdays = 0
if _pos_entry_date_str:
    try:
        _holding_bdays = business_days_since(
            pd.Timestamp(_pos_entry_date_str).normalize(), _today
        )
    except Exception:
        pass
```

**③ REPLACE_CANDIDATE 설정 직전에 유예 체크 삽입** (line 988~998 rank-based 교체 구간):

```python
# 변경 전 (line 988~998)
else:
    action = "REPLACE_CANDIDATE"
    reasons.append(f"outside hold range top{args.max_hold_rank}")

# 변경 후
else:
    if _holding_bdays < args.min_hold_days_before_replace:
        reasons.append(
            f"replace deferred: holding {_holding_bdays}bd < min {args.min_hold_days_before_replace}bd"
        )
    else:
        action = "REPLACE_CANDIDATE"
        reasons.append(f"outside hold range top{args.max_hold_rank}")
```

같은 패턴을 "missing from official recommendation set" 분기(line 997)에도 적용:

```python
# 변경 전
else:
    action = "REPLACE_CANDIDATE"
    reasons.append("missing from official recommendation set")

# 변경 후
else:
    if _holding_bdays < args.min_hold_days_before_replace:
        reasons.append(
            f"replace deferred: holding {_holding_bdays}bd < min {args.min_hold_days_before_replace}bd"
        )
    else:
        action = "REPLACE_CANDIDATE"
        reasons.append("missing from official recommendation set")
```

**주의**: confidence 기반 `REPLACE_CANDIDATE`(line 962~965)는 유예 대상이 아니다 — 신뢰도 미달은 랭크 이탈과 달리 즉각 대응이 맞다.

---

#### KR-G 운영 반영 조건

1. **즉시 적용 가능**: 1~2번 env var 변경 (컨테이너 재기동 필요)
2. **소스 배포 후 적용**: 3~4번 코드 변경
3. **반영 후 관찰 지표**:
   - `trailing_stop_exit` 발생 횟수 감소 확인
   - `max_holding_days_reduce` → `max_holding_days_exit` 패턴 전환 확인
   - 교체 유예 로그(`replace deferred`) 발생 건수 확인
4. **2차 검토 트리거**: 1~4번 적용 후 4주 경과, trailing/holding 패턴 및 보유 기간 분포 변화 확인 후 stop_loss 7% → 8% 전환 여부 결정

**우선순위**: 2순위 (KR-A, KR-B 서버 반영 완료 후 연속 적용)

---

### [KR-F] Financial Momentum Phase 5~8 연계

**현재 상태**: Phase 4까지 완료 (shadow 관찰). Phase 5 이후 서버 진행 예정.

Phase 5~6: 백테스트 검증 후 overlay·BUY gate 결정  
Phase 7: BUY gate 활성화 (`FINANCIAL_BUY_GATE_ENABLED=1`)  
Phase 8: qual_score 재구성 검토

설계 문서: [FINANCIAL_MOMENTUM_DESIGN.md](modules/Lee_trader_ai/FINANCIAL_MOMENTUM_DESIGN.md)

**우선순위**: 3순위 (KR-A, KR-B 완료 후)

---

### [KR-I] 매수 게이트 개선 (2026-05-22 소스 구현 완료)

**배경**: 고정 절대금액 방식의 매수 한도, Market Guard 경로 버그, daily_loss_pct 미수신 처리 불일치, TRIM ratio 임의 fallback 0.5, 진입 confidence 하한 과도 설정 등 매수/매도 게이트 보완점을 점검하고 개선함.

**소스 구현 완료 항목**:

| # | 항목 | 파일 | 상태 |
|---|---|---|---|
| 1 | 매수 한도 비율 방식 추가 (`GLOBAL_MAX_DAILY/WEEKLY_BUY_RATIO`) | `common_live_risk_guard.py` | ✅ 완료 |
| 2 | Market Guard 파일 경로 `_resolve()` 적용 | `common_live_risk_guard.py` | ✅ 완료 |
| 3 | `daily_loss_pct` 미수신 시 매수 차단 flag 추가 (`GLOBAL_BLOCK_BUY_ON_DAILY_LOSS_UNAVAILABLE`) | `common_live_risk_guard.py` | ✅ 완료 |
| 4 | TRIM ratio fallback 0.5 제거 → `trim_weight_unavailable` / `trim_ratio_zero` blocked_reason 처리 | `submit_live_orders.py` | ✅ 완료 |
| 5 | 진입 confidence 하한 완화 (`buy_candidate.min_confidence` 80 → 76) | `config/production_v1.yaml` | ✅ 완료 |

**새로 추가된 env var (서버 `.env` 반영 필요)**:

```env
# 매수 한도 비율 방식 (0.0=비율 미사용, 기존 절대금액 fallback)
GLOBAL_MAX_DAILY_BUY_RATIO=0.0
GLOBAL_MAX_WEEKLY_BUY_RATIO=0.0

# daily_loss_pct 미수신 시 매수 차단 여부 (기본 1=차단)
GLOBAL_BLOCK_BUY_ON_DAILY_LOSS_UNAVAILABLE=1
```

**비율 방식 운영 예시** (총자산 대비 일/주 매수 한도):
```env
GLOBAL_MAX_DAILY_BUY_RATIO=0.05   # 총자산 5% 이내 / 일
GLOBAL_MAX_WEEKLY_BUY_RATIO=0.15  # 총자산 15% 이내 / 주
```
비율 env가 0.0이면 기존 `GLOBAL_MAX_DAILY_BUY_AMOUNT` / `GLOBAL_MAX_WEEKLY_BUY_AMOUNT` 절대금액으로 자동 fallback.

**confidence 변경 영향**:
- `buy_candidate.min_confidence: 80 → 76` : confidence 76~79 종목이 후보 universe에 진입
- `execution_policy.standard_entry_confidence: 76` 이 실제 진입 게이트로 작동 시작
- `execution_policy.extended_entry_confidence: 70` 도 top9~10 구간에서 활성화

**우선순위**: 소스 완료 — 서버 `.env` 반영 후 스냅샷 로그 확인

---

### [KR-H] Feature Importance 추적 (재학습 모니터링)

**파일**: `python/model_train.py` 수정 + `python/model_feature_importance_report.py` (신규)

**목적**: 재학습마다 어떤 피처가 실제로 유효한지 기록하고 추세를 관찰한다. 신규 피처(`flow_*`, `fin_momentum_*`, `short_ratio` 등)가 추가될 때마다 모델이 실제로 이를 활용하는지 검증 근거가 없으면 피처 가감 판단이 주관에 의존하게 된다.

**현재 문제**:
- `model_train.py` 재학습 후 feature importance 출력이 없음
- `flow_*`, `fin_momentum_*`, `short_ratio` 등 새로 추가된 피처가 모델에서 실제로 유의미한 순위에 올랐는지 확인 불가
- 하위권 피처 목록이 없으니 overfitting 위험 제거를 위한 피처 정리도 근거 없이 진행할 수밖에 없음

**작업 내용**:

1. `model_train.py` 재학습 완료 후 importance 자동 저장 (5~10줄 추가)
   - 출력 경로: `data/model_feature_importance/{YYYYMMDD}_{model_name}.csv`
   - 컬럼: `feature`, `importance_gain`, `importance_split`, `model_name`, `run_date`
   - 전체 regressor 6개 + calibrated classifier 1개 각각 저장

2. `model_feature_importance_report.py` 신규 스크립트
   - 이력 파일 읽어 피처별 평균 순위·추세 출력
   - 하위 5~10% 피처 자동 플래그 (제거 후보 목록, 실제 제거는 별도 논의 후 결정)

**재학습 후 확인 포인트**:

| 확인 항목 | 기대 방향 |
|---|---|
| `flow_foreign_net_5d/20d`, `flow_inst_net_5d/20d` | 상위 30위 내 진입 |
| `quality_score`, `ret_60d`, `prob_top20_60d` 기반 피처 | 상위권 안정 유지 |
| `fin_momentum_*`, `short_ratio` 신규 피처 | 순위 확인 후 Phase 5 판단 참고 |
| 하위 5~10% 피처 | 제거 후보 목록으로 플래그 → 논의 후 결정 |

**구현 범위**: `model_train.py`에 importance 저장 블록 추가는 서버 의존 없이 소스 환경에서 구현 가능. 재학습 시 자동 기록.

**우선순위**: 3순위 (KR-E 서버 재학습 이전 소스 구현 완료 권장 — 재학습 첫 실행부터 이력 축적 시작)

---

### KR AI 타임라인

```
즉시 (서버 접속 직후)
├── KR-B: prob_top20_90d 제거, pred_return_30d 추가, 모델 재학습
├── KR-A: AI_MAX_HOLDING_DAYS 재검토 → 30일 조정 여부 결정
├── KR-G-1/2: AI_TRAILING_STOP_MIN_PROFIT=0.08, AI_TRAILING_STOP_PCT=0.10 .env 반영
├── KR-I: GLOBAL_MAX_DAILY_BUY_RATIO, GLOBAL_MAX_WEEKLY_BUY_RATIO, GLOBAL_BLOCK_BUY_ON_DAILY_LOSS_UNAVAILABLE .env 반영

2026-05-28 이후 (Gate BUY_ALLOWED 전환)
├── 포지션 수 제한 해제 확인
├── 투자 비중 정상화 (현재 현금 82.8% → 투자 확대)

즉시 (소스 환경) — 완료
├── KR-D: ai_position_state 하드캡 + 복구 방어 코드 수정 ✓ (2026-05-20)
│   - state_recovered 플래그 + WARNING 로그 추가
│   - AI_MAX_HOLDING_DAYS_HARD_CAP=45 (기본값) 하드캡 추가
├── KR-E: Sample Weight + Isotonic Calibration 소스 구현 ✓ (2026-05-21)
├── KR-G-3: ai_position_risk.py — pnl_pct < 0.02 → 0.0, AI_MAX_HOLDING_DAYS 기본값 15→30 ✓ (2026-05-22)
└── KR-G-4: apply_execution_policy.py — min_hold_days_before_replace 10영업일 파라미터 추가 ✓ (2026-05-22)

소스 환경 (예정)
└── KR-H: model_train.py importance 저장 블록 추가 + model_feature_importance_report.py 신규
    └── KR-E 서버 재학습 전 구현 완료 권장 (재학습 첫 실행부터 이력 축적 시작)

2주 내
└── KR-C: archive_predictions.py 구현

1개월 내
└── KR-E: 서버 재학습 + OOS 검증
```

---

## 3. US AI 자동매매 — 개선 과제

### [US-A] 포트폴리오 백테스트 실행 (최우선)

**파일**: 명세서 완성됨 → [20260519_US 자동매매 전략 백테스트 명세서.md](modules/Lee_trader_us/20260519_US%20자동매매%20전략%20백테스트%20명세서.md)

**왜 최우선인가**: 현재 US AI 전략이 실제로 돈을 버는지 검증된 데이터가 없음.  
월 2% 달성 가능성을 판단하려면 이 결과가 반드시 필요함.

**백테스트 재현 범위** (명세서 기준):
1. ml_v1 랭킹 소스 기반 후보 추출
2. 매수 차단 규칙 (grade/score/financial/benchmark/gap/volatility/cooldown) 재현
3. 정수 수량 + 다음 거래일 종가 체결 가정
4. 청산 규칙 완전 재현 (stop_loss/trailing/60d/rank/benchmark/take_profit)
5. 자산 곡선 집계

**백테스트 해석 조건**:
- 위 결과는 체결 가정이 실제 주문 경로와 동형인지 확인된 경우에만 향후 실전 검토 참고자료로 사용
- 주문 제출 시점, 체결 가격 가정, 미체결 처리 방식이 실운영과 다르면 별도 보정 또는 보수적 해석 필요

**서버 접속 직후 즉시 실행**

---

### [US-B] Paper 안정 운영 30거래일 이상 확보

**현재**: ml_v1 전환 어제 (2026-05-19), WDC 1종목 보유, 극초기 상태.

**관찰 항목**:
- 일별 paper 거래 로그 정상 여부
- sell automation 리포트에서 `TRAILING_STOP: UNKNOWN` 건수 확인 (B-2 이슈)
- `latest_probability` 확인 (WDC → min_prob_hold=0.25 적용 확인)
- paper 포지션 lifecycle 정합성 (체결→보유→청산 순서 오류 없는지)

---

### [US-C] ml_v1 랭킹 성과 검증

**현재 상태**: rule_v1 → ml_v1 전환 어제 (2026-05-19).  
ml_v1이 실제로 rule_v1보다 나은지 아직 검증 안 됨.

**작업 내용**:
- ml_v1 기준 ranking backtest 실행
- rule_v1과 성과 비교 (excess return vs SPY/QQQ, win rate)
- 결과에 따라 ranking source 유지/롤백 결정

---

### [US-D] 매수/보유 확률 기준 불일치 해결

**현재**:
```
매수 진입: min_prob = 0.0  (확률 불필요)
보유 유지: min_prob_hold = 0.25  (현재 운영값)
```

**문제**: ml_v1 확률이 0.25 미만인 종목이 진입 후 즉시 청산 신호 발생 가능.

**대응**: 서버에서 WDC `latest_probability` 확인 후 정책 결정.  
확률값이 안정적으로 공급될 때까지 `min_prob_hold=0.0`으로 일시 완화 검토.

---

### [US-E] KIS 해외 실계좌 / live 준비 상태 보존 (당분간 보류)

체크리스트: [20260520_실계좌 생성 후 작업.md](modules/Lee_trader_us/20260520_실계좌%20생성%20후%20작업.md)

| 단계 | 작업 | 완료 기준 | 현재 해석 |
|---|---|---|---|
| 1 | KIS 해외 계좌 개설 + API 키 발급 | `.env` KIS_US_CANO 입력 | 준비 상태 유지 |
| 2 | KIS Smoke test (AAPL $1 주문→취소) | broker_order_id 정상 반환 | 필요 시 재검증 |
| 3 | Gate 단계적 활성화 | 환경변수 순차 변경 | **보류** |
| 4 | 첫 실거래 (수동, $500 한도) | KIS 앱 주문 내역 대조 | **보류** |
| 5 | 소액 운영 안정화 후 한도 확대 | 1~2주 정합성 확인 | **보류** |

**현재 원칙**

- US는 당분간 paper 전용 유지
- 아래 Step 3~5는 즉시 실행 계획이 아니라, 향후 실전 재검토 시 참고용 절차로만 보존

**Step 3 진입 조건 — 실전 재검토 시에만 적용**

Step 3(Gate 활성화)는 Step 2 Smoke test 결과가 아래 3가지를 모두 충족할 때만 진입한다.  
하나라도 미충족 시 Step 3 진입 금지, 원인 파악 후 재시도.

| 확인 항목 | 기대값 | 판정 |
|---|---|---|
| `submit` 응답에 `broker_order_id` 포함 | 주문 ID 정상 생성 (`AAPL:...`) | ✅ / ❌ |
| `get_order_status` 응답 | 주문 상태 조회 가능 (`OPEN`/`PENDING`/동등 상태 포함) | ✅ / ❌ |
| `cancel_order` 응답 | 취소 요청 성공 후 최종 미체결 종료 상태 확인 가능 | ✅ / ❌ |

> Smoke test 실행 명령어:
> ```bash
> .venv\Scripts\python.exe -c "
> from brokers.us.kis_overseas_client import KisOverseasOrderClient
> c = KisOverseasOrderClient()
> r = c.submit_order({'side': 'BUY', 'symbol': 'AAPL', 'limit_price': 1.0, 'order_qty': 1})
> print('submit:', r)
> if r.get('broker_order_id'):
>     s = c.get_order_status(r['broker_order_id'])
>     print('status:', s)
>     print('cancel:', c.cancel_order(r['broker_order_id']))
> "
> ```

**Smoke test 실행 결과 기록** ← 실행 후 직접 채워넣기:

| 항목 | 실제 반환값 | 판정 |
|---|---|---|
| `broker_order_id` 형식 | 미발급 (주문 실패) | ❌ |
| 주문 상태 조회 반환값 | 미실행 | — |
| 취소 후 최종 상태 반환값 | 미실행 | — |
| 실행 일시 | 2026-05-21 | — |
| 특이사항 | `rt_cd=7`, `msg_cd=APTR0057` — API 인증 정상(token 200), 주문 API 에러. 해외주식 거래 서비스 미신청 또는 장 시간 외 주문 불가 가능성. KIS 앱에서 해외주식 거래 서비스 신청 완료 여부 확인 필요. |

> 이 기록은 이후 운영 판단의 기준선이 된다.  
> KIS API 응답 포맷을 최초 확인하는 시점이므로 빠짐없이 기록할 것.

---

**US Live 전환 환경변수 순서** (참고 보존용):

```env
# Step 3-1: 한도/세부 플래그 먼저 설정
US_LIVE_MAX_ORDER_AMOUNT_USD=500
US_LIVE_MAX_DAILY_ORDER_COUNT=5
US_LIVE_MAX_DAILY_NEW_BUYS=2
US_LIVE_BUY_ENABLED=true
US_LIVE_SELL_ENABLED=true

# Step 3-2: live 거래 플래그 활성화  ← Smoke test 통과 확인 후 적용
US_LIVE_TRADING_ENABLED=true
US_LIVE_ORDER_ENABLED=true

# Step 3-3: 마지막에 실주문 차단 해제  ← 위 두 단계 env 반영 확인 후 적용
US_MICRO_REAL_ORDER_BLOCKED=false
US_MICRO_ALLOW_LIVE=true
```

**Gate 오픈 전 필수 확인** (실전 재검토 시):
- 각 Step 적용 직후 관련 컨테이너/스케줄러 재기동 또는 환경변수 반영 확인
- dry-run 또는 preview 로그에서 주문 한도와 enable 플래그가 기대값으로 로드되는지 확인
- 첫 live 오픈 전 `.env` 스냅샷과 긴급 차단 값(`US_MICRO_REAL_ORDER_BLOCKED=true`, `US_LIVE_TRADING_ENABLED=false`)을 별도 기록

---

### [US-F] Phase 8-7 구현 (SELL Skeleton)

**배경**: Phase 8-6 (SELL 정책 설계) 완료, Phase 8-7 (SELL skeleton 구현) 미착수.

**작업 내용** (Paper position 기준):
- SELL/HOLD/REVIEW_REQUIRED 판정 로직 paper position lifecycle에 연결
- no real SELL order 제약 유지

**우선순위**: 백테스트 + paper 안정화 이후

---

### US AI 타임라인

```
이번 주 (서버 접속 즉시)
├── US-A: 포트폴리오 백테스트 실행
├── US-D: WDC latest_probability 확인 → min_prob_hold 정책 결정
├── US-G: 매수 정책 문장화 / Top N·grade·prob 기준 확정
├── US-H: 매도 정책 설계 확정 (winner run 구조 우선)
└── US-E Step 1~2: KIS 해외 계좌 / smoke test 상태 점검만 유지

2~8주 내
├── US-B: paper 30거래일 누적 + 일별 로그 점검
├── US-C: ml_v1 vs rule_v1 ranking 백테스트 비교
├── US-H: trailing / holding / take_profit 단계 개선 반영 여부 결정
└── US-F: Phase 8-7 구현
```

---

### [US-G] US AI 매수 정책 명세 정리 (2026-05-21 추가)

**목적**: KR에서 정리한 것과 같은 관점으로, US도 `무엇을 사는지`보다 `어떤 종목만 사도록 제한하는지`를 먼저 명확히 한다.  
US는 현재 매수 후보 필터가 비교적 잘 설계되어 있으므로, 당장 뒤엎기보다 **실운영 전 정책 문장과 실제 코드 기준을 정렬**하는 것이 우선이다.

**현재 코드 기준 매수 구조**:
1. ranking source 상위 종목 로드 (`top_n`)
2. grade / score / financial / benchmark strength / gap / intraday / volatility / cooldown / kill switch 다층 차단
3. 기존 포지션 보유, SELL 신호 존재, REVIEW_REQUIRED 존재 시 buy conflict guard로 추가 차단

**현재 장점**:
- KR보다 매수 필터가 더 분리되어 있고, `buy_automation` / `conflict_guard` / `trade_orchestration` 경계가 명확함
- 실운영 전 단계에서 `중복 매수`, `직전 청산 후 재진입`, `SELL 우선` 같은 운영 안전장치가 이미 들어가 있음

**현재 약점**:
- 문서상 `ranking grade는 자동 매수 신호가 아니다`라고 적혀 있지만, 실제 자동매수는 Top N + grade + score를 바로 후보로 사용
- `probability` 필드는 아직 안정적 공급이 부족하여 `US_BUY_MIN_PROB > 0` 사용 시 의도치 않게 대부분 차단될 수 있음
- 실운영 전이라도 `BUY`와 `STRONG_BUY`의 우선순위 차등 정책이 아직 약함

**권장 정책 원칙**:
1. **초기 실운영 전까지는 매수는 더 엄격하게 유지**
   - `Top N`은 5 또는 10 이내 유지
   - `recommend_grade in ('STRONG_BUY', 'BUY')` 유지
   - score 하한, financial data, benchmark strength, data fail-safe 유지
2. **`STRONG_BUY`를 `BUY`보다 우선 배정**
   - 동일 예산/슬롯이면 `STRONG_BUY` 먼저
   - `BUY`는 slot 여유가 있을 때만 진입
3. **probability는 안정화 전까지 보조 지표**
   - `US_BUY_MIN_PROB=0` 유지 또는 매우 보수적 적용
   - `latest_probability` 공급 안정 후에만 매수 필수 조건으로 승격
4. **실운영 오픈 전 섹터 집중 제한을 paper 기준으로 먼저 검증**
   - 단일 종목/단일 섹터 쏠림이 없는지 paper 20~60거래일 로그로 확인

**운영 적용 해석**:
- US 매수는 `좋은 종목을 많이 사는 구조`가 아니라 `좋은 종목만 소수로 제한 진입하는 구조`가 맞다.
- 따라서 KR처럼 매수 기준을 더 풀기보다, **현재의 엄격함을 유지한 채 매도/보유 정책을 개선**하는 것이 우선이다.

**우선순위**: 2순위 (정책 확정은 즉시 가능, 실제 반영은 US-H와 함께 검토)

---

### [US-H] US AI 매도 정책 명세 정리 — KR 노하우 전수 (2026-05-21 추가)

**배경**: 현재 US 매도 로직은 `stop_loss / trailing / max_holding / rank_score_deterioration / benchmark_underperformance / take_profit`가 분리되어 있으나, 실제 결과는 다수가 `FULL_SELL`로 귀결된다.  
이 구조는 손실 통제에는 유리하지만, KR에서 확인한 것처럼 **winner를 오래 보유하지 못하고 조기 청산할 가능성**이 크다.

**현재 코드 기준 핵심 문제**:
1. trailing stop이 `최소 수익 구간` 없이 고정 발동 성격
2. `max_holding_days` 도달 시 수익 여부와 무관하게 `FULL_SELL`
3. `rank/score/prob` 약화가 단계적 감액이 아니라 곧바로 `FULL_SELL`
4. `take_profit`도 partial 기본값이 아니면 전량청산

**권장 정책 원칙**:
1. **loser cut은 유지**
   - `stop_loss`는 즉시 청산 유지
   - `risk_off_market_exit`도 유지
2. **winner run 구조 추가**
   - trailing stop은 `충분한 수익 구간`에서만 발동
   - `max_holding`은 soft / hard 이중 구조로 분리
   - `take_profit`은 partial sell 기본값으로 전환
3. **순위/점수 약화는 단계화**
   - 단순 rank 이탈: REVIEW_REQUIRED 또는 PARTIAL_SELL
   - score/prob 의미 있는 약화: PARTIAL_SELL 우선
   - 구조적 붕괴 또는 손실 심화: FULL_SELL
4. **진입 직후 흔들림 유예**
   - rank 변동만으로 진입 직후 바로 전량청산하지 않도록 5~10영업일 교체/청산 유예 도입 검토
   - 단, stop_loss / risk_off / data error는 유예 없이 즉시 적용

**권장 설계안 A (단순형)**:
- `US_SELL_STOP_LOSS_PCT`: 현행 유지
- trailing:
  - `pnl < 8%` → trailing 미발동
  - `pnl >= 8%` → trailing 발동
  - trailing 폭은 `10%` 기준 유지 또는 소폭 완화 검토
- holding:
  - `soft max holding` 도입 (예: 30~40영업일)
  - `hard cap` 유지 (예: 60영업일)
  - soft max 초과 + 손실권 → FULL_SELL
  - soft max 초과 + 수익권 → HOLD 또는 PARTIAL_SELL
- deterioration:
  - `rank_exit_threshold` 이탈만으로는 즉시 FULL_SELL 금지
  - 1차는 REVIEW_REQUIRED 또는 PARTIAL_SELL
- take profit:
  - `US_SELL_PARTIAL_TAKE_PROFIT_ENABLED=true`
  - 예: +15% 도달 시 30~50% 익절, 잔여분은 trailing/hard cap로 관리

**권장 설계안 B (정교형, 2차 검토)**:
- trailing 구간별 차등
  - `8% <= pnl < 15%` → 고점 대비 -10%
  - `pnl >= 15%` → 고점 대비 -12%
- deterioration도 rank/score/prob 약화 정도에 따라 `PARTIAL_SELL -> FULL_SELL` 2단계화

**우선 적용 권장순서**:
1. trailing에 `최소 수익 구간` 추가
   - **코드 변경 필요**: 현재 `config.py`에 `trailing_stop_min_profit` 필드 없음 → `SellAutomationConfig`에 필드 추가 + `sell_rule_engine.py` 발동 조건 수정
   - 신규 env var: `US_SELL_TRAILING_MIN_PROFIT_PCT` (권장 기본값 `0.08`)
2. `max_holding_days` soft / hard 분리
   - **코드 변경 필요**: 현재 hard 60d만 존재 → `soft_max_holding_days` 필드 추가 + `sell_rule_engine.py` 에 soft/hard 이중 체크 로직 추가
   - 신규 env var: `US_SELL_SOFT_MAX_HOLDING_DAYS` (권장 기본값 `35`)
   - soft 초과 + 수익권 → HOLD, soft 초과 + 손실권 → FULL_SELL
3. `take_profit` partial sell 기본 활성화
   - **env var만 변경**: `US_SELL_PARTIAL_TAKE_PROFIT_ENABLED=true` (코드 이미 존재)
4. `rank/score/prob deterioration -> FULL_SELL` 완화
   - **코드 변경 필요**: `sell_rule_engine.py`에서 rank 이탈 즉시 FULL_SELL 분기를 REVIEW_REQUIRED/PARTIAL_SELL 분기로 전환
5. 진입 후 5~10영업일 유예 도입 검토
   - **코드 변경 필요**: KR-G-4와 동일 패턴, entry_date 기준 영업일 계산 + grace period 체크

**운영 해석**:
- US는 KR보다 매수 필터는 잘 잡혀 있으므로, **실운영 전 핵심 개선 포인트는 buy가 아니라 hold/sell lifecycle**이다.
- 목표는 `매수는 엄격`, `매도는 단계적`, `winner는 오래 보유`, `loser는 즉시 정리` 구조로 맞추는 것이다.

**우선순위**: 2순위 (US paper 정책 확정 필요, live 전환은 별도 보류)

#### US-H 보완 명세 (구현 후 재정리 포인트, 2026-05-21 추가)

US-H 1차 구현 방향은 타당하나, 아래 3개는 정책 완성도를 위해 추가 정리가 필요하다.

1. **`PARTIAL_SELL` 비율 분리**
   - 현재는 `TAKE_PROFIT`와 `RANK_SCORE_DETERIORATION`이 모두 동일 `partial_take_profit_ratio`를 사용한다.
   - 권장:
     - `US_SELL_PARTIAL_TAKE_PROFIT_RATIO`
     - `US_SELL_PARTIAL_DETERIORATION_RATIO`
   - 의도:
     - 익절은 보수적으로 30% 내외
     - 약화 신호 감액은 50% 내외
     - 서로 다른 성격의 partial sell을 분리 운영

2. **`partial take profit` 기본 정책 정렬**
   - 문서 방향이 `부분 익절 후 잔여 보유`라면, `US_SELL_PARTIAL_TAKE_PROFIT_ENABLED`는 기본 ON 정책으로 맞추는 것이 일관된다.
   - 반대로 기본 OFF를 유지할 경우, 문서도 “옵션 기능”으로 표현을 낮춰 코드와 맞춰야 한다.
   - 권장:
     - paper 단계에서는 ON으로 검증
     - paper 검증 종료 후 review에서 최종 기본값 확정

3. **`entry grace` 적용 범위 축소**
   - 현재 grace는 `rank`, `score`, `probability` 약화까지 넓게 유예할 수 있으므로, 유예 범위를 더 좁게 정의하는 것이 바람직하다.
   - 권장:
     - `RANK_EXIT_THRESHOLD` 기반 약화만 5~10영업일 유예
     - `SCORE_BELOW_HOLD`, `PROBABILITY_BELOW_HOLD`는 즉시 `PARTIAL_SELL` 또는 `REVIEW_REQUIRED`
   - 해석:
     - rank 하락은 초기 noise 가능성이 큼
     - score/prob 약화는 구조적 약화 가능성이 높아 무조건 유예하면 안 됨

**권장 운영 기본값 예시**:
```env
US_SELL_TRAILING_MIN_PROFIT_PCT=0.08
US_SELL_TRAILING_STOP_PCT=0.10
US_SELL_SOFT_MAX_HOLDING_DAYS=35
US_SELL_MAX_HOLDING_DAYS=60
US_SELL_ENTRY_GRACE_DAYS=7
US_SELL_PARTIAL_TAKE_PROFIT_ENABLED=true
US_SELL_PARTIAL_TAKE_PROFIT_RATIO=0.30
US_SELL_PARTIAL_DETERIORATION_RATIO=0.50
```

**정리**:
- `winner run` 방향 자체는 유지
- 수정 포인트는 “되돌리기”가 아니라 “partial sell 세분화”와 “grace 범위 정리”다.

---

## 4. KR Rule 중단 처리

### 즉시 결정 (서버 접속 후)

**Step 1 — 신규 매수 차단**:
```env
RULE_LIVE_ENABLED=0
```

**Step 2 — 포지션 처리**:
- 보유 종목: 보유자 직접 청산 처리

**Step 3 — 완전 중단 확인 후**:
```env
RULE_TRADING_RUN_MODE=paper
```

**Step 4 — 적용 검증**:
- 관련 scheduler / before-open / after-open 경로 재시작 또는 재배포
- preview 또는 수동 실행 로그에서 KR Rule 주문 제출 경로가 비활성으로 표시되는지 확인
- 다음 실행 주기에서 신규 실주문 요청이 생성되지 않았는지 확인

### KR Rule 재가동 조건 (미래 검토)

아래 3가지 완료 후 재가동 여부 결정:
1. 백테스트 실거래 동형 재구축 (rule_score_v3 기준 통일)
2. BUY 버퍼 0.25%/0.5% 유효성 검증
3. entry_date / highest_price 포지션 메타 안정적 수집 확인

---

## 5. 월 2% 달성 체크리스트

### 달성에 필요한 조건 (시스템 기준)

#### KR AI 측

- [ ] Gate BUY_ALLOWED 전환 (2026-05-28 예상) — 포지션 수 확대
- [ ] 모델 예측 기간(60d) ↔ 보유 기간(15d) 불일치 해소 [KR-A]
- [ ] prob_top20_90d 제거 + pred_return_30d 추가 + 모델 재학습 [KR-B]
- [ ] max_holding_days 하드캡 추가 (무한 보유 방지) [KR-D]
- [ ] Financial Momentum Phase 5~6 백테스트 검증 후 overlay 반영 [KR-F]
- [ ] 실거래 3개월 이상 → 월 1.0~1.5% 지속 여부 확인

#### US AI 측 (paper 전용 단계 — 실전 기여 목표 없음)

> US는 당분간 paper 전용 유지. 아래 항목은 "실전 전환 조건"이 아니라 "paper 검증 완성도" 기준이다.

- [x] 포트폴리오 백테스트 결과 양(+) 확인 [US-A] ✅ 완료
- [x] ml_v1 랭킹 rule_v1 대비 우위 확인 [US-C] ✅ 완료
- [ ] Paper 30거래일 이상 안정 운영 [US-B] — paper 검증 지속
- [ ] US 매수 정책 기준 확정 [US-G] — paper 환경에서 검증
- [ ] US 매도 정책 기준 확정 [US-H] — paper 환경에서 검증
- [ ] 현재 운영 로직 그대로의 lifecycle / 정합성 검증 확보
- [ ] US-E (실계좌 / live 전환) — **장기 보류**. 아래 4가지 모두 충족 후 재검토:
  - KR AI 월 1% 이상 3개월 안정 실거래
  - Paper 30거래일 lifecycle 정합성 확인
  - KIS smoke test 통과
  - 별도 의사결정 승인

#### 종합 판단 시점

```
2026-07~08
├── KR AI: 3개월 실거래 성과 데이터 → 월 1.0~1.5% 지속 여부 판단
├── US AI: paper 30거래일 + 정책 정합성 확인 → 실전 전환 조건 재검토
└── US 실전 전환 여부 결정 후에야 합산 월 2% 목표 재산출 가능
```

### Go / No-Go 기준

- KR AI 운영 변경은 OOS 또는 paper 기준으로 기존 대비 alpha 저하 없이 MDD 악화가 통제될 때만 live 반영
- US AI는 당분간 paper 전용 유지
- US AI 실전 전환 검토는 별도 의사결정으로 분리하고, 현재 문서 범위에서는 목표에 포함하지 않음
- 월 2% 목표 재평가는 KR/US 모두 최소 표본 기간 충족 후 수행하고, 미충족 시 목표 달성 판단 보류
- 아래 중 하나라도 발생하면 증액/확대 중단:
  - 백테스트와 운영 가정 구조 불일치 확인
  - 주문 게이트/한도 반영 실패
  - paper 또는 실거래 검증 과정에서 허용 불가능한 운영 오류 또는 예상 밖 손실 발생

---

## 6. 리스크 관리 원칙

### 계좌 방어 우선순위

```
1. 손실 제한 > 2. 수익 극대화
   ↓
stop_loss / trailing_stop 룰 엄수
EXIT_REVIEW 종목 빠른 수동 청산 검토
물타기 없음
```

### 레짐별 운영 원칙

| 레짐 | KR AI | US AI |
|---|---|---|
| Bull | 정상 운영, 포지션 확대 가능 | 정상 운영 |
| Neutral (현재) | 선별적 진입, 보수적 비중 | 보수적 매수 |
| Defensive | 신규 매수 자제, 현금 확보 | BUY 제한 |

### 현금 비중 원칙

- **전체 자금 50% 이상 현금 유지**
- KR AI: Gate 전환 후 자동 투자 비중 상향
- US AI: 당분간 paper 전용 유지, 실자금 배정 없음

### 자금 배분 원칙

- KR AI 중심으로 실투입 비중을 관리
- US AI는 당분간 paper 전용으로 유지하고 실투입 자금 배정 대상에서 제외
- KR AI Gate 전환 후에도 단일 전략 증액은 최근 1개월 운영 오류 없음, 손실 한도 준수, 현금 비중 50% 유지 조건 하에서만 진행
- 목표 수익률 미달을 이유로 즉시 원금 증액하지 않고, 먼저 전략 성과와 운영 정합성을 재검토

---

## 6-1. 월 2% 달성을 위한 전략 정리

### 현재 판단

- 현재 KR/US 자동매매에는 `월 2%를 안정적으로 만드는 검증된 필승전략`이 이미 완성되어 있다고 보기 어렵다.
- KR은 종목 선정 엔진은 준수하나, 보유 기간과 청산 로직이 예측 기간과 정합되지 않아 알파가 누수되고 있다.
- US는 리스크 차단 규칙은 갖춰졌지만, 운영 로직 그대로의 실전형 백테스트와 충분한 paper 검증이 아직 부족하다.
- 따라서 지금 단계의 핵심 목표는 `고수익 약속`이 아니라 `알파가 나는 구조를 검증하고 복리형 운영 구조로 정렬하는 것`이다.

### KR AI 판단

- 강점:
  - `ret / prob / tech / qual / flow - risk_penalty` 구조의 다축 점수 체계가 이미 운영에 반영되어 있음
  - 단일 팩터 추종보다 안정적인 종목 선별 기반을 갖추고 있음
- 약점:
  - `pred_return_60d / 90d` 중심 예측과 `AI_MAX_HOLDING_DAYS=15`가 구조적으로 충돌
  - 상태 복구 취약점으로 trailing / holding rule이 무력화될 수 있음
  - 익절보다 조기 정리 쪽으로 기울어 있어 강한 종목을 오래 보유하는 구조가 약함
- 결론:
  - KR은 `종목 선정 엔진`보다 `보유/청산 엔진` 개선이 더 시급하다.

### US AI 판단

- 강점:
  - 매수 차단 규칙, 쿨다운, kill switch, benchmark strength, volatility 차단 등 방어 로직이 잘 갖춰져 있음
  - ML ranking 기반으로 확률·수익예측을 함께 활용할 수 있는 구조가 있음
- 약점:
  - 아직 paper 단계라 실제 자본 투입 구조의 성과 검증이 없음
  - 매수 시 확률 기준과 보유 시 확률 기준이 완전히 정합되지 않음
  - 현재 운영 로직을 그대로 재현한 net 백테스트 결과가 아직 없음
- 결론:
  - US는 `리스크 제어형 paper 파일럿` 단계이며, 아직 실전 자금 확대 대상이 아니다.

### 개인 ETF형 운영 원칙

- 목표는 단기 고배율 승부가 아니라 `분산 + 교체 + 손실 제한 + 재투자` 구조를 만드는 것이다.
- 소수 종목 집중 베팅보다, 검증된 상위 후보를 일정 규칙으로 반복 편입/교체하는 방식이 더 적합하다.
- 월 2%는 개별 종목 대박보다 `작은 우위의 반복`으로 접근해야 한다.
- 따라서 전략 운영 원칙은 아래와 같이 정리한다.

1. 종목 선정보다 운영 정합성을 우선한다.
2. 수익률 목표보다 MDD와 운영 오류를 먼저 통제한다.
3. 강한 종목을 너무 빨리 자르지 않는다.
4. 실거래 반영 전에는 반드시 paper / backtest / 비용 반영 검증을 거친다.
5. 원금 증액은 성과 검증 이후에만 한다.

### 실무 우선순위

1. KR: `pred_return_30d` 학습 + `AI_MAX_HOLDING_DAYS` 30일 정합 + hard cap 추가
2. KR: `ai_position_state` 복구 취약점 보완
3. US: 현재 운영 로직 그대로의 `gross / net` 백테스트 완료
4. US: paper 30거래일 이상 누적 후 buy/sell lifecycle 정합성 검증
5. KR/US 공통: 개인 ETF형 포지션 운영 규칙 확정
   - 종목 수
   - 종목당 비중
   - 교체 주기
   - 손실 제한
   - 현금 비중

### 최종 방향

- KR은 `좋은 종목을 더 오래 보유하도록` 바꾸는 것이 핵심이다.
- US는 `현재 전략이 실제로 돈을 버는지`를 paper / backtest로 먼저 증명하는 것이 핵심이다.
- 두 시스템 모두 검증이 끝난 뒤에야 비중 확대와 월 2% 재평가가 가능하다.
- 현 단계에서는 `필승전략 추구`보다 `검증된 복리 운영 시스템 구축`이 더 현실적이고 중요하다.

---

## 7. 전체 작업 순서 요약

```
소스 환경 (지금 즉시)
├── KR-D: ai_position_state 복구 방어 + max_holding 하드캡 코드 수정

서버 접속 직후 (이번 주)
├── KR Rule 중단: RULE_LIVE_ENABLED=0
│   └── 스케줄러 재기동 + 주문 경로 비활성 로그 확인
├── KR-B: prob_top20_90d 제거 + pred_return_30d 추가 + 모델 재학습
│   └── OOS / paper 비교 후 반영 여부 결정
├── KR-A: AI_MAX_HOLDING_DAYS 30일 조정 검토
│   └── 백테스트 / paper 승인 후에만 운영 반영
├── US-A: 포트폴리오 백테스트 실행
│   └── 체결 가정과 실운영 동형성 함께 점검
├── US-D: WDC latest_probability 확인
└── US-E: KIS 해외 계좌 / smoke test 상태 점검
    └── 실전 전환 계획은 문서 범위에서 보류

2~4주 내
├── KR-C: archive_predictions.py 구현
├── US-B: paper 30거래일 누적 관찰
└── US-C: ml_v1 vs rule_v1 비교

4~8주 내
├── KR-E: Calibration + Sample Weight (수급 피처 안정화 후)
├── KR-F: Financial Momentum Phase 5~6 백테스트
└── US-F: Phase 8-7 구현

2026-07~08
└── KR 실거래 + US paper/backtest 성과 재평가 → 월 2% 달성 가능성 재판단
    (미달 시: 모델 개선 or 운영 구조 재검토)
```

---

## 8. 참고 파일

| 파일 | 내용 |
|---|---|
| [python/ai_position_risk.py](../python/ai_position_risk.py) | KR AI 청산 로직 |
| [python/model_train.py](../python/model_train.py) | KR AI 모델 학습 |
| [python/us/sell_automation/sell_rule_engine.py](../python/us/sell_automation/sell_rule_engine.py) | US 청산 규칙 엔진 |
| [python/us/sell_automation/config.py](../python/us/sell_automation/config.py) | US 청산 환경변수 |
| [python/us/buy_automation/decision_engine.py](../python/us/buy_automation/decision_engine.py) | US 매수 의사결정 |
| [doc/explain/20260518 학습 및 예측 전략 논의.md](explain/20260518%20학습%20및%20예측%20전략%20논의.md) | KR AI 모델 개선 논의 |
| [doc/modules/Lee_trader_us/20260519_US 자동매매 전략 백테스트 명세서.md](modules/Lee_trader_us/20260519_US%20자동매매%20전략%20백테스트%20명세서.md) | US 포트폴리오 백테스트 명세 |
| [doc/modules/Lee_trader_us/20260520_실계좌 생성 후 작업.md](modules/Lee_trader_us/20260520_실계좌%20생성%20후%20작업.md) | US 실계좌 / smoke test 참고 체크리스트 (현재 보류) |
| [doc/modules/Lee_trader_ai/FINANCIAL_MOMENTUM_DESIGN.md](modules/Lee_trader_ai/FINANCIAL_MOMENTUM_DESIGN.md) | 재무 모멘텀 설계 |
| [doc/modules/Lee_trader_rule/20260519_로직진단.md](modules/Lee_trader_rule/20260519_로직진단.md) | KR Rule 진단 (참고) |

---

## 9. 2026-05-20 기준 진행 현황 점검

아래 상태는 현재 워크스페이스의 코드, `.env`, 출력 산출물 기준으로 점검한 결과다.

### 완료

- [x] KR Rule 중단: `.env`에 `RULE_LIVE_ENABLED=0` 반영
- [x] KR-D: `ai_position_state` 복구 방어 로직 반영
- [x] KR-D: `AI_MAX_HOLDING_DAYS_HARD_CAP` 하드캡 로직 추가
- [x] KR-B: `pred_return_30d` 학습 타겟 추가
- [x] KR-C: `archive_predictions.py` 구현
- [x] KR-C: `evaluate_prediction_accuracy.py` 구현
- [x] KR-A: `.env`에 `AI_MAX_HOLDING_DAYS=30` 및 `AI_MAX_HOLDING_DAYS_HARD_CAP=45` 명시 반영
- [x] US-A: 포트폴리오 백테스트 실행
- [x] US-A: 백테스트 결과 양(+) 확인
- [x] US-C: `rule_v1 -> ml_v1` 운영 소스 전환
- [x] US-C: `ml_v1 vs rule_v1` 동일 조건 포트폴리오 백테스트 비교 완료
- [x] US-D: `latest_probability` 공급 확인 및 초기 정책 조정
- [x] US-F: SELL skeleton 수준의 자동 판단/청산 흐름 동작 확인
- [x] US paper orchestration / integrated report 생성 확인
- [x] KR-E: `model_train.py` Sample Weight + Isotonic Calibration 소스 구현 완료 (2026-05-21)
  - `compute_sample_weights()`: 지수 감쇠 halflife 3년 기본, 평균=1 정규화
  - `CalibratedClassifierCV(method="isotonic")`: 마지막 fold 검증 데이터 기반 후보정
  - `--sample-weight-halflife`(기본 3.0) / `--no-calibrate` CLI 인자 추가
  - 서버 재학습 시 `python model_train.py`로 자동 적용, 기존 동작: `--sample-weight-halflife 0 --no-calibrate`

### 서버 검증 필요

- [ ] KR-A: 30일 horizon 정합성 검증 진행중
  - `pred_return_30d` 추가와 `.env`의 `AI_MAX_HOLDING_DAYS=30` 반영까지는 완료
  - 다만 전용 backtest / paper 재현 근거는 아직 별도 축적 필요
- [ ] KR-B: `prob_top20_90d` 제거는 부분 완료
  - 운영 점수에서는 제외됐으나 컬럼/연구용 보조지표로는 유지 중
- [ ] US-B: paper 운영은 시작됐으나 30거래일 누적 전
- [ ] US-E: 해외 계좌 정보는 `.env`에 반영됐으나 smoke test 재검증 및 live 전환은 현재 보류

### 서버 반영 대기 / 추가 구현 필요

- [ ] KR AI `BUY_ALLOWED` 전환
- [ ] KR-C: accuracy 평가 산출물 실제 생성 및 누적 확인 (최소 60일 경과 후 의미 있는 리포트 생성)
- [ ] KR-E: 서버에서 `model_train.py` 재학습 실행 → OOS 수익률·MDD 기존 대비 열화 없음 확인
- [ ] KR-H: `model_train.py` feature importance 저장 블록 추가 + `model_feature_importance_report.py` 신규 구현 (소스 환경, KR-E 재학습 전 완료 권장)
- [ ] KR-F: Financial Momentum Phase 5~6 검증 및 운영 반영
- [ ] KR-G: 매도 정책 개선 운영 반영 (2026-05-21 소스 구현 완료, 서버 배포 대기)
  - ✅ 코드: `ai_position_risk.py` — pnl 조건 0.02→0.0, AI_MAX_HOLDING_DAYS 기본값 15→30
  - ✅ 코드: `apply_execution_policy.py` — `min_hold_days_before_replace=10` 파라미터 + 유예 로직
  - ⏸ env var: `AI_TRAILING_STOP_MIN_PROFIT=0.08`, `AI_TRAILING_STOP_PCT=0.10` → 서버 `.env` 반영 필요
- [ ] KR-I: 매수 게이트 개선 (2026-05-22 소스 구현 완료, 서버 배포 대기)
  - ✅ 코드: `common_live_risk_guard.py` — 비율 방식 매수 한도, Market Guard 경로 fix, daily_loss_pct flag
  - ✅ 코드: `submit_live_orders.py` — TRIM ratio fallback 0.5 제거
  - ✅ config: `production_v1.yaml` — `buy_candidate.min_confidence` 80 → 76
  - ⏸ env var: `GLOBAL_MAX_DAILY_BUY_RATIO`, `GLOBAL_MAX_WEEKLY_BUY_RATIO`, `GLOBAL_BLOCK_BUY_ON_DAILY_LOSS_UNAVAILABLE` → 서버 `.env` 반영 필요
- [ ] US-B: paper 30거래일 이상 안정 운영 확보 (paper 검증 지속)
- [ ] US-G: 매수 정책 기준 확정 (paper 환경 기준)
- [ ] US-H: 매도 정책 기준 확정 (paper 환경 기준)
- [ ] US-E: **장기 보류** — KR 안정화 + 승격 조건 4가지 충족 전까지 실전 전환 없음
  - smoke test 재시도도 KR 안정화 이후로 우선순위 후순위

### 장기 보류

- [ ] US 실전 전환
- [ ] US smoke test 재시도 후 live gate 검토
- [ ] KR Rule 재가동 검토

### 현재 판단 (2026-05-22 방향 확정 반영)

- **KR**: 핵심 보완 코드(`보유/청산`, `state 복구`, `30d horizon`) 상당 부분 완료. BUY_ALLOWED 전환이 최우선 과제.
- **US**: ml_v1 전환, 백테스트, paper orchestration까지는 완료. **당분간 paper 전용 유지**. 실전 전환은 KR 안정화 후 별도 결정.
- **운영 계층 기준**: [20260522_시스템운영방얀.md](20260522_시스템운영방얀.md) 참조.
- **요약**: `KR 집중 → US 조건부 실전` 순서로 진행.

### 검토 결과 / 발견된 문제점

- KR-C는 스크립트 구현 자체는 완료됐지만, 현재 `labels.csv` 최신 기준일이 `2026-04-06`이고 `predictions_archive`는 `2026-05-18~2026-05-20`이라 아직 병합 평가가 되지 않는다.
  - 즉 `evaluate_prediction_accuracy.py`는 오늘 실행 가능하지만, 현 시점에서는 `병합 0행`이 정상 결과다.
  - 실제 accuracy 리포트는 최소 30일/60일 maturity가 지난 예측 아카이브가 쌓여야 의미 있게 생성된다.
- US-C는 동일 기간 포트폴리오 백테스트 비교 결과 `ml_v1`가 `rule_v1`보다 우위다.
  - 비교 기간: `2023-01-03 ~ 2026-05-18`
  - `ml_v1`: 누적 `+159.32%`, CAGR `32.67%`, MDD `-2.46%`, 승률 `75.8%`
  - `rule_v1`: 누적 `+12.38%`, CAGR `3.52%`, MDD `-4.52%`, 승률 `41.4%`
- US-E는 계좌 정보(`KIS_US_APP_KEY`, `KIS_US_APP_SECRET`, `KIS_US_CANO`, `KIS_US_ACNT_PRDT_CD`)는 `.env`에 반영됐지만, 현재는 실전 전환이 아니라 준비 상태 보존 목적이다.
  - `US_LIVE_TRADING_ENABLED=0`, `US_LIVE_ORDER_ENABLED=0` 상태 유지 중
  - 따라서 현재는 계좌 준비 상태 유지이지, 실거래 검증 완료 상태는 아니다.
- US portfolio backtest 리포트의 `strategy.source` 메타데이터가 고정값으로 기록되던 버그를 수정했다.
  - 비교 숫자 자체에는 영향 없었고, 결과 파일의 source 표기 정확도만 보정했다.
- 운영 배치 안정성 문제 확인:
  - `auto_ops_scheduler_status.json` 기준 `2026-05-20 18:10` close 배치가 실패했다.
  - 실패 단계는 `run_manual_close_batch.py -> sync_web_display_data.py` 이며, 다음 성공 전까지 스케줄러 상태가 `error`로 남아 있다.
  - 따라서 모델/전략 검토와 별도로 `close batch 정상화`를 운영 우선순위에 포함해야 한다.

### 2026-05-21 할 일 처리 결과

| # | 항목 | 결과 |
|---|---|---|
| 1 | close batch 실패 원인 확인 및 `sync_web_display_data.py` 정상화 | ◑ 소스 수정 + `sync_web_display_data.py` 단독 재실행 성공, `run_manual_close_batch.py --local` 전체 재완주는 아직 미검증 |
| 2 | US-E: KIS 해외 실계좌 smoke test 실행 | ❌ 실패 (`rt_cd=7, APTR0057`) — KIS 앱 해외주식 서비스 신청 여부 확인 필요, 다만 현재는 실전 전환 보류 |
| 3 | US-B: paper 운영 로그 / lifecycle / review_required 점검 | ⏸ 서버 접속 필요, 미실행 |
| 4 | KR-C: accuracy 평가 가능 시점 및 선행 조건 명시 | ✅ 확인 완료 (최소 60일 경과 후, 현재는 병합 0행이 정상) |
| 5 | KR-A: 30일 검증 경로 정의 | ✅ 소스 구현 완료 (KR-E 포함), 서버에서 재학습 후 OOS 비교가 다음 단계 |

### 소스 환경 완료 항목 (2026-05-21)

- KR-E 코드 구현 완료 (아래 추가 조치 참조)
- `model_train.py` 중복 import 제거
- KR-G-3: `ai_position_risk.py` 수정 완료
  - `AI_MAX_HOLDING_DAYS` 소스 기본값 15 → 30
  - max_holding soft exit 조건 `pnl < 2%` → `pnl < 0%`, reduce 로직 제거 (0% 이상은 hard_cap까지 HOLD)
- KR-G-4: `apply_execution_policy.py` 수정 완료
  - `--min-hold-days-before-replace` CLI arg 추가 (기본 10영업일)
  - per-code 루프에 `_holding_bdays` 계산 블록 추가
  - 랭크 이탈 / 추천 미포함 REPLACE_CANDIDATE 2개소 유예 체크 삽입
- KR-B: `model_predict.py` `CLS_TARGET_PROB_COL`에서 `target_90d_top20` 매핑 제거 (재학습 후 stale 예측 방지)
- US-G: `buy_automation/decision_engine.py` 수정 완료
  - STRONG_BUY → BUY 순 우선 배정: 동일 슬롯 경합 시 grade 높은 종목 먼저 선택
  - 기존 rank 정렬은 동일 grade 내 2차 기준으로 유지
- US-H: `sell_automation/config.py` + `sell_rule_engine.py` 수정 완료
  - trailing stop: `US_SELL_TRAILING_MIN_PROFIT_PCT=0.08` 기본, pnl < 8% 시 trailing 미발동
  - max_holding: soft(35d)/hard(60d) 이중 구조 — soft 초과 + 손실권만 FULL_SELL
  - deterioration: `entry_grace_days=7` 진입 유예, 유예 후 FULL_SELL → PARTIAL_SELL 단계화
  - 신규 env var: `US_SELL_TRAILING_MIN_PROFIT_PCT`, `US_SELL_SOFT_MAX_HOLDING_DAYS`, `US_SELL_ENTRY_GRACE_DAYS`

### 바로 끝내기 어려운 항목 (서버 의존)

- KR AI `BUY_ALLOWED` 전환 ← **최우선**
- KR-E 서버 재학습 및 OOS 검증
- KR-F Financial Momentum Phase 5~6 검증 완료
- 웹 `ranking.html` 종가배치 후 상태 초기화 원인 확정 및 서버 로그/DB 기준 검증
- US paper 30거래일 이상 확보 (paper 검증 지속)
- US paper 정책/라이프사이클 검증 완료 (paper 검증 지속)
- US-E smoke test 재시도 — **보류** (KR 안정화 이후로 우선순위 후순위)

### 2026-05-21 추가 조치

- `sync_web_display_data.py` 1차 방어 수정 반영
  - `daily_ranking` parity 검증을 전체 누적 row 수 비교가 아니라 최신 일자 날짜 및 최신 일자 row 수 비교 중심으로 보정했다.
  - US web 지원 테이블 bootstrap은 대표 테이블이 이미 존재하면 `postgres/us_trade_tables.sql`, `postgres/us_stock_tables.sql` 전체 재실행을 건너뛰도록 변경했다.
- `sync_web_display_data.py` 2026-05-21 수동 재실행 성공
  - `LOCAL_DATABASE_URL -> WEB_DATABASE_URL` 경로로 전체 동기화를 실제 실행했고 `Web display sync completed`까지 확인했다.
  - 이전 close batch 실패 원인이던 `daily_ranking parity mismatch`, `US DDL statement timeout`는 재현되지 않았다.
  - 실행 로그 기준 주요 병목은 `recommend.us_stock_rank_daily` 43,116행 적재와 `public.fact_price_daily` 20,300행 동기화였다.
  - 특히 US ranking은 2,000행 배치 22회로 나눠 적재했고, 이 구간만 약 11분 소요됐다.
- 기대 효과
  - `daily_ranking` 누적 이력 때문에 발생하던 false mismatch를 줄인다.
  - 기존 US DDL 재실행으로 인한 `statement timeout` 가능성을 낮춘다.
- 추가 관찰사항
  - 현재 `sync_web_display_data.py` 전체 수동 실행시간은 대략 16~17분 수준으로 길다.
  - 따라서 운영상으로는 “실패”와 “장시간 실행”을 구분해서 봐야 하고, US ranking / fact_price_daily 구간은 정상이어도 오래 걸릴 수 있다.
- 남은 리스크
  - `sync_web_display_data.py` 단독 재실행은 성공했지만 `run_manual_close_batch.py --local` 전체 재완주까지는 아직 별도 검증하지 않았다.
  - 실행시간이 길기 때문에 스케줄러/운영 관점에서는 timeout, 중복 실행 방지, 배치 시간창 확보를 추가로 점검해야 한다.

### 랭킹분석 화면 초기화 이슈 (2026-05-21 추가 확인)

- 증상
  - 웹 `ranking.html`은 종가배치 이후 `신규 진입` 항목이 대거 `NEW`로 보이고, `급부상/급하락`이 비는 식으로 이전 비교 상태가 초기화된 것처럼 보인다.
- 소스 기준 확인 결과
  - 프론트 `ranking-trend`는 `daily_ranking`의 최근 다일자 이력을 전제로 `5영업일 전 순위`를 비교한다.
  - `daily_ranking` 이력이 부족하면 비교 기준일(`compare_5d`)이 비거나 종목별 과거 순위(`r5`)가 `null`이 되어 `is_new=True`로 처리된다.
  - KR 원본 `daily_ranking`은 생성 시 누적 append가 아니라 최신 스냅샷 교체 구조이며, 웹 동기화는 그 후 `history/ranking/*_ranking_final.csv` 아카이브를 읽어 웹 DB 이력을 복구하는 구조다.
  - 기존 `sync_web_display_data.py` fallback은 archive 미가용 시 source DB `public.daily_ranking`을 최근 날짜 기준으로 다시 읽도록 되어 있었으나, 이 테이블 자체가 최신 스냅샷 교체 구조라 history source로는 부적합하다.
  - 즉 archive가 없을 때는 fallback이 "과거 5영업일 이력 복구"를 못 하고, 결과적으로 웹 DB `daily_ranking` 날짜 수가 부족해질 수 있다.
- 구조 원인 확인 결과 (2026-05-22 소스 검토)
  - `ranking-trend` API는 `daily_ranking` 최근 10개 날짜 중 5번째 이전 날짜(`compare_5d`)를 기준으로 비교한다.
  - 비교일 row가 없으면 `rank_5d=null`, `is_new=true`가 되어 `신규 진입 NEW`가 늘고 `급부상/급하락`은 비게 된다.
  - 원인 후보는 단순 프론트 문제가 아니라, **웹 DB `daily_ranking` 이력 자체가 줄어드는 동기화 경로** 쪽이다.
- 소스 수정 완료 (서버 반영 필요)
  - `python/sync_web_display_data.py` fallback 순서를 아래처럼 보강했다.
  - 1차: `data/history/ranking/*_ranking_final.csv` archive 사용
  - 2차: archive가 없으면 `research.ranking_history`에서 최근 날짜별 최신 `run_id` 기준 이력 복구
  - 3차: 위 둘 다 없을 때만 마지막 fallback으로 source DB `public.daily_ranking` 사용
  - 기대 효과: archive 미가용 상황에서도 `research.ranking_history`가 있으면 웹 `daily_ranking` 최근 5~10영업일 이력을 유지할 수 있다.
- 현재 판단
  - 로컬 화면이 정상이라면 원본 데이터 자체보다 `웹 DB insert / history 복구 / fallback sync` 경로를 우선 점검하는 것이 맞다.
  - 이번 이슈의 핵심은 "archive가 없을 수 있음"보다 더 좁게, **archive 부재 시 기존 fallback이 history 복구를 못 하는 구조**였다는 점이다.
- 서버에서 우선 확인할 항목
  1. `data/history/ranking/*_ranking_final.csv`가 최근 5~10영업일 이상 실제로 존재하는지
  2. `sync_web_display_data.py` 실행 로그에서 아래 중 어떤 경로를 탔는지 확인
     - `Loaded KR ranking archives ...`
     - `Loaded KR ranking history from research.ranking_history ...`
     - `KR ranking archive/history unavailable; fallback to source DB daily_ranking sync`
  3. 웹 DB `daily_ranking`의 `DISTINCT date`가 최근 며칠치 유지되는지
  4. close batch 직후 `ranking.html` 증상 재현 시점과 웹 DB 날짜 이력 감소 시점이 일치하는지
- 결론 / 작업 원칙
  - 소스 수정은 완료됐고, 이제 **서버 환경에서 어떤 fallback 경로가 실제로 탔는지와 웹 DB 날짜 이력이 복구되는지**를 확인하는 단계다.
  - 서버 반영 후에도 `research.ranking_history` 자체가 비어 있거나 최근 날짜가 부족하면 같은 증상이 날 수 있으므로, archive와 ranking_history 둘 다 확인해야 한다.
  - 서버 반영 확인 기준:
    1. `sync_web_display_data.py` 재실행 성공
    2. 웹 DB `daily_ranking` 최근 5영업일 이상 존재
    3. `ranking.html`에서 `NEW` 과다 / `급부상`, `급하락` 공란 증상 해소

- KR-E 소스 구현 완료 (`python/model_train.py`)
  - 중복 `from pathlib import Path` import 제거
  - `compute_sample_weights(dates, halflife_years)` 함수 추가
    - 지수 감쇠: 최근 데이터일수록 높은 가중치 (반감기 3년 기본)
    - `halflife_years=0` 이면 None 반환(균등 가중치, 기존 동작)
  - `train_regressors`, `train_classifiers` 모두 `sample_weight` 적용
  - Isotonic Calibration: 마지막 fold 검증 데이터(>=20행)로 `CalibratedClassifierCV` 적용
    - 주의: 최종 모델이 전체 데이터로 학습된 후 마지막 fold val로 calibration → 약한 data leakage 허용 (Phase 3 수준, 추후 OOF 방식으로 개선 가능)
  - CLI 인자: `--sample-weight-halflife`(기본 3.0) / `--no-calibrate`(기본 calibration 활성)
  - 서버 반영 방법:
    ```bash
    python model_train.py                                        # 새 동작 (KR-E 적용)
    python model_train.py --sample-weight-halflife 0 --no-calibrate  # 기존 동작 유지
    ```
  - 반영 조건: 서버에서 재학습 후 OOS 수익률·MDD가 기존 대비 열화되지 않을 것 (작업계획서 [KR-E] 항목 조건 준수)

- US-E smoke test 결과 기록 (2026-05-21 실행)
  - `rt_cd=7`, `msg_cd=APTR0057` — API 인증(token 200)은 정상, 주문 API 에러
  - 원인 추정: KIS 앱에서 해외주식 거래 서비스 미신청 또는 장 시간 외 주문
  - 다음 단계: KIS 앱 해외주식 서비스 신청 완료 여부 확인 → 장 중 재시도

### KR-G 매도 정책 개선 설계 확정 (2026-05-21)

**배경 분석**: 기존 AI_MAX_HOLDING_DAYS=30(env) + AI_TRAILING_STOP_MIN_PROFIT=3% + AI_TRAILING_STOP_PCT=5% 조합에서 한국 주식 정상 변동성(±5%) 때문에 trailing stop이 30일 도달 전에 빈번히 발동함. 결과적으로 30일 보유 의도와 달리 실질 보유기간이 15일 내외로 유지되는 구조적 충돌이 있음.

**확정 설계 (4항목)**:

| # | 항목 | 내용 |
|---|---|---|
| 1 | `AI_TRAILING_STOP_MIN_PROFIT` | 3% → **8%** |
| 2 | `AI_TRAILING_STOP_PCT` | 5% → **10%** |
| 3 | max_holding soft exit | `pnl < 2%` → `pnl < 0%` (0~5%는 hard_cap 45일까지 HOLD) |
| 4 | 진입 후 교체 유예 | `min_hold_days_before_replace` 10영업일 파라미터 추가 |

**보류 결정**:
- stop_loss 7%→8%: 현재 문제는 winner 조기 청산이지 초기 손절이 너무 빠른 것이 아님 → 1~4번 결과 보고 2차 결정
- 구간별 trailing B안(저점 갱신 vs 고점 기준): 1~4번 안정화 후 검토

**현재 상태**: 설계 확정, **소스 구현 완료 / 서버 반영 대기**. 상세 코드 명세는 [KR-G] 섹션 참조.

**다른 환경에서 구현 시 작업 순서**:
1. `python/ai_position_risk.py` line 182: `_cfg_int("AI_MAX_HOLDING_DAYS", 15)` → `15` → `30`
2. `python/ai_position_risk.py` line 256~265: pnl 조건 및 action 변경 (KR-G-3 코드블록 참조)
3. `python/apply_execution_policy.py`: CLI arg + per-code loop + REPLACE_CANDIDATE 2개소 수정 (KR-G-4 코드블록 참조)
4. 서버 `.env`: `AI_TRAILING_STOP_MIN_PROFIT=0.08`, `AI_TRAILING_STOP_PCT=0.10` 추가

---

### 서버 실행 우선순위 (2026-05-21 정리)

아래 순서는 **소스 수정은 이미 끝난 상태**를 전제로, 실제 데이터와 운영 로그가 있는 서버 환경에서 실행할 우선순위다.  
원칙은 `KR 운영 정상화 -> KR 검증 -> US policy/paper 검증` 순서다.

#### 1순위 — KR 운영 정상화 / close batch / 웹 동기화 확인

1. close batch 전체 재실행
2. `sync_web_display_data.py` 로그 확인
3. KR 웹 DB `daily_ranking` 날짜 이력 확인
4. `ranking.html` 초기화 이슈 재현 여부 확인

확인 포인트:
- `data/history/ranking/*_ranking_final.csv`가 최근 며칠치 정상 존재하는지
- archive 경로로 history sync가 수행됐는지
- fallback sync로 인해 이력이 잘리지 않았는지

#### 2순위 — KR 30일 보유 정책 / 모델 / 검증 반영

1. 서버 `.env`에서 KR 관련 운영값 확인
2. `pred_return_30d` 기준 재학습 / 재예측 실행
3. KR-E 반영 후 OOS / paper 비교
4. KR-G 반영 후 sell / hold 동작 로그 확인
5. accuracy maturity 조건 충족 여부 확인

확인 포인트:
- `AI_MAX_HOLDING_DAYS=30`
- `AI_TRAILING_STOP_MIN_PROFIT=0.08`
- `AI_TRAILING_STOP_PCT=0.10`
- `trailing_stop_exit`, `max_holding_days_*`, `replace deferred` 로그가 의도대로 바뀌는지

#### 3순위 — KR 승격 조건 확인

1. walkforward / paper / backtest 산출물 재확인
2. `BUY_ALLOWED` 전환 조건 충족 여부 판단
3. KR cash / exposure 운영 상태 확인

확인 포인트:
- KR는 코드 반영보다 `실제 운영 검증`이 핵심
- 승격 판단은 close batch 정상화 이후에만 진행

#### 4순위 — US 정책 반영 검증 (US-G / US-H)

1. US buy automation 로그 확인
2. `STRONG_BUY` 우선 선택 동작 확인
3. `latest_probability` 공급 상태 확인
4. US-H 반영 후 sell decision 로그 확인
5. partial sell / entry grace / soft-holding 동작 확인

확인 포인트:
- `TAKE_PROFIT`은 partial sell로 가는지
- `RANK_EXIT_THRESHOLD`만 grace 대상인지
- `score/prob` 약화는 바로 `PARTIAL_SELL` 또는 `REVIEW_REQUIRED`로 가는지
- `soft max holding` / `hard cap` 분리가 실제로 동작하는지

#### 5순위 — US paper lifecycle 검증

1. paper 30거래일 누적 운영
2. buy / sell / review_required lifecycle 점검
3. paper 성과와 backtest 방향성 비교
4. max position / sector / cash 제약이 유지되는지 확인

확인 포인트:
- `winner run` 개선 후 paper 성과가 나빠지지 않는지
- partial sell 이후 잔여 포지션 관리가 정상인지
- KR처럼 `조기 청산`이 완화됐는지

#### 6순위 — US 실계좌 준비 상태 점검 (보류 항목)

1. KIS 해외주식 서비스 신청 상태 확인
2. 필요 시 smoke test 재실행
3. 계좌 / API 키 / 차단 플래그 상태만 확인

확인 포인트:
- `US_LIVE_TRADING_ENABLED=0` 상태 유지
- 현재 문서 범위에서는 실전 gate 활성화나 제한 실거래는 진행하지 않음

#### 최종 실행 순서 요약

1. KR close batch / web sync 정상화
2. KR 30일 보유 정책 및 모델 검증
3. KR 승격 조건 판단
4. US-G / US-H 정책 반영 검증
5. US paper 30거래일 lifecycle 검증
6. US 실계좌 준비 상태 점검만 유지

---

## 10. Market Guard (2026-05-22 추가)

### 추가 배경

- 시장 급락 시 KR/US 양 시스템 동시 손실을 방어하는 자동 안전망 필요.
- 전략이 아닌 인프라 계층 — 세 시스템 공통 방어막.

### 시장 급락 감시 (Market Guard Kill Switch)

**파일**: `python/market_guard/` (신규 모듈)

**동작**:
- 하루 2회 실행 (09:10, 14:00)
- KOSPI 데이터 수집 후 조건 평가
- CRITICAL 조건 충족 시 KR/US 매수 자동 차단 + 텔레그램 알람
- 조건 해제 시 자동 복구

**감지 조건**:
| 수준 | 조건 | 동작 |
|---|---|---|
| CRITICAL | KOSPI 당일 -3% 또는 5일 누적 -7% | KR/US 매수 차단 + 알람 |
| WARNING | KOSPI 10일 누적 -10% | 알람만 |
| NONE + 이전 활성 | 회복 조건 충족 | 차단 해제 + 알람 |

**KR 차단 방식** (기존 코드 최소 수정):
- `data/market_guard_kill.json` 파일 생성
- `common_live_risk_guard.py`에 3줄 추가 (파일 존재 시 `market_guard_kill_active` reason 추가)

**US 차단 방식** (기존 코드 무수정):
- `activate_kill_switch()` 함수 호출 → `risk.us_stock_live_kill_switch` DB 테이블

**파일 구조**:
```
python/market_guard/
  config.py            ← MARKET_GUARD_* 환경변수
  detector.py          ← KOSPI 수집 + 조건 평가
  kill_switch.py       ← KR 파일 기반 / US DB 기반 활성화·해제
  notifier.py          ← 텔레그램 / 로그 알람
  run_market_guard.py  ← 메인, outputs/market_guard_status.json 기록
```

**주요 환경변수**:
```env
MARKET_GUARD_ENABLED=0
MARKET_GUARD_DRY_RUN=1                  # 1=감지만, 차단 없음
MARKET_GUARD_KOSPI_1D_THRESHOLD=-0.03
MARKET_GUARD_KOSPI_5D_THRESHOLD=-0.07
MARKET_GUARD_TELEGRAM_TOKEN=            # 선택
MARKET_GUARD_TELEGRAM_CHAT_ID=          # 선택
MARKET_GUARD_US_ACCOUNT_ID=US_TRADE_PAPER
```

**운영 반영 조건**:
1. `DRY_RUN=1`로 1~2주 실행 → 오감지 없는지 로그 확인
2. `DRY_RUN=0`으로 전환 → US kill switch 자동 활성화 시작
3. KR은 `data/market_guard_kill.json` 파일 생성 → `common_live_risk_guard.py`가 읽어 자동 차단
4. 수동 해제는 파일 삭제: `rm data/market_guard_kill.json`

---

### 구현 현황 (2026-05-22 기준)

| 항목 | 상태 |
|---|---|
| `python/market_guard/` 모듈 전체 | ✅ 소스 구현 완료 |
| `common_live_risk_guard.py` 파일 체크 3줄 | ✅ 소스 반영 완료 |
| `docker-compose.yml` 서비스 추가 | ✅ 완료 |
| `.env.example` 환경변수 추가 | ✅ 완료 |
| Phase 1 dry-run 검증 | ⏸ 서버 배포 후 실행 |
| Phase 2 실전 차단 전환 | ⏸ Phase 1 검증 후 |

### 서버 배포 시 적용 순서

```
1. 소스 배포 (docker image 재빌드)

2. .env에 추가:
   MARKET_GUARD_ENABLED=1
   MARKET_GUARD_DRY_RUN=1

3. docker-compose up -d scheduler-market-guard

4. 1~2주 로그 관찰:
   - logs/market_guard.log — 오감지 여부
   - outputs/market_guard_status.json — 최신 상태

5. 이상 없으면 DRY_RUN=0 전환
```

---

## 11. 서버 착수 체크리스트 (2026-05-22 최종 정리)

> 이 섹션은 소스 환경 작업이 끝난 뒤 서버에서 실행할 순서를 최신 상태 기준으로 정리한 단일 참조 목록이다.  
> 위 섹션들의 내용 중 "서버 반영 대기" 항목만 뽑아 실행 순서로 재정렬했다.

---

### 소스 환경 선행 작업 (서버 접속 전 완료)

| # | 항목 | 내용 | 상태 |
|---|---|---|---|
| 1 | KR-H | `model_train.py` feature importance 저장 블록 구현 + `model_feature_importance_report.py` 신규 | ⏸ 미착수 |

> KR-H는 서버 재학습 첫 실행 전에 소스에 반영해야 이력이 처음부터 축적된다.

---

### 서버 접속 후 실행 순서

#### Step 1 — 소스 배포

```bash
git pull   # 또는 docker image 재빌드
```

이 배포로 반영되는 소스 변경:
- KR-G-3: `ai_position_risk.py` — pnl soft exit 조건 완화, `AI_MAX_HOLDING_DAYS` 기본값 30
- KR-G-4: `apply_execution_policy.py` — `min_hold_days_before_replace` 10영업일 유예
- KR-I: `common_live_risk_guard.py` — 비율 방식 매수 한도, Market Guard 경로 fix, daily_loss_pct flag
- KR-I: `submit_live_orders.py` — TRIM ratio fallback 0.5 제거
- KR-I: `config/production_v1.yaml` — `buy_candidate.min_confidence` 76
- `sync_web_display_data.py` — ranking history fallback 3단계 보강
- `market_guard/` 모듈 전체, `docker-compose.yml` 서비스 추가
- KR-H: `model_train.py` feature importance 저장 블록 (소스 선행 작업 완료 후)

---

#### Step 2 — .env 반영 및 컨테이너 재기동

아래 값 서버 `.env`에 추가/수정 후 관련 컨테이너 재기동:

```env
# KR Rule 중단
RULE_LIVE_ENABLED=0

# KR 보유 기간 정합 (KR-A / KR-G)
AI_MAX_HOLDING_DAYS=30
AI_MAX_HOLDING_DAYS_HARD_CAP=45
AI_TRAILING_STOP_MIN_PROFIT=0.08
AI_TRAILING_STOP_PCT=0.10

# KR 매수 게이트 (KR-I)
GLOBAL_MAX_DAILY_BUY_RATIO=0.0
GLOBAL_MAX_WEEKLY_BUY_RATIO=0.0
GLOBAL_BLOCK_BUY_ON_DAILY_LOSS_UNAVAILABLE=1

# Market Guard Phase 1 (dry-run)
MARKET_GUARD_ENABLED=1
MARKET_GUARD_DRY_RUN=1
```

확인:
- 재기동 후 게이트/리스크 로그에서 값이 기대대로 로드되는지
- RULE 주문 경로가 비활성으로 표시되는지

---

#### Step 3 — close batch 재실행 + 웹 동기화 확인

```bash
python run_manual_close_batch.py --local
```

확인 항목:
- `sync_web_display_data.py`까지 중단 없이 전체 완주
- 스케줄러 상태가 `error`로 남지 않는지
- `sync_web_display_data.py` 로그에서 아래 중 어떤 경로인지 확인
  - `Loaded KR ranking archives ...` ← 정상
  - `Loaded KR ranking history from research.ranking_history ...` ← 정상
  - `KR ranking archive/history unavailable; fallback to source DB daily_ranking sync` ← 이력 부족 가능성
- 웹 DB `daily_ranking` 최근 5영업일 이상 유지
- `ranking.html` 급부상/급하락 공란 증상 해소

---

#### Step 4 — model_train.py 재학습 및 OOS 검증

```bash
python model_train.py
```

확인 항목:
- 재학습 정상 종료
- OOS 수익률·MDD 기존 대비 열화 없음
- feature importance CSV 생성 확인 (`data/model_feature_importance/`)
- 재예측 실행 후 `pred_return_30d` 값 정상 생성 확인

---

#### Step 5 — BUY_ALLOWED 전환 판단

아래 4가지 모두 충족 시에만 전환:

| 조건 | 확인 방법 |
|---|---|
| close batch 안정 | Step 3 전체 완주 확인 |
| 웹 이력 복구 정상 | `daily_ranking` 5영업일 이상 |
| KR 재학습/OOS 통과 | Step 4 결과 열화 없음 |
| 로그 패턴 정상 | `trailing_stop_exit`, `max_holding_days_*`, `replace deferred` 로그 확인 |

```env
# 조건 충족 시에만 변경
AUTO_TRADE_GATE_STATUS=BUY_ALLOWED
```

---

#### Step 6 — Market Guard Phase 1 시작

```bash
docker-compose up -d scheduler-market-guard
```

관찰 (1~2주):
- `logs/market_guard.log` — 오감지 없는지
- `outputs/market_guard_status.json` — 최신 상태 확인

이상 없으면 Phase 2 전환:
```env
MARKET_GUARD_DRY_RUN=0
```

---

### 이후 지속 과제

| 시점 | 항목 | 내용 |
|---|---|---|
| 1~2주 | Market Guard Phase 2 전환 | DRY_RUN=0, 실제 차단 활성화 |
| 4주 후 | KR-G 2차 검토 트리거 | trailing/holding 패턴 확인 후 stop_loss 7→8% 전환 여부 결정 |
| 60일 경과 후 | KR-C accuracy 리포트 | `evaluate_prediction_accuracy.py` 첫 유의미한 결과 생성 |
| paper 30거래일 후 | US lifecycle 검증 | buy/sell/grace/soft-holding 동작 확인 |
| 2026-07~08 | 성과 재평가 | KR 3개월 실거래 + US paper 결과 합산 → 월 2% 달성 가능성 판단 |
