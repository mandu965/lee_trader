# 20260518 US 실계좌연결 작업계획

## 목적

US 자동매매를 현재 `rule 기반 paper 운영` 상태에서 `실계좌 연결 가능한 live 운영` 상태로 전환한다.

목표 조건:

- `.env` 에 실계좌 관련 값과 브로커 설정을 넣으면 운영 가능해야 한다.
- `종목 후보선정 -> 주문 초안 -> 사전점검 -> 승인 -> 주문 제출 -> 체결 동기화 -> 잔고/현금/손익 반영` 이 끊기지 않아야 한다.
- paper 화면과 live 화면이 동일한 운영 포맷을 최대한 공유해야 한다.
- kill switch, 주문 한도, 승인 체계, 복구 절차가 포함되어야 한다.

## 현재 상태 요약

현재 US 모듈은 다음 수준까지 구현되어 있다.

- rule 기반 종목 후보선정 가능
- paper 주문 생성 가능
- paper 포지션/현금/평가금액 화면 확인 가능
- trade orchestration, dashboard, health check 구조 존재
- live pre-trade check, approval, micro-order 프레임워크 존재

현재 실전 전환을 막는 핵심 문제:

1. US live broker 어댑터에 `KIS(한투)` 가 없다.
2. BUY live 경로는 `LIVE_NOT_IMPLEMENTED` 로 차단되어 있다.
3. SELL live 경로도 `LIVE_NOT_IMPLEMENTED` 상태다.
4. 메인 orchestration 은 real order path 없이 paper 중심으로 설계되어 있다.
5. live order 생성 경로와 current US orchestration 이 자동 연결되어 있지 않다.
6. 체결/잔고/현금 reconciliation 이 KIS US 기준으로 붙어 있지 않다.

## 코드 기준 현황

핵심 확인 파일:

- [python/us/buy_automation/decision_engine.py](/d:/ai/lee_trader/python/us/buy_automation/decision_engine.py)
- [python/us/sell_automation/sell_decision_engine.py](/d:/ai/lee_trader/python/us/sell_automation/sell_decision_engine.py)
- [python/us/trade_orchestration/daily_trade_orchestrator.py](/d:/ai/lee_trader/python/us/trade_orchestration/daily_trade_orchestrator.py)
- [utils/us_live_order_client.py](/d:/ai/lee_trader/utils/us_live_order_client.py)
- [utils/us_micro_order_request.py](/d:/ai/lee_trader/utils/us_micro_order_request.py)
- [python/us/run_us_live_pre_trade_check.py](/d:/ai/lee_trader/python/us/run_us_live_pre_trade_check.py)

정리:

- `utils/us_live_order_client.py`
  - 현재 live broker 지원: `ALPACA`, `IBKR`
  - `KIS` 미지원
- `buy_automation/decision_engine.py`
  - `cfg.mode == "LIVE"` 시 `LIVE_NOT_IMPLEMENTED` 차단
- `sell_automation/sell_decision_engine.py`
  - `cfg.mode == "LIVE"` 시 `LIVE_NOT_IMPLEMENTED` 처리
- `run_us_live_pre_trade_check.py`
  - live 실주문 전 검증/approval 요청 생성은 가능
  - 실제 자동매매 main path 와는 분리

## 최종 아키텍처 목표

실계좌 연결 완료 후 목표 흐름:

1. `US ranking/rule` 기반 후보 추출
2. risk guard/conflict guard 반영
3. live pre-trade check 실행
4. approval 정책에 따라 승인 요청 생성 또는 자동 통과
5. micro live order 생성
6. KIS US broker 로 주문 제출
7. 주문 상태/체결 동기화
8. live holdings/cash/equity/reconciliation 반영
9. dashboard/UI 에 paper 와 동일 포맷으로 표시

## 작업 범위

### 1. 브로커 연동

필수 작업:

- `brokers/us/kis_live_client.py` 신규 구현
- KIS 미국주식 주문 API 매핑
- 주문 제출
- 주문 정정/취소
- 주문 상태 조회
- 체결 조회
- 계좌 잔고/현금 조회

반영 대상:

- `utils/us_live_order_client.py`
- `utils/us_order_status_mapper.py`
- `utils/us_live_account_client.py`

완료 기준:

- `US_LIVE_BROKER=KIS` 설정 시 adapter resolve 가능
- dry-run/sandbox/live 경로가 브로커명 기준으로 분기 가능

### 2. BUY 실주문 경로 연결

필수 작업:

- `LIVE_NOT_IMPLEMENTED` 제거
- BUY candidate -> approval -> micro order create path 연결
- `allowed candidate` 를 live order candidate 로 변환
- order type/limit price/qty 계산 규칙 확정

반영 대상:

- `python/us/buy_automation/decision_engine.py`
- `python/us/trade_orchestration/daily_trade_orchestrator.py`
- `utils/us_micro_order_request.py`

완료 기준:

- live mode 에서 차단 이유가 `LIVE_NOT_IMPLEMENTED` 가 아니어야 함
- approval 통과 시 실제 micro order row 생성 가능

### 3. SELL 실주문 경로 연결

필수 작업:

- SELL signal -> approval/auto path 정의
- full sell / partial sell 의 live order 변환
- 보유수량과 매도가능수량 검증
- 중복매도 방지 로직 추가

반영 대상:

- `python/us/sell_automation/sell_decision_engine.py`
- `utils/us_micro_order_request.py`
- `python/us/trade_orchestration/portfolio_state_loader.py`

완료 기준:

- live mode 에서 SELL도 실제 주문 생성 가능
- 동일 종목 중복 SELL 방지 가능

### 4. Approval / Safety 체계 정리

필수 작업:

- BUY 승인 필요 여부 정책 확정
- SELL 승인 필요 여부 정책 확정
- final confirmation text 적용 범위 정리
- kill switch 계층 점검
- 장 시간 외 차단 규칙 반영

반영 대상:

- `utils/us_live_order_safety.py`
- `utils/us_live_order_approval.py`
- `utils/us_live_kill_switch.py`
- `python/us/run_us_live_pre_trade_check.py`

완료 기준:

- 승인 없이 실주문이 나가지 않도록 정책 강제 가능
- 승인 정책을 env/policy 로 제어 가능

### 5. 체결/잔고/현금 동기화

필수 작업:

- broker order id 기준 상태 조회
- partial fill / full fill / reject / cancel 동기화
- 체결 반영 후 holdings, cash, equity 계산
- live reconciliation report 생성

반영 대상:

- `utils/us_micro_order_sync.py`
- `utils/us_micro_reconciliation.py`
- `utils/us_live_account_client.py`
- DB `live.us_stock_micro_order_*`, reconciliation tables

완료 기준:

- 주문 제출 후 체결 상태가 DB와 UI에 반영
- broker cash / internal cash 차이 검증 가능

### 6. UI / 운영화면 통합

필수 작업:

- current `us-trading` 을 paper/live 공용 보드로 확장
- execution/result/reconciliation 섹션 추가
- broker reject 사유 표시
- approval pending 건 표시

반영 대상:

- `node/index.js`
- `node/public/us-trading.html`
- `node/public/us-trading.js`

완료 기준:

- operator 가 한 화면에서 후보, 주문, 체결, 보유, 손익, 오류를 확인 가능

## 단계별 실행 계획

### Phase 1. KIS US 연결 기반 구축

- KIS 미국주식 주문/조회 API 스펙 정리
- KIS live adapter 구현
- env 항목 정의
- mock adapter 와 동일 interface 검증

산출물:

- `brokers/us/kis_live_client.py`
- `.env.example` 업데이트
- adapter unit test

### Phase 2. Live order orchestration 연결

- BUY/SELL live mode 구현
- approval -> micro order -> send 연결
- live pre-check 와 orchestration bridge 작성

산출물:

- live mode orchestration patch
- micro order create/send integration test

### Phase 3. Sync / Reconciliation / Dashboard

- 주문 상태 sync
- fill sync
- live cash/holding reconciliation
- UI 반영

산출물:

- sync runner
- reconciliation report
- live dashboard cards

### Phase 4. 운영 안전성 검증

- 장전/장중/장후 시나리오 점검
- reject/retry/cancel 시나리오 점검
- duplicate order 방지 점검
- kill switch 점검

산출물:

- 실전 전환 체크리스트
- 장애복구 runbook

## 필수 env 항목

현재 실전 기준으로 최소 필요:

- `US_LIVE_BROKER=KIS`
- `US_MICRO_ALLOW_LIVE=true`
- `US_MICRO_REAL_ORDER_BLOCKED=false`
- `US_LIVE_TRADING_ENABLED=true`
- `US_LIVE_ORDER_ENABLED=true`
- `US_LIVE_BUY_ENABLED=true`
- `US_LIVE_SELL_ENABLED=true`

추가로 KIS US 전용 값이 필요할 가능성이 높음:

- `KIS_US_APP_KEY`
- `KIS_US_APP_SECRET`
- `KIS_US_CANO`
- `KIS_US_ACNT_PRDT_CD`
- `KIS_US_BASE_URL`
- `KIS_US_ACCOUNT_PRODUCT_CODE`

주의:

- 현재 repo 에는 국내 KIS 계좌 값은 있으나, US live broker path 와 직접 연결되지 않는다.
- 실전 전환 전까지 `US_MICRO_REAL_ORDER_BLOCKED=true` 기본값 유지가 필요하다.

## 실전 전환 완료 기준

다음 조건이 모두 충족되어야 `env만 넣으면 실전 가능` 으로 본다.

1. `US_LIVE_BROKER=KIS` 로 live adapter 정상 생성
2. BUY/SELL live mode 에서 `LIVE_NOT_IMPLEMENTED` 제거
3. 승인 후 실제 주문 제출 가능
4. broker order status / fill sync 가능
5. holdings/cash/equity/reconciliation 자동 반영
6. UI 에 실주문 상태와 reject 사유 표시
7. kill switch / pre-check / order limit / duplicate guard 검증 완료
8. 종가 기준 paper 와 실시간 live 계좌 상태 비교 가능

## 리스크

- KIS 미국주식 API 제한사항 미확인
- 지정가/시장가/장외시간 규칙 차이
- 환율 및 settlement 반영 방식 차이
- broker reject 사유 normalization 누락
- partial fill 시 internal state mismatch
- approval flow 와 auto orchestration 충돌

## 우선순위

1. `KIS live adapter 구현`
2. `BUY live 경로 연결`
3. `SELL live 경로 연결`
4. `status/fill sync`
5. `cash/holding reconciliation`
6. `UI/live dashboard 통합`

## 권고

현 시점에서는 US를 즉시 실전투입하지 않는다.

권장 순서:

1. KIS US live adapter 구현
2. mock -> sandbox -> 승인형 live 1주 검증
3. 소액 실주문 pilot
4. 이상 없을 때 full live 전환

## 다음 작업 제안

다음 구현 순서는 아래로 진행하는 것이 가장 효율적이다.

1. `brokers/us/kis_live_client.py` 초안 작성
2. `utils/us_live_order_client.py` 에 KIS 추가
3. `BUY/SELL LIVE_NOT_IMPLEMENTED` 제거 설계
4. `approval -> create_micro_order -> send_live` 연결
5. 실계좌 동기화 및 reconciliation 반영
