# 점수산정(Score) 개선과제 — 2026-05-15

**작성일**: 2026-05-15  
**기준**: v9_flow 공식 적용 완료 후 잔여 과제 정리

---

## 완료 현황

| 완료 시점 | 항목 |
|---|---|
| 2026-05-14 | v9_flow 공식 적용 (`scoring/final_score.py`, `production_config.py`) |
| 2026-05-14 | flow_score 컴포넌트 추가 (Bull 12%, Neutral 10%, Defensive 8%) |
| 2026-05-14 | A-1 중장기 모멘텀 피처 (`ret_60d`, `ret_120d`) |
| 2026-05-14 | A-2 52주 신고가 근접도 (`high_52w_ratio`) |
| 2026-05-14 | 웹 DB 신규 컬럼 추가 (`flow_*` 4개 + `ret_60d` + `ret_120d` + `high_52w_ratio`) |
| 2026-05-15 | KRX 백필 완료 → flow_score 입력 품질 향상 (51일 → 330일) |

**현재 적용 공식 (v9_flow)**

```
final_score = w_ret   * ret_score
            + w_prob  * prob_score
            + w_tech  * tech_score
            + w_qual  * qual_score
            + w_flow  * flow_score
            - w_risk  * risk_penalty

flow_score = 0.6 * percentile(flow_foreign_net_5d)
           + 0.4 * percentile(flow_inst_net_5d)

Bull    : ret=0.33, prob=0.24, tech=0.23, qual=0.08, flow=0.12
Neutral : ret=0.28, prob=0.23, tech=0.21, qual=0.18, flow=0.10
Defensive: ret=0.23, prob=0.19, tech=0.16, qual=0.34, flow=0.08
```

---

## 잔여 과제

> **주의**: 아래 과제들은 모두 `features.csv` 피처 추가 → **LightGBM 모델 재학습** 필요.  
> `Lee_trader_ai/20260515_개선과제.md`와 연동하여 진행.

### 🟡 1순위 — qual_score 입력 개선 (YoY 성장률)

현재 `qual_score`는 레벨 지표(ROE, 부채비율 등)만 반영. 성장 방향을 보지 못함.

- 추가 피처: `revenue_growth_yoy`, `op_income_growth_yoy`, `roe_yoy`
- 작업 파일: `python/quality_builder.py` → YoY 계산 추가
- 모델 재학습 후 `qual_score` 내 기여도 확인

### 🟡 2순위 — tech_score 입력 개선 (섹터 상대강도)

현재 `tech_score`는 절대 모멘텀만 반영. 섹터 내 상대 포지션 없음.

- 추가 피처: `sector_rel_momentum_20d` = 종목 ret_20d − 섹터 평균 ret_20d
- 작업 파일: `python/feature_builder.py`
- 활용: Defensive 레짐에서 섹터 약세 종목 감점 강화

### 🟡 3순위 — flow_score 입력 보완 (외국인 지분율)

현재 flow_score는 순매수 방향만 반영. 지분율 한도 근접 여부 없음.

- 추가 피처: `foreign_ownership_pct`
- 작업 파일: `python/fetch_top_universe.py` 확장
- 의미: 지분율 25% 이상 → 추가 유입 여력 감소 → flow_score 소폭 감산

### 🔵 4순위 — qual_score 데이터 신선도 (DART 분기 재무)

현재 DART 연간 사업보고서(11014)만 수집 → 최대 12개월 지연.

- 분기(`11011`/`11013`), 반기(`11012`) 추가
- 작업 파일: `python/fetch_fundamentals_dart.py`
- 주의: 연결·별도 기준 불일치 검증 필요

---

## 모니터링

| 일정 | 항목 |
|---|---|
| 2026-05-15 모델 재학습 완료 후 | feature importance에서 `flow_*` 순위 확인 |
| 2026-05-15 모델 재학습 완료 후 | `ranking_final.csv` 상위 20위 변동 shadow 비교 |
| 2026-05-28~ | Gate PILOT → BUY_ALLOWED 자동 전환 확인 |
