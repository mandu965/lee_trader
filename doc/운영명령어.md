
# 운영 명령어 모음
# 마지막 갱신: 2026-05-29 — market_guard 정상화, 모델 재학습(OOF calibration), ai_filtered 파이프라인 복귀

# 작업 전에 최신 원격 반영
git fetch origin
git switch main
git pull --ff-only origin main

## 완전히 다시 받기
git fetch origin
git reset --hard origin/main
git clean -fd

python python\import_git_release.py --source D:\ai\git\lee_trader --clean-target

# git 반영

python python\export_git_release.py --target D:\ai\git\lee_trader --clean-target

cd D:\ai\git\lee_trader
git status
git add --all
git commit -m "update deploy package"

git push origin main

# 수동 배포
docker compose build --no-cache --progress=plain python-pipeline
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

# 앞으로 수동 배치 실행 명령어 (Docker 불필요, 전체 18:10 동일)
.venv\Scripts\python.exe python/run_manual_close_batch.py --local

# 파이프라인 재스타트
docker compose restart python-pipeline


# 컨테이너 역할
scheduler: 18:10 close 배치
scheduler-recovery: 12:00 intraday refresh
scheduler-auto-buy: 09:30 자동매매
scheduler-live-account-sync: 실계좌 동기화
node-api: 웹 조회 API
postgres: 웹/API/동기화 대상 DB

# 재배포
docker compose up -d --build scheduler-auto-buy
docker compose up -d --build scheduler-live-account-sync
docker compose up -d --build node-api

# 로그
docker compose logs -f --tail=500
docker compose logs -f --tail=500 node-api
docker compose logs -f --tail=500 lee_trader_scheduler
node-api
scheduler-market-guard
postgres
scheduler
scheduler-auto-buy
scheduler-live-account-sync
scheduler-us-macro
scheduler-us-macro-shadow
scheduler-us-pipeline

# 런타임 필수 자산 검증
docker compose run --rm scheduler-auto-buy python python/validate_runtime_assets.py --command-set auto_buy --strict
docker compose run --rm scheduler-live-account-sync python python/validate_runtime_assets.py --command-set live_sync --strict

# 상시운용 컨테이너 
docker compose up -d postgres
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
Start-Sleep -Seconds 65

docker compose up -d --build node-api
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
Start-Sleep -Seconds 65

docker compose up -d scheduler
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
Start-Sleep -Seconds 65

docker compose up -d scheduler-recovery
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
Start-Sleep -Seconds 65

docker compose up -d scheduler-auto-buy
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
Start-Sleep -Seconds 65

docker compose up -d scheduler-live-account-sync
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
Start-Sleep -Seconds 65

docker compose up -d scheduler-us-macro
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
Start-Sleep -Seconds 65

docker compose up -d scheduler-us-macro-shadow
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
Start-Sleep -Seconds 65

docker compose up -d scheduler-us-pipeline
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
Start-Sleep -Seconds 65

docker compose up -d scheduler-market-guard
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

# 전체 계좌 체결내역 강제 조회가 필요할 때만 사용
.venv\Scripts\python.exe python\sync_live_order_fills.py --query-all --start-date 20260424 --end-date 20260426

# 실매매 리뷰 조회 API
curl "http://localhost:3400/api/live-trade-review-report"
curl "http://localhost:3400/api/live-trade-review-summary"
curl "http://localhost:3400/api/live-trade-consistency"

# 실매매 리뷰/체결 DB 확인
docker compose exec -T postgres psql -U lee_trader -d lee_trader -c "SELECT review_date, reviewer, COUNT(*) AS rows FROM research.live_trade_review GROUP BY review_date, reviewer ORDER BY review_date DESC, reviewer;"
docker compose exec -T postgres psql -U lee_trader -d lee_trader -c "SELECT outcome_label, COUNT(*) AS rows FROM research.live_trade_review WHERE reviewer='auto_review' GROUP BY outcome_label ORDER BY outcome_label;"
docker compose exec -T postgres psql -U lee_trader -d lee_trader -c "SELECT COUNT(*) AS fill_rows, MIN(filled_at) AS first_fill, MAX(filled_at) AS last_fill FROM research.live_order_fill;"
docker compose exec -T postgres psql -U lee_trader -d lee_trader -c "SELECT payload_key, asof_date, updated_at FROM research.app_payload_store WHERE payload_key IN ('live_trade_consistency_report','live_trade_review_report','live_trade_review_summary') ORDER BY payload_key;"


# =============================================================================
# [US Stock Module — 미운영]
# 코드는 python/us/ 에 존재하나 현재 스케줄러 비활성화 상태.
# 아래 명령은 수동 실행 참고용으로만 보관. 재개 시 별도 설계 필요.
# =============================================================================

# ── 초기 백필 (최초 1회 실행, 순서대로) ──────────────────────────────────────

# 1. 가격 백필 (5년치, 약 30~60분 소요)
.venv\Scripts\python.exe python/us/run_us_daily_pipeline.py --backfill --skip-features

# 2. 기술 피처 빌드 (MA, 모멘텀, 변동성 등)
.venv\Scripts\python.exe python/us/build_us_features.py

# 3. 재무 데이터 수집 (yfinance annual+quarterly, 약 20~40분)
.venv\Scripts\python.exe python/us/collect_us_financials_yfinance.py

# 4. 재무 피처 빌드 (ROE, 마진, 부채비율 등)
.venv\Scripts\python.exe python/us/build_us_financial_features.py

# 5. 상대강도 피처 빌드 (SPY/QQQ 대비 5/20/60/120/252일)
.venv\Scripts\python.exe python/us/build_us_relative_strength_features.py

# 6. 룰 점수 계산 및 랭킹 테이블 저장 (날짜 지정 필수)
.venv\Scripts\python.exe python/us/calculate_us_stock_rule_scores.py --trade-date 2026-05-16

# 7. 초기 백필 완료 확인 — 상위 랭킹 조회
.venv\Scripts\python.exe python/us/report_us_stock_top_rank.py

# ── 일일 운영 (미국 종가 후 — 한국시간 오전 6~7시) ──────────────────────────

# 일일 Step 1: 가격 증분 수집 + 기술 피처 + Shadow 매수 결정 (어제 랭킹 기준)
.venv\Scripts\python.exe python/us/run_us_daily_pipeline.py --incremental

# 일일 Step 2: 상대강도 업데이트 (오늘 가격 반영)
.venv\Scripts\python.exe python/us/build_us_relative_strength_features.py

# 일일 Step 3: 룰 점수 계산 (내일 Shadow 매수에 사용될 랭킹)
$today = Get-Date -Format 'yyyy-MM-dd'
.venv\Scripts\python.exe python/us/calculate_us_stock_rule_scores.py --trade-date $today

# 일일 Step 4: Shadow 매수 결정 리포트 확인
.venv\Scripts\python.exe python/us/run_us_buy_report.py

# ── 확인 / 조회 ───────────────────────────────────────────────────────────────

# 상위 랭킹 종목 조회
.venv\Scripts\python.exe python/us/report_us_stock_top_rank.py

# 데이터 품질 검증
.venv\Scripts\python.exe python/us/validate_us_price_data.py
.venv\Scripts\python.exe python/us/validate_us_stock_ml_dataset.py

# 백테스트 결과 확인
.venv\Scripts\python.exe python/us/report_us_stock_rank_backtest.py

# 레짐 분석 (SPY/QQQ 변동성 기반)
.venv\Scripts\python.exe python/us/build_us_market_regime_daily.py

# Live Readiness 점검 (20일 Shadow 후 의미 있음)
.venv\Scripts\python.exe python/us/run_us_buy_readiness.py

# ── Phase B 전환 (60일 Shadow 후) ─────────────────────────────────────────────
# .env 변경:
#   US_PAPER_TRADING_ENABLED=1
#   US_BUY_AUTOMATION_MODE=PAPER
# 재무 데이터 주 1회 갱신:
#   .venv\Scripts\python.exe python/us/collect_us_financials_yfinance.py


# =============================================================================
# [US Stock Module — ML 모델 (LightGBM 기반 랭킹)]
# 2026-05-19 활성화 | .env: US_ML_RANKING_ENABLED=1
# OOS 검증 결과: ml_v1 TOP5 60d +9.12% avg, +14.20% alpha_SPY vs rule_v1 +3.68%
# =============================================================================

# ── 모델 학습 ────────────────────────────────────────────────────────────────

# 프로덕션 모델 학습 (전체 기간 2021~ 학습, data/us_model.pkl 생성, 약 2~5분)
.venv\Scripts\python.exe python/us/us_model_train.py

# OOS 검증 모델 학습 (2021~2023 학습, data/us_model_oos.pkl 생성)
.venv\Scripts\python.exe python/us/us_model_train.py `
  --train-end-date 2023-12-31 `
  --output-pkl data/us_model_oos.pkl `
  --model-version oos_v1

# ── ML 랭킹 백필 ─────────────────────────────────────────────────────────────

# 전체 기간 ML 랭킹 백필 (source=ml_v1, 약 10~20분)
.venv\Scripts\python.exe python/us/us_ranking_builder_ml.py `
  --start-date 2021-01-01 --end-date 2026-05-19 --source ml_v1

# OOS 기간 ML 랭킹 백필 (source=ml_oos_v1, 2024-01-01 이후만)
.venv\Scripts\python.exe python/us/us_ranking_builder_ml.py `
  --start-date 2024-01-01 --end-date 2026-05-19 `
  --source ml_oos_v1 --model-pkl data/us_model_oos.pkl

# 특정 날짜만 재계산
$today = Get-Date -Format 'yyyy-MM-dd'
.venv\Scripts\python.exe python/us/us_ranking_builder_ml.py `
  --trade-date $today --source ml_v1

# ── 일일 ML 랭킹 운영 ─────────────────────────────────────────────────────────

# 일일 파이프라인에서 자동 실행됨 (.env US_ML_RANKING_ENABLED=1 확인)
# 수동으로 오늘 ML 랭킹만 재생성
$today = Get-Date -Format 'yyyy-MM-dd'
.venv\Scripts\python.exe python/us/us_ranking_builder_ml.py --trade-date $today

# ML 랭킹 상위 종목 조회 (source 구분 포함)
.venv\Scripts\python.exe python/us/report_us_stock_top_rank.py

# ML vs Rule 랭킹 DB 비교 (최근 3 영업일)
docker compose exec -T postgres psql -U lee_trader -d lee_trader -c `
  "SELECT trade_date, source, COUNT(*) AS cnt, ROUND(AVG(score)::numeric,1) AS avg_score FROM recommend.us_stock_rank_daily WHERE trade_date >= CURRENT_DATE - 5 GROUP BY trade_date, source ORDER BY trade_date DESC, source;"

# ── 백테스트 / 성과 검증 ─────────────────────────────────────────────────────

# 전체 백테스트 (rule_v1 · ml_v1 · ml_oos_v1 비교)
.venv\Scripts\python.exe python/us/report_us_stock_rank_backtest.py

# ML 데이터셋 품질 검증 (레이블 비율, 피처 NaN 등)
.venv\Scripts\python.exe python/us/validate_us_stock_ml_dataset.py

# ── 모델 재학습 절차 (3개월마다 권장) ──────────────────────────────────────

# Step 1: 신규 레이블 생성 (최신 가격 반영)
.venv\Scripts\python.exe python/us/build_us_stock_labels.py

# Step 2: 모델 재학습 (data/us_model.pkl 덮어쓰기)
.venv\Scripts\python.exe python/us/us_model_train.py

# Step 3: 오늘 날짜 ML 랭킹 재계산
$today = Get-Date -Format 'yyyy-MM-dd'
.venv\Scripts\python.exe python/us/us_ranking_builder_ml.py --trade-date $today --source ml_v1


# ── KR AI 모델 재학습 (Docker 컨테이너 내부 실행) ──────────────────────────

# Step 1: 모델 재학습 (OOF calibration 적용, data/model.pkl 덮어쓰기, 약 10분)
docker exec lee_trader_scheduler bash -c "cd /app && python /app/python/model_train.py"

# Step 2: 새 모델로 예측 재생성
docker exec lee_trader_scheduler bash -c "cd /app && python /app/python/model_predict.py"

# Step 3: 새 예측으로 ranking 재생성
docker exec lee_trader_scheduler bash -c "cd /app && python /app/python/ranking_builder.py"

# Step 4: 운영 파이프라인 전체 재실행 (ai_entry_quality → ai_filtered → trade_intents)
docker exec lee_trader_scheduler bash -c "cd /app && python /app/python/run_operational_refresh.py"


# ── 시장 급락 감시 (market_guard) ───────────────────────────────────────

# 수동 감지 (KOSPI 데이터 즉시 평가, DRY_RUN 유지)
docker exec lee_trader_market_guard bash -c "cd /app && python /app/python/market_guard/run_market_guard.py"

# 컨테이너 재시작 (코드 변경 후)
docker restart lee_trader_market_guard

# 상태 확인
cat outputs/market_guard_status.json | python -m json.tool


# ── A-3 features merge 버그 검증 (재현 방지) ────────────────────────────

# features 테이블에 revenue_growth_yoy / op_income_growth_yoy 정상 채워졌는지 확인
docker exec lee_trader_scheduler bash -c "cd /app && python -c \"
import psycopg2, os
from dotenv import load_dotenv
load_dotenv('/app/.env')
conn = psycopg2.connect(os.getenv('DATABASE_URL'))
cur = conn.cursor()
cur.execute('SELECT COUNT(*), COUNT(revenue_growth_yoy), COUNT(op_income_growth_yoy) FROM features')
print(cur.fetchone())
\""

# features 재빌드 (강제)
docker exec lee_trader_scheduler bash -c "cd /app && python /app/python/feature_builder.py"


# ── 자동매매 초안 (실주문 안 함, preview만) ──────────────────────────────

# 주문 요청 preview 생성
docker exec lee_trader_scheduler bash -c "cd /app && python /app/python/submit_live_orders.py"

# Live order preview (운영자 페이지 표시용)
docker exec lee_trader_scheduler bash -c "cd /app && python /app/python/build_live_order_preview.py"

# 결과 확인
type outputs\order_requests_preview.md
type outputs\live_order_preview.md


# ── 운영자 페이지 ──────────────────────────────────────────────────

# 접속: http://localhost:3400/ops-readiness.html
# 캐시 강제 갱신: Ctrl+F5

# node-api 컨테이너 재시작 (코드 변경 후)
docker restart lee_trader_api


# ── 종료된 US 관련 명령어 (참고용, 더 이상 실행 안 함) ──────────────────
# 2026-05-29 US 서비스 종료. 위 US 백테스트/재학습 명령어는 호환성 유지용이며
# 실제 운영에서는 사용하지 않습니다. KR AI 단독 운영 체제.
