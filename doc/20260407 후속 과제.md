# 2026-04-07 후속 과제

기준 문서
- [20260406 개선사항.md](/d:/ai/Lee_trader/doc/20260406%20개선사항.md)
- [20260406 실행 체크리스트.md](/d:/ai/Lee_trader/doc/20260406%20실행%20체크리스트.md)
- [20260407 다음 진행 순서.md](/d:/ai/Lee_trader/doc/20260407%20다음%20진행%20순서.md)

이 문서는 2026-04-06 개선사항 1차 반영 이후 남은 후속 작업을 운영 기준으로 정리한 문서다.

## 현재 정리된 상태
- `buy_eligibility` 분리와 UI 반영 완료
- `confidence` 기반 비중 규칙 반영 완료
- `walkforward / calibration` 운영 반영 복구 완료
- `전일 종가 기준` 운영 규칙 고정 완료
- `quality_risk_guard shadow` 분석, sidecar, UI 반영 완료
- 운영 화면 내 `BLOCK / REJECTED / ALERT` 해석 문구 반영 완료
- `market regime` 운영 해석 문구 고도화 완료
- `theme`는 production 점수가 아니라 설명 축으로 정리 완료

## 후속 과제 상태

### 1. quality_risk_guard 반복성 검증
목표
- `quality_risk_guard`가 일회성 개선이 아니라 반복적으로 유효한지 확인

진행 결과
- [repair_shadow_ranking_snapshots.py](/d:/ai/Lee_trader/python/repair_shadow_ranking_snapshots.py) 추가
- [run_operational_refresh.py](/d:/ai/Lee_trader/python/run_operational_refresh.py)에 snapshot 복구 단계 연결
- `2026-04-07` archived ranking snapshot을 shadow 컬럼 포함 버전으로 복구
- repeatability 리포트 재생성 완료

최신 기준
- [shadow_quality_risk_guard_repeatability_report.json](/d:/ai/Lee_trader/outputs/shadow_quality_risk_guard_repeatability_report.json)
- `usable_snapshot_count = 1`
- `judgment = insufficient_history`

해석
- 이전에는 archived snapshot에 shadow 컬럼이 없어 usable history가 `0`으로 과소집계되었다.
- 현재는 최소 1일치 usable snapshot이 정상 반영된다.
- 다만 `emerging_repeatability` 판단까지는 아직 하루 이상 추가 이력이 더 필요하다.

남은 일
- 다음 운영일 snapshot 1회 이상 추가 누적
- repeatability 리포트 재생성 후 `emerging_repeatability` 도달 여부 확인

### 2. quality upstream coverage 개선
목표
- `qual_score`를 단순 보조축이 아니라 해석 가능한 축으로 정리
- 실제 원천 데이터 측면에서 `roe`, `ocf_to_assets`, `net_margin` 커버리지 개선

진행 결과
- [quality_builder.py](/d:/ai/Lee_trader/python/quality_builder.py)에 종목별 1년 한도 limited carry-forward 보완 추가
- `quality -> features -> ranking -> quality coverage report` 전 구간 재생성 완료

최신 기준
- [quality_coverage_report.json](/d:/ai/Lee_trader/outputs/quality_coverage_report.json)

개선 수치
- `mean_quality_missing_ratio`: `0.2414 -> 0.1828`
- `mean_quality_factor_count`: `3.79 -> 4.09`
- `mean_quality_score_confidence`: `75.86 -> 87.20`
- `low_confidence_count`: `15 -> 6`
- `heavy_missing_count`: `15 -> 10`
- `top20_mean_quality_missing_ratio`: `0.16 -> 0.09`
- `top20_mean_quality_score_confidence`: `84.0 -> 94.0`
- `top20_low_confidence_count`: `0 -> 0`
- `top20_low_qual_count`: `2 -> 2`

해석
- quality coverage는 초기 대비 유의미하게 개선됐다.
- `quality_coverage_ready = true` 판단을 다시 지지할 수 있는 상태다.
- 다만 금융주 및 일부 종목은 여전히 low coverage 구간이 남아 있어 원천 수집 고도화 여지는 남아 있다.

추가 후보
- DART 원천 수집 정확도 개선
- 금융/특수 업종 별도 처리 규칙 검토

### 3. 운영 보고 체계 고도화
목표
- 운영 화면과 daily report만으로 현재 상태를 빠르게 읽을 수 있게 만들기

진행 결과
- [build_operational_buy_gate.py](/d:/ai/Lee_trader/python/build_operational_buy_gate.py)에 `interpretation` 계층 추가
- `summary_decision`, `summary_reason`, `action_guide`, `critical_reasons` 생성
- [operational_buy_gate_report.md](/d:/ai/Lee_trader/outputs/operational_buy_gate_report.md) 상단에 `Executive Summary`, `Key Blockers` 추가
- [build_shadow_quality_risk_guard_daily_report.py](/d:/ai/Lee_trader/python/build_shadow_quality_risk_guard_daily_report.py) 확장
- repeatability가 유효해지면 daily report에 `Top Repeaters`가 자동 반영되도록 연결

현재 운영 결론 예시
- 오늘 결론: `실매수 진행 금지`
- 핵심 blocker:
- `benchmark matured dates = 0`
- `walk-forward acceptance = REJECTED`
- `operational confidence calibration = not reliable`
- `top20 blocked ratio = 100%`
- `market regime = neutral`

해석
- 데이터는 충분했지만 읽기 쉬운 결론층이 약했던 문제를 정리했다.
- 이제 운영 markdown과 JSON 모두 같은 요약 문구를 사용한다.

### 4. quality_risk_guard production 승격 판단
목표
- `quality_risk_guard`를 production 점수에 올릴지 여부를 근거 기반으로 판단

진행 결과
- [quality_risk_guard_promotion_report.json](/d:/ai/Lee_trader/outputs/quality_risk_guard_promotion_report.json) 최신 재생성 완료
- coverage 개선분을 반영한 뒤 promotion report 다시 생성

최신 기준
- `verdict = HOLD_FOR_MORE_EVIDENCE`
- `ordering_ok = true`
- `repeatability_judgment = insufficient_history`
- `quality_coverage_ready = true`

현재 체크 결과
- PASS `ordering_ok`
- PASS `top20_return_not_worse`
- PASS `top20_mdd_not_worse`
- PASS `low_quality_count_improved`
- PASS `high_risk_count_improved`
- PASS `quality_coverage_ready`
- HOLD `repeatability_ready`

핵심 수치
- `top20_avg_return`: `34.13% -> 40.47%`
- `top20_avg_mdd`: `-27.11% -> -26.57%`
- `top20_low_qual_count`: `9 -> 4`
- `top20_high_risk_count`: `7 -> 5`

해석
- 최신 matured run 기준 성능과 quality coverage는 승격 검토에 충분한 수준이다.
- 현재 남은 blocker는 사실상 `repeatability` 하나다.
- repeatability usable snapshot이 2일 이상 누적되어 `emerging_repeatability`로 올라오기 전까지는 production 승격 보류가 맞다.

## 현재 우선순위
1. 다음 운영일 snapshot 누적
2. `shadow_quality_risk_guard_repeatability_report` 재생성
3. `quality_risk_guard_promotion_report` 재판단

## 운영 결론
- `20260406 개선사항.md` 기준의 1차 반영 작업은 사실상 정리됐다.
- 이 문서 기준 후속 과제 중 실질적으로 남은 핵심은 `repeatability` 이력 축적과 재판단이다.
- 다음 재판단 시점 전까지는 `quality_risk_guard`를 production으로 승격하지 않는다.
