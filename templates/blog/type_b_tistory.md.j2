---
title: {{ pick("title_b").replace("{date}", date_display) }}
category: AI 자동매매
tags: [AI주식, 자동매매, 한국주식, 랭킹, 종목분석]
---

# {{ pick("title_b").replace("{date}", date_display) }}

{{ pick("intro_b") }}

> 기준일: **{{ date_display }}** | 비교 기준: **{{ prev_display }}**

---

## 오늘의 랭킹 급상승 종목 TOP{{ count }}

{{ pick("riser_intro") }}

{% if skin == 0 %}
### 종목별 상세

{% for r in risers %}
#### {{ loop.index }}. {{ r.name }} ({{ r.sector }})

| 항목 | 값 |
|---|---|
| 전일 순위 | {{ r.prev_rank }}위 |
| 오늘 순위 | {{ r.rank }}위 |
| 순위 변화 | ▲{{ r.rank_gain }}계단 |
| AI 종합점수 | {{ r.final_score }}점 |

{{ r.commentary }}

{% endfor %}

### AI 랭킹이란?

AI 점수(0~100점)는 수익률·확률·기술·품질·수급 등 5가지 축을 종합한 지표입니다.
순위는 전체 유니버스(약 204종목) 내 상대 순위이며, 매 영업일 자동 갱신됩니다.

{% else %}
### AI 랭킹이란?

AI 점수(0~100점)는 수익률·확률·기술·품질·수급 등 5가지 축을 종합한 지표입니다.
순위는 전체 유니버스(약 204종목) 내 상대 순위이며, 매 영업일 자동 갱신됩니다.

### 종목별 상세

{% for r in risers %}
#### {{ loop.index }}. {{ r.name }} ({{ r.sector }})

| 항목 | 값 |
|---|---|
| 전일 순위 | {{ r.prev_rank }}위 |
| 오늘 순위 | {{ r.rank }}위 |
| 순위 변화 | ▲{{ r.rank_gain }}계단 |
| AI 종합점수 | {{ r.final_score }}점 |

{{ r.commentary }}

{% endfor %}
{% endif %}

---

## 관찰 포인트

순위 급상승은 AI 점수 개선을 의미하지만, 단기 급등과는 다릅니다.

{{ pick("watch_point_body") }}

---

{{ pick("closing_b") }}

---

▶ 전체 종목 AI 점수 및 랭킹 확인: [LeeTrader](https://www.leetrader.kr/app)

> **면책 고지**: 본 글은 투자 참고용 정보이며 투자 판단과 책임은 투자자 본인에게 있습니다. AI 모델의 점수·순위는 과거 데이터 기반 통계적 지표로, 실제 수익을 보장하지 않습니다.
